
## 十四、视觉重制：HOMM2 风格化（已完成）

把 Polytopia 式扁平等距换成了 HOMM2 味道的像素俯视风。关键结论：

- **HOMM2 冒险地图其实是 32px 正交方格，不是等距**（等距是 HOMM3 才引入的）。
  因此渲染反而简化：`render/iso.ts` 删除，换成 `render/ortho.ts`，格子坐标 1:1 对应像素。
- **画面全部程序化逐像素生成**（`render/pixel.ts` + `render/atlas.ts`），
  启动时画进一张 1024×1024 图集，运行时只做 `drawImage`，仍是零外部素材、零依赖。
  放大用 `imageSmoothingEnabled=false` 保持硬边。
- 物件按 y 排序绘制（画家算法），城堡/树等高物件正确互相遮挡。
- UI 换成 HOMM2 配色：深木框 + 羊皮纸 + 鎏金边框，衬线标题字。

**踩过的坑**：JS 的普通乘法超过 2^53 会丢精度，导致经典整数哈希退化成
近似常数（表现为满地图同一种装饰）。修复：哈希全部改用 `Math.imul`。

体积：JS 187KB（图集是运行时生成的，不占下载量）。
