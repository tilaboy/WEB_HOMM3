# 可访问性要求（A11y Baseline）· V1

| 项 | 值 |
|---|---|
| 状态 | **定稿 V1**（2026-09-19；A-1~A-4 已拍板，见 §7） |
| 作者 | 林绘澄（美术 / 视觉 / **可访问性**） |
| 触发 | **D-39**（`production/roadmap.md`）：`cartoon-style.md §7` 早就声明「完整分级矩阵是**独立交付物** `design/accessibility-requirements.md`，等待主理人排期」，**却从未排期、文件至今不存在** → 规则只能在每份 UI 规格里各自重申（漂移风险）。本档落成分级矩阵并**收编已发现的实项** |
| 前置 | `cartoon-style.md §7`（色板硬约束）· `design/ux/in-game-ia.md` §4.4 / §5 / Q11 · `design/art-bible/asset-spec.md §2.3.1 / §7` |
| 目标平台 | OPPO PMA110 / Android 16 / DPR 3 / **CSS 视口 792×360 横屏**（手机、触屏、常无实体键盘） |

> ## 0. 一句话
> **本项目目标档 = `Standard`（L2），并额外拉入 2 项 AAA 级**（焦点外观 2.4.11、触控目标 2.5.5）—— 手机上这两项成本近乎为零、收益最大。
> **本文档是 a11y 的唯一权威基线**：任何 UI 规格（含 `in-game-ia.md`）**不得各自重申 a11y 规则**，只能**引用本文档的分级**（如「满足 L2 + 本文档 §4.1 焦点口径」）。
> **a11y 是基础要求，不是锦上添花。**

---

## 1. 分级定义（4 级）

| 档 | 名称 | 一句话 | 基线 |
|---|---|---|---|
| **L1** | **Basic** | 「能用」——不出现阻断性障碍 | WCAG 2.1 **A** 中与游戏相关的子集 |
| **L2** | **Standard** ✅ **本项目目标** | 「能用得舒服」——主流残障场景可**独立**完成核心循环（探索 + 经营） | WCAG 2.1 **AA** + 游戏适配（触控 / 色盲冗余 / 读屏标签 / 减少动效） |
| **L3** | **Comprehensive** | 「深度可配置」 | L2 + 选定 **AAA**（焦点外观 / 44px 目标 / 关键文本 7:1）+ 可重映射 + 字幕自定义 |
| **L4** | **Exemplary** | 「面向辅助技术深度集成」 | L3 + 全键位重映射 + 认知辅助 + 完全冗余提示 |

> **为什么要分级而不是「一档搞定」**：分级让**每份 UI 规格的验收可判定**（「这个按钮满足 L2 的 2.5.5 吗？」），也让**成本可预算**（每上一档要付什么）。没有分级的 a11y 只能变成一堆互不引用的散装规则 —— 这正是 D-39 要根治的病。

---

## 2. 特性矩阵（分级 × 特性）

`✅`=该档包含 · `＋`=该档新增 · `—`=不在该档范围。最后一列是**本项目**的取值。

| # | 特性 | 判据（WCAG） | L1 | L2 | L3 | L4 | **本项目** |
|---|---|---|---|---|---|---|---|
| 1 | 文字对比 ≥ 4.5:1（大文本 ≥ 3:1） | 1.4.3 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 2 | 非文本对比 ≥ 3:1（图标 / 边框 / 焦点） | 1.4.11 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 3 | **焦点可见** | 2.4.7 (AA) | ✅ | ✅ | ✅ | ✅ | **L2（现状部分缺，见 §4.1）** |
| 4 | **焦点外观 ≥ 2px 且 ≥ 3:1** | 2.4.11 (AAA) | — | — | ＋ | ✅ | **⚡ 额外拉入** |
| 5 | 触控目标 ≥ 24×24 | 2.5.8 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 6 | **触控目标 ≥ 44×44** | 2.5.5 (AAA) | — | — | ＋ | ✅ | **⚡ 额外拉入（本项目取 48 主 / 44 常规）** |
| 7 | 键盘可达 + 焦点顺序合理 | 2.1.1 / 2.4.3 (A/AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 8 | 名称-角色-值（图标 / 按钮有可读名） | 4.1.2 (AA) | ✅ | ✅ | ✅ | ✅ | **L2（现状全缺，见 §4.3）** |
| 9 | 标签即名称（可见文字 ∈ 可访问名） | 2.5.3 (A) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 10 | 颜色非唯一手段（色盲 / 灰度可辨） | 1.4.1 (A) | ✅ | ✅ | ✅ | ✅ | **L2（见 §4.6）** |
| 11 | 文本缩放 ≥ 200% 不丢内容 | 1.4.4 (AA) | — | ✅ | ✅ | ✅ | **L2** |
| 12 | 减少动效 / 无高频闪烁 | 2.3.1 / 2.3.3 | ✅ | ✅ | ✅ | ✅ | **L2（现状缺，见 §4.5）** |
| 13 | 音频：字幕 / 文字大小 | 1.1.1 / 1.2.x | — | ✅ | ✅ | ✅ | **L2 档位；本项目 N/A**（音频线确认，见 §6 / §7 A-3） |
| 14 | 可重映射输入（键位 / 手势） | 2.5.1 / 2.1.4 | — | — | ＋ | ✅ | **L3+（暂缓）** |
| 15 | 昼夜光照可关（光敏 / 偏好） | — | — | ✅ | ✅ | ✅ | **L2（已有 `lightingOn()`）** |

> **注**：#4 与 #6 是**主动拉入的 AAA 项**（见 §3）。#14 暂缓（触屏优先、无复杂键位）。

---

## 3. 本项目目标档与「额外拉入」的理由

| 项 | 决策 | 理由 |
|---|---|---|
| **基线** | **L2 Standard** | 覆盖 ~8% 男性红绿色盲、老年低视力、触屏误触三大主要场景，成本可控 |
| **额外 #4 焦点外观 2px** | **拉入** | 现状**已有单点缺陷**（1px，见 §4.1）；统一 `:focus-visible` 是一次改、长期受益，成本 ≈ 0 |
| **额外 #6 触控 44/48** | **拉入** | 真机实测 **p90 = 23px** 落点散布（`in-game-ia.md §5.1`）→ 40px 宽按钮横向余量仅 1px；44/48 是**可用性刚需**，非锦上添花 |
| **暂缓 #14 可重映射** | 不进 L2 | 本作触屏优先、无复杂键位；待有键盘/手柄需求再评估 |

---

## 4. 已发现的实项（D-39 收编 · 逐项带代码证据）

> 证据口径：`git grep` / `rg` 于 `homm-web/src/`。定位一律用**符号 / 选择器 / 关键字**（**不写行号** —— 行号是「会腐烂的坐标」，见 `production/roadmap.md` **D-52**）。

### 4.1 焦点可见性 —— 「对比达标、厚度不足」

| 项 | 内容 |
|---|---|
| **现状** | 全项目**只有一条** `:focus` 规则：`style.css` 的 `.ss-field input[type='text']:focus { outline: none; border-color: var(--gold); }`。焦点指示 = 把默认 `1px` 边框从 `--frame` 换成 `--gold`，**厚度不变（1px）**，并**移除了** outline |
| **量化** | `--gold #f0d488` vs `--frame #8a6a34` → 对比 **≈ 3.5:1**（≥ 3:1 ✅）；厚度 **1px < 2px** ❌ |
| **判据** | **2.4.7（AA）本身满足**；不满足 **2.4.11（AAA 焦点外观）**。即「**对比达标、厚度不足**」 |
| **补充事实** | 60+ 按钮**没有自定义 focus 样式** → 现靠浏览器默认 focus ring；`outline:none` **只加在输入框**、**未全局清除** → 属「**未统一**」而非「被删光」 |
| **处置** | 加全局 `:focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }`（或与既有输入框规则统一，避免两套）。**归属工程**（`style.css` 单写者 = engineering-lead），时机 = UI 重构批 |

### 4.2 既有 emoji 的读屏播报不一致

| 项 | 内容 |
|---|---|
| **现状** | `HUD.ts` 的 `muteBtn` `🔇` / `🔊`（音效）、`lightBtn` `🌗` / `☀️`（光照）。读屏（TalkBack）**播报各家不一**：有的念「静音 / 扬声器」，有的直接跳过，有的念错；`☀️` 含变体选择符 **U+FE0F**，部分平台渲染成缺字框 □ |
| **判据** | 违反 **4.1.2（名称-角色-值）**；且 emoji 全彩 / 抗锯齿 / 尺寸随 OS 变 → 违反 `cartoon-style.md §3.2 色板 / §1.1 描边 / §1.3 整数缩放`（= **G-17**） |
| **处置** | ① 换**图集精灵** `ic_sound_on/off`、`ic_light_on/off`（**已登记** `asset-spec.md §2.3.1`）；② 每个按钮补 `aria-label`（如 `aria-label="音效"`、`role="switch" aria-checked`）。时机 = UI 重构批（与 setup 搬迁同片 DOM）。**归属 = 美术出帧（已登记）+ 工程接线** |

### 4.3 图标不得为唯一语义载体（+ `aria-label`）

| 项 | 内容 |
|---|---|
| **现状** | 顶栏资源项是「**图标 + 数字**」（`HUD.ts` 资源项 `icon.src = atlas.url(\`ic_${k}\`)`），**无 `aria-label`** → 读屏只念出一个孤零零的数字。**`src/` 全目录 `aria-label` 数量 = 0**（`rg` 证据） |
| **判据** | **4.1.2 / 2.5.3** |
| **处置** | ① 「图标 + 数字」项：`aria-label="<资源名> <值>"`（如 `aria-label="金币 12"`）；② 图标按钮**必须有文字标签或 `aria-label`**；③ 拇指带已是文字标签（`in-game-ia.md §4.4 / Q10`）→ **天然达标**，仍建议补 `aria-label` 覆盖缩写（如「日志2」→ `aria-label="事件日志，2 条未读"`）。时机 = UI 重构批 |

### 4.4 触控目标（vs 实测 p90 = 23px 落点散布）

| 项 | 内容 |
|---|---|
| **现状** | 真机实测：误触 0/8，但**落点偏差 p50 = 13px / p90 = 23px**。最小 `宝物▾` = **40×32**（半宽 20px，手指落 19px 处 → 横向余量 **1px**） |
| **判据** | **2.5.8（AA，24px）** / **2.5.5（AAA，44px）**。本项目取 **≥ 44（常规）/ ≥ 48（主操作）/ ≥ 40（密集区，且相邻按钮间距足够）**（与 `in-game-ia.md §5` 一致） |
| **处置** | 尺寸批已把 `.btn.tiny` **32 → 40**（commit `028ec4a`）；其余按 IA §5。⚠️ **判定口径**：`.btn.tiny.tap::after` 用负 offset 扩**点击热区**（仅垂直向）——扩的是**热区**不是**可见尺寸**；对 2.5.5 的判定**以热区为准时 OK、以可见为准时不足**，验收时须写明用哪一种 |

### 4.5 减少动效 / 无高频闪烁

| 项 | 内容 |
|---|---|
| **现状** | **`prefers-reduced-motion` 在 `src/` 中数量 = 0**（`rg` 证据）；现有动画均为 **≤ 4 帧循环**（低闪），**无高频闪烁** |
| **判据** | **2.3.1（三次闪烁）/ 2.3.3（交互动效）**；本项目动效均为低幅（±1px 整数位移 + `squash & stretch`） |
| **处置** | 补 `@media (prefers-reduced-motion: reduce)`：关掉 idle 浮动、缩短面板过渡（**不改整数位移本身**，那是可读性手段）。**归属工程** |

### 4.6 色盲 / 灰度（色板硬约束，引自 `cartoon-style.md §7`）

| 约束 | 值 | 归属 |
|---|---|---|
| 灰度可辨 | 4 族明度台阶相邻差 **≥ 12 L\***（= §2.4 明度签名） | 美术（色板） |
| 色盲可辨 | **禁止**靠 p2 红 / p3 绿的色相差传递**唯一**信息；每族同时具备**形状母题** | 美术（§5 母题是形状） |
| 战斗队伍辨识 | 队伍色环（`cbase_*` 蓝 / 红）对红绿色盲失效 → 建议**形状区分**（我方圆环 / 敌方方环或带缺口） | 美术 + 工程 |
| 光照可关 | `lightLayer.ts` 已有 `lightingOn()` ✅ | 工程（保留 + 设置面板暴露） |
| **地图可达染色（④）** | 见下方专段。**归 §2 #2（1.4.11，信息承载描边）+ §2 #10（1.4.1 冗余）** | 美术（色）+ 工程（常量） |

> **④ 地图可达染色的 a11y 判定（2026-09-21 · 触发：`engineering-lead-2` 实测「纯色相线索、灰度 ~1.1–1.4:1」并问 scope）**
> - **在范围内**；SC 归属 **= §2 #2（WCAG 1.4.11 非文本 3:1）+ §2 #10（1.4.1 非色彩唯一）**。
>   ⚠️ **本行 2026-09-21 归一**（原写「归 #10、**不归 #2**」，与 team-lead 裁决冲突 —— `ux-ia` 报、`68abf37`）：**1.4.11 的 3:1 适用** —— 可达 / 不可达是**信息承载的 UI 元素**（不读它下不了令），**不是画面层**（team-lead 裁定 · `in-game-ia.md §15.4` / `6bb923c`）。
>   **但"量的对象"要分清**（这是原写法的正确内核，保留）：3:1 量的是**承载信息的"边界描边" vs 其相邻色** —— 描边的相邻色**是确定的**（线两侧那两格地形），**不是**"半透明整格填充 vs 无界地形"。**蓝填充**只是**冗余"区域水洗"**（色觉正常者读得到；色觉障碍者另有描边兜底）⇒ **不单独套 3:1**。⇒ 两条不矛盾：**3:1 打"描边"、不打"填充"**。
> - **判据**：可达 / 不可达**两态在灰度下彼此可辨** —— 量化锚在 WCAG 1.4.11 的 **3:1**（量「状态 vs 其底」），**不套**「vs 无界地形 3:1」。
> - ★ **验收契约（team-lead 钉死 2026-09-21；「怎么测」不再是可选项）**：
>   1. **口径 = A/B「状态 vs 其底」**（工具 `tools/reachmeas.mjs`）—— **不是「空间口径」**（那套已被"被染格恰好是亮地形"污染 ⇒ **已弃**，仅存历史）。
>   2. **`population` = 按地形逐类**（沙土 / 草亮 / 草中 / 草深 / 水 / 暗缝），**不合并**。
>   3. **决策规则 = ★ 每一类都必须达 WCAG 非文本 3:1（中位）** —— **不是「全图统一 `population` 的中位」**。
>      **理由**：`1.4.11` 管的是「**能不能识别出那个信息**」；**玩家站在草深上读不出边界 = 在那里它就是不可识别的** ⇒ **不能靠「多数地形过」把那一类洗掉**；且它**比「统一中位」更抗操纵**（中位可被大片亮地形拉过去）。
>   4. ★ **分类帧 = 身份帧（光照恒等帧 · 正午 `none`）** —— 「**地形**」是**身份**，不是"此刻看起来多亮"。
>      ⇒ `reachmeas.mjs` **必须带第 4 参**（正午 `none` 的 before 底图）；不给则退回同相位并**告警**（**与 `tintab` 不可拼表**）。
>      **理由（实测）**：用**同相位**分类时，夜间 multiply 把亮草压到 `L*<55` ⇒「草·亮」在夜间**几乎不存在** ⇒ 第 3 条会被「**该类在本相位不存在**」**真空通过** ⇒ 那正是"死区洗白"。
>   5. ★ **每个数必须自带 `population(n)` + 中位 + `≥3:1` 占比**；**`n<200` 不得判「过」**，**且汇总里 `n<200` 的类不许计入「通过」**。
>      **理由（血案）**：本表夜间「草·亮 `5.43` / `≥3:1` 100%」曾被读成**过关**，实际 **`n=5`** —— **没有 `n` 一列，就把真空桶记成了通过。**
>   ⇒ **`population(n)` 与统计量必须逐条写在数旁边**（不许脱离 `population` 引用）。
> - **现状 = FAIL**（`engineering-lead-2` 真 Chrome CDP，seed 20260917；**两个口径都记，量化锚用 A/B 口径**）：
>   - **A/B 口径（★ 唯一验收口径 · 契约第 1 条）** —— 染色开/关、**同页同相机、同像素同地形**，只差染色层（**去掉"被染区恰好是亮地形"的混淆**）：**WCAG 对比度中位 1.15(午) / 1.12(夜)**，max 2.93 / 2.43，**≥3:1 的像素 = 0.0%**；染色占屏 **19.4%(午) / 19.3%(夜)**。
>   - **⛔「空间口径」（已弃 · 仅存历史，勿再引用作判据）** —— 真实被染格 vs 紧邻未染格（钩子记录被染集、不靠猜）：**被染蓝 vs 未染底 1.74(午) / 1.53(夜)**；**红格缘 vs 未染底 1.18(午) / 1.12(夜)**；逐对 **min 1.00**（有格完全分不开）。
>   - **⇒ FAIL 成立**：两态在灰度下都远低于 3:1。非色线索现为「可达 = 蓝填充 / 不可达 = **红格缘**」= *填 vs 缘* 的形状差 —— **部分**满足 1.4.1；但**"不可走"侧的唯一载体（红缘）自身只有 1.12–1.18** ⇒ 工程侧删掉 `0.07` 红填充**没解决夜读**，反而**坐实"锚必须打在边上"**（纯色相：b−r≈86–100，亮度分量仅 ~7）。
>   - **复算对照（`engineering-lead-2` 直连口径 · 旁证 · **非契约口径**）**：蓝 **1.14** · 红缘 **1.05** ⇒ 与 A/B 口径（1.15 / 1.12）**同量级、差在口径**。**以本节 + `reachmeas`（契约口径）为全仓唯一权威数**（`in-game-ia.md §15.4` 已指向本处、不另存第二份）。
> - **「两把尺子相反」的断法**：`cartoon-style.md §3.1.1` 的 ΔL\* 尺子（0.49 / 2.22）答的是「**纹理看不看得见**」；WCAG 3:1 答的是「**两个状态分不分得开**」。对**二值状态线索**，**以 WCAG 为准** —— ΔL\* 只证「变化是真的」，不证「状态可辨」。⇒ `engineering-lead-2` 的发现**成立，非误报**。
> - **处方（team-lead 2026-09-21 裁定取 **(α)**）**：给"可达区**边界**"一个**亮度锚**，而不是在色相上再叠色：
>   **(α) 已落** —— 边界改成「**墨色 `ink0` `#2a1a12` 外描边**（复用 `cartoon-style.md §1.1`，**不透明** ⇒ 灰度高对比）+ **内侧红**（色相锚）」。落点 = `MapRenderer.ts` 的 `NOGO_EDGE_INK` / `NOGO_EDGE` / `EDGE_W`（**未动别处**）。**位置选"边"而非"填充"，正是被"红缘 1.12–1.18 是最弱一环"倒推出的**（填充那侧 1.53–1.74 已略强于边，且填充面积大、改了更伤观感）。
>   未采纳：**(β)** 可达侧也描同类格缘（格缘本身无亮度分量 ⇒ 解决不了）；**(γ)** 可达格叠 cost 数 / 点纹（最稳但最贵、要改管线）。
> - **状态**：✅ **(α) 已落（2026-09-21）**；**数值复测已入 `shots/`**（真机视口 **792×320@DPR3** · **A/B 口径** · **绑构建 `68abf37`→`3e8084f`**）：
>   **★ 判词（契约第 3 条「每一类 × 每一相位都必须 ≥3:1」）＝ `(α)` FAIL。**
>   **依据（**身份帧** · 契约口径 · 同构建 `i_reach_*` · 三跑互为校验 · 工具 `0fc66e6`）：**
>
>   | 类（`population` 全称） | 正午 n | 正午中位 | 正午 `≥3:1` | 深夜 n | 深夜中位 | 深夜 `≥3:1` | 判 |
>   |---|---|---|---|---|---|---|---|
>   | 沙/土（暖亮） | 7580 | **8.98** | 100.0% | 6834 | **5.12** | 99.4% | ✅ ✅ |
>   | 草·亮 | 873 | **4.72** | 100.0% | 727 | **2.92** | 42.4% | ✅ ❌ |
>   | 草·中 | 2798 | **3.72** | 84.1% | 2322 | **2.43** | 0.0% | ✅ ❌ |
>   | 草·深 | 100 ⚠️ | **2.33** | 0.0% | 90 ⚠️ | **1.69** | 0.0% | ❌ ❌ |
>   | 水（蓝主导） | 5378 | **3.04** | 59.5% | 5379 | **2.14** | 4.5% | ✅ ❌ |
>   | 暗(物件/接缝) | 15 ⚠️ | **1.16** | 0.0% | 517 | **1.13** | 0.0% | ❌ ❌ |
>   | 其他（兜底桶） | 230 | 3.34 | 89.1% | 312 | 2.26 | 7.4% | — |
>
>   ⚠️ = `n<200` ⇒ **不得判「过」、也不计入「通过」**（契约第 5 条）。
>   ⇒ **结论：`(α)` FAIL —— 夜间 6 类里 5 类不过（只有「沙/土」过）**；正午「草·深 / 暗缝」两类也不到 3。
>   ⇒ **`(ii)` 的验收目标 = 让每一类 × 每一相位都 ≥3:1**（**不是**把中位抬上去）。**⇒ 死格不止两个。**
>
>   **⛔ 取代横幅（`f35b34f`：不静默替换）—— 本节曾有的那张夜间逐类表已失效，勿再引用：**
>   旧记：`沙/土 5.12 · 草·亮 5.43 · 草·中 2.92 · 水 2.14 · 草·深 2.43 · 暗(物件/接缝) 1.14`（午）：`沙/土 8.98 · 草·亮 4.72 · 草·中 3.72 · 水 3.04 · 草·深 2.33 · 暗 1.16`。
>   **失效原因 = 它用「相位帧」分类**（`0fc66e6` 已把该表**逐字复现** ⇒ 口径**确证**，非推测），而契约**第 4 条**要求**身份帧**。
>   **最要命的一格**：旧表夜间「**草·亮 `5.43` / `≥3:1` `100%`**」看着**过关**，实际 **`n=5`** —— 夜间 multiply 把亮草压到 `L*<55` ⇒ 该类在夜里**几乎不存在** ⇒ **5 个像素的"真空通过"**。
>   ⇒ **"为什么当初会出这个错" = 缺 `n` 一列 + 用了相位帧。** 这两条现已是**契约第 4、5 条**（本节上方）。
>
>   **同一事实的另一种说法（保留，勿当结论）**：「状态 vs 其底」**全图统一 `population` 的中位 = 2.93（午）/ 2.08（夜）**、≥3:1 像素 44.6% / 25.3%
>   —— **「中位不过」会让人误以为是全局性问题**，真因**逐类不过** ⇒ **以逐类判为准**。
>   ⚠️ **防误引（更正我此前两处错说 · V1.10）**：早前那批逐类小数**可复现**（`0fc66e6` 已逐字复现 ⇒ 它就是**相位帧**读数）—— **不是「跨相位错桶产物」、也不是「未入库即无效」**；其**正午列与身份帧逐格相同**（正午 ≡ 身份帧），**只有夜列错桶**（草三行整体下移一格）。⇒ **作废的是「夜列」、理由是契约第 4 条（分类帧），不是「不可复现 / 未入库」** ⇒ **别引旧表夜行当验收数**。
>   ⚠️ **原「两个未锁边界」的处置（2026-09-21 · 我复算后；`engineering-lead-2` 同证）**：
>   ① **`草·深` 样本薄 —— 可撤销**：`n=100/90` 是**只数 `±3` 墨核**时的数。按契约「状态 vs 其底」的**全变化人口**，`草·深` **`n=475`(午)/`416`(夜) ≥ 200`**，且**仍 FAIL（`1.58` / `1.65`）** ⇒ **样本薄不再挡结论**；**两种人口同判 FAIL**。
>   ② **跨相位分类帧「同页同相机」—— 已闭**：`engineering-lead-2` 的**维度守卫**（身份帧/相位帧尺寸不等即 `throw`）+ **同桶 `n` 跨相位恒定**（`grass-bright` 42256/42256/42256）证明同坐标系；我侧独立旁证：**水 `n` 跨相位近乎恒定（`5 378`/`5 379`）**（水是帧无关类）。⇒ **「用正午 `before` 当夜间分类帧」成立，不必再跑第二遍。**
>
>   ⚠️ **新增未锁（我实测 · 待 team-lead 裁）：`i_reach` (α) 对**疑非单变量**。**
>   **非墨色、且不与墨核 8 邻接**的变化占非墨变化的 **81%**（午 `17 346 / 21 460`），且其 `after` **比「墨」与「底」都更暗**（如 `rgb(24,18,17)`）⇒ **抗锯齿解释不通**（混合不可能比两端都暗）。
>   **最可能根因** = `before`/`after` 出自**两个不同构建**（`68abf37` ↔ `3e8084f`）⇒ 其间任何其它漂移都会落在**高频区（物件 / 投影 / 岩）**而被记进变化集。
>   **建议**：加**默认为空操作**的 `?devtint=0`（跳过可达染色层；惯例同 `?devshade=0` / `?devregion=0`）⇒ 出**同构建 / 同会话 / 同帧 / 冻帧**的真 A/B ⇒ 这条与 `#128` 的 (ii) 素材**一并解决**。
>   ⚠️ 该疑点**不改现行判词**（判据在**两种人口下同判**：暗 `1.16` vs 全变化 `1.20`；夜 `1.13` vs `1.11`），但**削弱 `i_reach` 对的「干净单变量」资格** ⇒ 引用其逐类数**须连此保留一起说**。
>   ⚠️ **推及全文的读法**：`reachmeas` 逐类那列的人口 = **`±3` 墨核**，**对暗底偏置**（暗底上墨达不到纯墨 ⇒ 暗类墨核 `15/517`，而实际变化 `11 094`/`10 785`）⇒ **「某格 `n=15`」≠「该类只有 15 个像素」**。
>
>   ⚠️ **水的量级更正（回应 `engineering-lead-2` 的核数请求）**：水**不是** `1.80`/`1.55`（那是**动画污染**；`5ed22d3` 已冻帧）。我的冻帧对（身份帧）：**水面积 `283 098`、水上变化 `7 537`/`7 535` = 该类的 `2.7%`**、WCAG 中位 **`2.97`(午) / `2.10`(夜)** ⇒ **冻结后仍 `< 3` ⇒ 水仍判 FAIL**（**是真死角，但量级不是 40k**）⇒ 见 `production/roadmap.md`「水/暗缝待重测」，**重测目标值 ≈ `2.97`/`2.10`**。
>   **复算（契约口径 · 必带第 4 参 = 正午 `none` 底图）**：`node tools/reachmeas.mjs ../design/art-bible/shots/i_reach_after_noon.png ../design/art-bible/shots/i_reach_before_noon.png 正午 ../design/art-bible/shots/i_reach_before_noon.png`（全表 = `shots/README.md`「★ `shots/` 判定」）
>   **⛔ 作废**：早前「Δ>40 核心墨像素」子选得 4.37 / 2.86 —— **拿结果筛人口**，作废。
>
> - **★ `(ii)` 双色 halo 复核（`#124` / `#128` · 2026-09-21 · 判定方）—— 仍**未达**「每类 × 每相位 ≥3:1」**：
>   **口径/来源**：`tintab`（**冻结版**，输出自印「动画: 已冻结 performance.now」）+ **身份帧** + 三相位；**统计量 = WCAG 中位**。**来源分工（team-lead 已确认）**：**逐类 = `tintab`；全图 A/B = `reachmeas`（旁证腿）**。
>   **绑构建**：`src f971db7a` / `dist 13d3b420` / `main ecbfa1e2` / `HEAD ec3ae85c`（`tintab` 自证三件套①②③ 全 OK：启动即核 `src≠dist` 一致、测前=测后指纹未变、**测前=测后 HEAD**）⇒ **非历史读数**。
>   **判据**：亮芯/暗墨**两锚落在同一格地形** ⇒ 设计自述「暗底吃亮芯、亮底吃暗边」⇒ **两极其一 ≥3:1 即算过**（§4.6 (a-1)/(ii)）。**⚠️ 不是**「四条线全 ≥3:1」—— 半透明蓝填 `α.16` / 红 `α.42` **天生低**，那样读任何方案都过不了。**此读法待 team-lead 确认。**
>
>   | 类 | 正午 亮芯/墨 | 晨昏 亮芯/墨 | 深夜 亮芯/墨 | 判 |
>   |---|---|---|---|---|
>   | 草·亮 | 2.53 / **5.18** | 2.52 / **4.07** | 2.30 / **3.18** | ✅✅✅ |
>   | 草·中 | 3.43 / 3.52 | 3.34 / 2.86 | **2.94** / 2.38 | ✅✅**❌** |
>   | 草·深 | **6.79** / 1.81 | **6.06** / 1.60 | **4.74** / 1.44 | ✅✅✅ |
>   | 沙/土 | 1.72 / **7.43** | 1.64 / **6.07** | 1.62 / **4.32** | ✅✅✅ |
>   | 水 | **4.71** / 3.05 | **4.79** / 2.38 | **3.64** / 2.18 | ✅✅✅ |
>   | 暗(物件/接缝) | **4.00** / 1.32 | 3.46 / 1.24 | **2.77** / 1.18 | ✅✅**❌** |
>   | 岩(rock) | 3.18 / 1.56 | **2.83** / 1.44 | **2.36** / 1.29 | ✅**❌❌** |
>
>   失败格**全部 `n≥200`**（晨昏岩 `n=239`、夜草中 `n=1239`、夜暗 `n=1579`、夜岩 `n=239`）⇒ **可判、非腾空桶**；`墨(岩) n=153 <200` ⇒ 按契约第 5 条记**样本不足、不得据此判过**；`蓝fill(水) n=0`（水不可达 ⇒ 无蓝填，非缺陷，按第 5 条亦**未判**）。
>   ⇒ **`(ii)` 判 FAIL**（正午**全过**；晨昏 + 夜各有过不了的类）。
>   ⇒ **但 `(ii)` 把 `(α)` 的「草·深」死格救活了**（亮芯三相位 `6.79/6.06/4.74` 全过）⇒ `-2` 的预判成立。**死格换位**：`(α)`「草·深 + 暗缝」→ `(ii)`「**岩（晨昏+夜）/ 草·中（夜）/ 暗缝（夜）**」。**「死格不止两个」仍成立，只是集合变了。**
>
>   **⚠️ 机制线索（给 owner，未定论）**：亮芯三相位**单调走低**（暗 `4.00→3.46→2.77`、岩 `3.18→2.83→2.36`）—— **同一对色、只因相位变暗就掉** ⇒ 选色解释不了。`MapRenderer.ts:462-468` 自述「世界坐标在 `ctx.restore()` 结束，`applyLighting` 是屏幕空间且在实体循环之后」，而 halo 画在世界坐标里 ⇒ **疑 halo 落在光照乘之前**（夜间整条线被乘暗 ⇒ 亮极被压）。**若属实 = 管线层，不是色板层。**
>
>   **`#128` 观感（真机图已抓 · 人眼过）**：`SHOT_FREEZE=1` + `devzoom=2`（真机视口，canvas 1584×480）。
>   ① 总宽 `3×EDGE_W = 6px` ✓（= 上限）｜② 三色均在既有 `REACH/NOGO` 常量内 ✓｜③ 同格双极（亮芯+暗墨同格）✓｜④ 见上表。
>   ⑤ **语义可读**：日间尚可（亮芯+暗墨成明确断线）；**夜间那几格按 ④ 即"读不出"** ⇒ **⑤ 随 ④ 一并不过**。
>   ⑥ **无内部网格线**：放大图里染色格内部**未见**网格线（只在可达/不可达前沿描边）✓。
>   ⚠️ **本观感读数只覆盖 `devzoom=2` 一档（= 可达区放大的最有利视角），且未做 `(ii)` 的"染色开/关"同构建 A/B** ⇒ 待 `?devtint=0` 那类入口到位后补（见上「新增未锁」）。
>
>   **⛔ 不移除旧表（不静默替换）**：本块是**新增**，**不改**上面 `(α)` 那张表的判词 —— `(α)` / `(ii)` 是**两次不同实现、各自绑各自构建**的读数，**两张表并存**。

> ### ★ 两条 a11y 裁定（2026-09-21 · 应 team-lead 请裁；**裁定权在本文档**）
>
> **(a) 夜间是否仍以 WCAG 3:1 为判据？ —— 是：判据不随相位变，夜间不豁免、不降尺。**
> - **1.4.11 无「夜间」豁免**：它管的是**渲染出来的状态**的可辨性；夜间是渲染结果的一部分，**不是**另一种可豁免的呈现。
> - **降夜间尺 = 正被禁的「调到达标」**（`production/roadmap.md` `bd4ebb0`）。若因夜间过不去就把夜间阈值降到能过的水平，等于绕开与 `a35f923`（阈值必须与所守护代码同构建）同源的纪律。
> - **`ΔL* 16.8` 与 `1.56:1` 不矛盾、也不互替**：前者是「**看得出**」（感知量），后者是「**达不达标**」（规范量）。按 `3f11752`（★ **不是"把 ΔL\* 换成 ΔE\*ab"，而是"判据必须说清它回答什么问题；两个方法的数不能共用"** —— 此读法经 team-lead 确认、并更正了他先前"尺子改 ΔE\*ab"的窄表述），**判据仍是 WCAG 3:1**；`ΔL*` 只作**旁证列**，**不能顶替 1.4.11**。
> - **⇒ 夜间墨线对草地 ≈ 1.56:1 时，「夜读 FAIL」如实成立**（与我 V1.2 的 **1.12(夜)** 同向；(α) 已改善但仍不足）。**不换尺子、不叠下一层。**
>   - **⚠️「不叠下一层」禁的对象（写死，防误禁标准技法）**：禁的是「**为凑一个标定值而叠加的装饰性色层**」（= `5.09` / 「调到达标」那一类）。**不在禁列**：(a-1) 的 **双色 halo（亮芯 + 暗边）** —— 它是**同一个「边」语义载体**在**两个底色方向**上各给一条，**不是两层装饰**（1.4.11 本就要求对**两侧**相邻色**各** ≥3:1）。⇒ **判 (a-1) 可做，不算"叠层"。**
> - **处置（要求，本轮不实现）**：④ 是**信息层**，不该被氛围层调暗。三条路：
>   - **(a-1) 「双色 halo（亮芯 + 暗边）」【推荐】** —— 原写「相位感知描边」，2026-09-21 按 `ux-ia` 提法**归一命名**（同 = team-lead 的 **(ii)**，勿再当两套处方）：**边界同时给亮芯 + 暗边**，使描边对**亮底与暗底各自 ≥3:1**（暗底吃**亮芯**、亮底吃**暗边**）。**成本小**（④ draw 块再画一条更亮的线）；归属 `MapRenderer.ts`（engineering-lead）。
>     - **为什么主句必须是"双色"而不是"整条换亮色"**（`ux-ia` 指出 · 我按**绑构建读数**确认）：**同一相位内"极亮底"与"极暗底"同时存在** ⇒ **整条换成任何单一颜色，必把其中一类留在失败侧**。实测（见 §4.6 状态行）：单条暗墨在**亮底过**（沙/土 **8.98**）、**暗底败**（草·深 **2.33** / 暗 1.16 / 夜 **2.08**）⇒ **"整条换亮色"只会把失败从暗底挪到亮底**（= team-lead 判 (i)「换极性」为伪修法的同一逻辑）。⇒ **必须双向。**
>     - **「相位感知」降为可选增益**（按相位微调亮芯亮度或可让某侧更稳），**不靠它救夜读**。
>   - **(a-2) 让 ④ 免于 `applyLighting` 的 multiply 【不推荐】**：④ 画在**世界坐标**、光照是**屏幕空间**且在**实体循环之后**（源码注释已写明）⇒ 搬到光照之后会**既错坐标、又盖住单位**。
>   - **(a-3) 不修、如实记 FAIL**：`#15 光照可关` 仅作**缓解**，**不是** 1.4.11 的合规替代（1.4.11 没有「关掉功能即合规」的机制）。
>
> **(b) 「全色相」是否构成 1.4.1 失败？墨线的「边界形状」够不够当非色兜底？ —— 不构成失败；(β)/(γ) 都不需要。**
> - 现编码**不是「全色相」**，它有**两条非色腿**：
>   1. **形式腿（面 vs 边）**：可达 = 整格**面**水洗（`REACH_TINT`）；不可达 = 只在**邻接可达区那一侧**画**线**（`NOGO_EDGE` + `NOGO_EDGE_INK`，`EDGE_W`）。灰度下「一片被洗过的**面**」与「一条**暗边**」是**结构差**，与色相无关。
>   2. **明度腿**：外侧墨 `#2a1a12` **不透明** ⇒ 自带明度分量（= (α) 的贡献）。
> - ⇒ 「纯色相线索、灰度 1.1–1.4」这句**只对「水洗」那一半成立** —— **这正是 (α) 把明度锚改打在「边」上、而不再加在「填」上的原因**。把「水洗灰度低」读成「整个编码色相唯一」，是**把半个线索当了全部**。
> - ⇒ **1.4.1（非色彩）满足**（形式腿**相位不变**）。**不要 (β)**（可达侧也描格缘：格缘本身无亮度分量，与 (a) 同病，**解决不了夜读**）；**不要 (γ)**（点纹 / cost 数：最稳但**改管线**、伤观感，**留备选** —— 仅当 (a-1) 仍推不到 3:1 才上）。
>   **成本**：(β) ≈ **0 但无效**；(γ) ≈ **高**（图集点纹 + 逐格覆盖 pass + 读 `cost`；跨 `atlas.ts` / `MapRenderer.ts`；需冻结 + 复测）。
> - **边界（诚实）**：明度腿的**强度**受 (a) 制约 —— 夜间 1.56:1 ⇒ 该腿夜间变弱。故 **(b) 的「够」以 (a) 的处置落地为前提**：**1.4.1（结构性）现在就满足**；**1.4.11（定量）待 (a) 处置**。
>
> **⚠️ 口径警示（判定方必写，防「并排即同源」）**：本轮流转的 **1.56 / 2.41 / 3.46:1** 与 **ΔL\* 16.8** 系 **team-lead 转述**；我在**仓库与临时产物里未定位到其「绑构建」记录**，且并行实例的两次 `tintab` 运行**墨族 `ink×0`（一次都没画到、非零退出）**、其中一次的 `NOGO_EDGE_INK` 被解析为 `rgba(0,0,0,0)`（= 正在跑 **ink-off 负测**的中间态）。
> ⇒ **上面两裁定只依赖「判据本身」，不依赖该数**；**该数在「G1/④ owner 于冻结窗口内按 A/B 口径复算并入库」之前，不作判据、不入验收**。
> - **✅ 复算已做（2026-09-21 · `art-director` 生产方）**：绑构建 `68abf37`→`3e8084f`、真机视口、A/B 口径 ⇒ **2.93（午）/ 2.08（夜）**（见上方状态行）。**转述的 1.56 / 2.41 / 3.46 仍不入验收**（未定位到绑构建出处）；**以本节 + `shots/README.md` 的绑构建数为准**（仍受「阈值须与所守护代码同构建」约束：④ 代码再改须重测）。
> - **✅ `tintab.mjs` 墨族 gap —— 已修（`6867cff`）**：原解析器只认 `'rgba(...)'`、会**静默漏掉 `#hex` 的墨**（`NOGO_EDGE_INK = '#2a1a12'`）⇒ `none`/`edge` 都漏不掉墨 ⇒ 净贡献是对**含墨的底**算的 ⇒ **系统性错归因**（与上面「被解析为 `rgba(0,0,0,0)`」同源）。**owner `engineering-lead-2` 已修**（补 hex / 不透明色解析 + `src≡dist` 前置校验 + 0 命中硬守卫）。
>   **修复后实跑交会**（桌面 1280×757 emulation；墨 vs 底）：uniform-grass **正午 3.40 / 夜 2.58**、sand **6.15 / 3.82** ⇒ **与 `shots/` 的 A/B 口径（2.93 / 2.08）同向、同判 FAIL（夜不过）**。**口径分工（team-lead 裁 2026-09-21「一个验收数一处产」；登记见 `production/roadmap.md` `4bd1851`）**：`tools/reachmeas.mjs`（A/B「状态 vs 其底」）= **④ 验收数唯一出处**；`tintab.mjs`（差分净贡献 + population + ΔE\*ab + 色盲）= **参考**旁证 —— 按登记**其 WCAG 列标「参考」、不作验收数**。（原写"`tintab` 可作唯一源"与登记冲突，**已按登记更正**；`engineering-lead-2` 报此张力。）
>   **⛔ `imagecmp.mjs` 的 `CMP_EDGE=<hex>` 已撤**（`f740ec3` 加 · `bf92be4` 裁撤）：它量该色**整图所有出现**、**不是**只量 ④ 格缘 ⇒ **当不了验收数** ⇒ 既然当不了，就**不留第二把尺**（**撤的是代码；下表那行数留痕备查、工具不再提供**）。
> - **🔖 每个数必须自带 `population`（`ux-ia` 报「3.40 与 2.93 正午相反」后自行复现并撤回 ⇒ 写成硬性口径）**：**同一个量（墨描边 vs 其底）、两个 population，后者低是应该的、不是矛盾**：
>   | 数 | population（必写） | 来源 |
>   |---|---|---|
>   | 正午 **3.40** / 夜 2.58 | **uniform-grass**（受控图：全草、无物件） | `tintab.mjs` |
>   | 正午 **2.93** / 夜 **2.08** | **真机试玩图**（全地形 + 物件；population = **全部变化像素**） | `reachmeas.mjs`（`shots/` A/B 口径，**本节权威数**） |
>   | 正午 **1.29** | 真机试玩图；population = **全部 `ink0` 像素**（含物件描边） | `imagecmp.mjs CMP_EDGE=2a1a12` **【该模式已撤 · `bf92be4`】** —— 数留痕备查 |
>   ⇒ **不许把任一数脱离 population 引用**；三数**都判 FAIL（夜不过）**，结论一致。

---

## 5. 验收口径

**可机检（自动化，进既有探针批）**
1. **UI 壳**对比度：前景 / 背景对 ≥ 4.5:1、非文本（图标 / 边框 / 焦点）≥ 3:1（抽查木框 + 羊皮纸配色，`cartoon-style.md §7` 已提示可能不足）。**地图覆盖层**：**可达性「边界描边」套此 3:1**（WCAG 1.4.11，量「描边 vs 其相邻色」）；**蓝填充是冗余水洗、不单独套**（见 §4.6 专段）。
2. **无 emoji 当图标**：`rg "🔇|🔊|🌗|☀️" src/` 应为 0（G-17 防回归）；图标**只来自** `atlas.url()`。
3. **每个 `.icon` / 图标按钮有可访问名**：`rg 'class="[^"]*icon'` 的每处须有 `aria-label` 或相邻文字。
4. **焦点环厚度**：`focus-visible` 规则存在且 `outline-width ≥ 2px`。
5. **减少动效**：存在 `@media (prefers-reduced-motion: reduce)` 块。
6. **无全局 `outline: none`**（除已声明的受控例外）。

**手检（真机走查）**
7. 键盘 / TalkBack 走查：顶栏（图标 + 数字）、拇指带（文字标签）、setup（音效 / 光照开关）—— 逐项播报**有意义**且**不重复**。
8. 焦点环截图（≥ 2px、金对棕 ≈ 3.5:1）。
9. 文本缩放 200%：不裁切、不重叠。
10. 色盲模拟（灰度 + 红绿）：4 族可辨、队伍可辨；**地图可达染色两态在灰度下可辨**（可达 vs 不可达 ≥ 3:1，**判据全天候含夜、不随相位放宽**，判据与现状见 §4.6 专段 + 裁定 (a)/(b)）。

---

## 6. 与其它文档的接口（避免重复重申）

| 来源 | 内容 | 本文档角色 |
|---|---|---|
| `cartoon-style.md §7` | 色板硬约束（灰度 / 色盲 / 对比 / 闪烁） | **本文档 §4.6 收编**；§7 保留为「色板视角」 |
| `design/ux/in-game-ia.md` §4.4 / §5 / **Q11** | 图标非唯一语义载体；尺寸 44/48/40；缺 a11y 基线 | **Q11 由本文档闭合**；IA 规格改为**引用本文档分级** |
| `asset-spec.md §2.3.1` | `ic_sound_on/off`、`ic_light_on/off` 帧名 | §4.2 引用的图标来源 |
| `homm-web/src/style.css` | 焦点 / 触控 / 动效实现 | **单写者 = engineering-lead**；本文档只提**要求与判据**，不写实现 |
| `design/audio-direction.md` | 音频 / 字幕 | #13 的字幕项**已对齐（音频线 = `audio-lead`）**：结论「**否 / N/A**」，依据 `audio-direction.md §14` |

---

## 7. 决策记录（A-1 ~ A-4 · **已拍板**）

| # | 问题 | 选项 | **裁决（team-lead / 用户）** |
|---|---|---|---|
| **A-1** | 目标档定哪一档？ | (a) L2 Standard；(b) L3 Comprehensive | ✅ **(a) L2 + 2 项 AAA**（焦点外观 2.4.11 / 触控目标 2.5.5）。理由加强：那 2 项 AAA 是本项目**已实测暴露**的，不是按规范大全凑的 |
| **A-2** | 色盲支持 = 可切换开关 还是 设计层冗余？ | (a) 开关；(b) 冗余 | ✅ **(b) 设计层冗余**（形状母题 + 明度签名 + 数值本就双通道）。加开关会引入第三种配色状态、成本更高 |
| **A-3** | 字幕（#13）是否要做？ | (a) 全套；(b) 仅关键音；(c) 不做 | ✅ **(c) 不做 = N/A** —— 音频线已回：**全 11 个音效 / 12 个触发点都已有同步视觉反馈，缺陷清单为空** ⇒ 字幕**不必要**（**明确 N/A，不是「未满足」**）。依据 = `design/audio-direction.md §14`（音频线 = `audio-lead`） |
| **A-4** | 验收探针并入既有探针批 还是 另开？ | 并入 / 另开 | ✅ **并入** engineering-lead 既有探针批（D-42 批）。§5 的可机检项与「顶栏纵向守卫（`max(child.offsetHeight) ≤ topbar.offsetHeight`）」**同批**，不另开 |

> **A-3 的「L2 却 N/A」不矛盾**：#13 在 §2 矩阵里属 L2 档（基线**包含**字幕项），但本项目经音频线证实**不适用** → 标 **N/A**。**档位是基线，N/A 是经证实的不适用**，两者不冲突。

---

## 8. 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| **V1** | 2026-09-19 | 首版（D-39 交付）：建立 L1–L4 分级矩阵；目标档 L2 + 2 项 AAA；收编 6 项已发现实项（焦点 / emoji / 图标语义 / 触控 / 动效 / 色盲）；定义验收口径与文档接口 |
| **V1（定稿）** | 2026-09-19 | **A-1~A-4 拍板**（见 §7）：A-1 目标档 **L2 + 2 项 AAA**；A-2 色盲 **设计层冗余**；A-3 字幕 **N/A**（依据 `audio-direction.md §14`，音频线 = `audio-lead`）；A-4 探针**并入** D-42 批。状态：建议稿 → **定稿**。**归属更正**：音频项一律写「音频线 / `audio-lead`」（`audio-director` 在本环境工具表失效） |
| **V1.1** | 2026-09-21 | **§2 #2 的 3:1 范围写死 = UI 壳**（原只写"抽查木框+羊皮纸"，未声明是否含地图覆盖层 → 正是本轮 scope 争议根因）。**新增 §4.6「④ 地图可达染色」专段**：归 **§2 #10（1.4.1）**、**不归 #2**；判据 = **两态灰度下 ≥3:1**（非「vs 无界地形 3:1」）；现状 **FAIL**（灰度 1.25–1.44:1）；处方 **(α) 边界加 `ink0` 亮度锚**。同步改 §5 #1（写死 UI 壳）+ #10（增地图两态灰度可辨）。触发：`engineering-lead-2` 真 Chrome 实测 + 问 scope |
| **V1.2** | 2026-09-21 | **§4.6 专段的量化细化**（`engineering-lead-2` 补测）：① 记**两个口径** —— **A/B 口径**（同像素同地形、只差染色层）**中位 1.15(午)/1.12(夜)、≥3:1 像素 0.0%**、染色占屏 19.4%/19.3%；**空间口径** 被染蓝 vs 未染底 **1.74/1.53**、红缘 vs 未染底 **1.18/1.12**、min **1.00**；② **写死量化锚 = A/B 口径**（去掉"被染区恰好是亮地形"的混淆）；③ 结论 **FAIL 不变**，**红缘为最弱一环** ⇒ 进一步支撑 (α)「锚打在**边**上」。触发：`engineering-lead-2` 补齐数并接受 scope 裁定 |
| **V1.3** | 2026-09-21 | **team-lead 裁定取 (α) 并落地**：§4.6 处方改为「已落」——`MapRenderer.ts` 的 `NOGO_EDGE_INK`/`NOGO_EDGE`/`EDGE_W`：**内侧红（色相锚）+ 外侧不透明墨色 `ink0`（明度锚）**，把"靠色相"改成"靠明度"；**未采纳 (β)/(γ)** 及理由入档。**状态 ✅ 已落**；数值复测按 **A/B 口径**、真机视口、**绑构建指纹**，**入 `shots/`**（若仍未过 3:1 → 如实记，不换尺子）。触发：team-lead 裁定 + `art-director` 落地 |
| **V1.4** | 2026-09-21 | **SC 归属归一**（`ux-ia` 报跨文档冲突 · `68abf37`）：§4.6 原写「归 #10、**不归 #2**」，与 team-lead 裁决（尺子 = **WCAG 非文本 3:1**；可达染色是**信息承载 UI 元素**、不是画面 · `in-game-ia.md §15.4` / `6bb923c`）**冲突** ⇒ 改为「**归 §2 #2（1.4.11）+ §2 #10（1.4.1）**」，并写清 **3:1 量的是「边界描边 vs 其相邻色」、不是「填充 vs 无界地形」**（保留原写法的正确内核：填充是冗余水洗、不单独套）。§4.6 表行 + §5 #1 同步。另并入 `engineering-lead-2` 直连复算（蓝 **1.14** / 红缘 **1.05**，**同量级、口径略异**）作对照，**写明本节为全仓唯一权威数**（`§15.4` 已指向本处）。触发：`ux-ia` 报冲突 + 请归一 |
| **V1.5** | 2026-09-21 | **落两条 a11y 裁定**（应 team-lead 请裁）：**(a) 夜间仍以 WCAG 3:1 为判据** —— 不豁免、不降尺（1.4.11 无夜间豁免；降尺 = `bd4ebb0` 禁的「调到达标」；`ΔL* 16.8` 是感知旁证、**不能顶替**规范判据）⇒ **夜间 1.56:1 ⇒ 夜读 FAIL 如实成立**；处置 **推荐 (a-1) 相位感知描边**（(a-2) 免 multiply 有「错坐标/盖单位」坑、(a-3) `#15` 仅缓解非合规）。**(b) 不构成 1.4.1 失败、(β)/(γ) 都不需要** —— 编码有**两条非色腿**（**面 vs 边** 形式差 + 墨线**明度**差），「纯色相」只对「水洗」那一半成立。并加**口径警示**：流转的 1.56/2.41/3.46 + 16.8 **未绑构建、出处未入库** ⇒ **不作判据**，待冻结窗口复算。§5 #10 同步。触发：team-lead 请裁（两条列为「唯一未交项」） |
| **V1.6** | 2026-09-21 | **（1）收口 `ux-ia` 报的两处张力**：**(a-1) 归一命名 = 「双色 halo（亮芯 + 暗边）」**（原「相位感知描边」= team-lead 的 (ii)，同一件事，勿再当两套处方）；**「不叠下一层」的对象写死** —— 禁的是「**为凑标定值叠加的装饰性色层**」，**双色 halo 不在禁列**（= 同一「边」语义载体在两个底色方向各一条，1.4.11 本就要求对**两侧**相邻色各 ≥3:1）；**(a-1) 主句改为「双色 halo」**、「相位感知」降为可选增益 —— 依据 = **同一相位内极亮底与极暗底并存 ⇒ 整条换单一颜色必留一类失败**（`ux-ia` 按 WCAG 公式指出 · 我按读数确认）。**(2) 落绑构建复算读数**：`68abf37→3e8084f`、真机视口、A/B 口径 ⇒ **WCAG 中位 2.93（午）/ 2.08（夜）**、≥3:1 **44.6% / 25.3%** ⇒ **两相位均未过**（§4.6 状态行 + `shots/README.md`）；**转述的 1.56/2.41/3.46 仍不入验收**。**(3) `tintab.mjs` 墨族 gap** —— 复现后已由 owner `engineering-lead-2` 修（`6867cff`）；修复后实跑交会：墨 vs 底 uniform-grass **3.40（午）/ 2.58（夜）**、sand **6.15 / 3.82** ⇒ **与本节 A/B 口径同向同判**。触发：`ux-ia` 请收口 + ④ owner 复算 |
| **V1.7** | 2026-09-21 | **撤 `imagecmp CMP_EDGE`（team-lead 裁「一个验收数一处产」· 加于 `f740ec3`、撤于 `bf92be4`）**：该模式量该色**整图所有出现**（物件描边也是 `ink0`）、**不是**只量 ④ 格缘 ⇒ **当不了 §4.6 验收数** ⇒ **撤代码、不留第二把尺**。**④ 空间口径验收数唯一出处 = `tools/reachmeas.mjs`**；`tintab.mjs`（净贡献）为唯一源旁证。§4.6 分工句 + population 表那行（正午 1.29）同步加「**该模式已撤**」注 —— **数留痕、工具不再提供**（加口径、不重写）。触发：team-lead 裁 + `art-director` 执行 |
| **V1.8** | 2026-09-21 | **钉死 ④ 验收契约（team-lead 裁定）+ 口径措辞更正**：**(1) 契约三条写死** —— ① **口径 = A/B「状态 vs 其底」**（`reachmeas.mjs`），**「空间口径」已弃**（被"被染格恰好是亮地形"污染，仅存历史）；② **`population` 按地形逐类**（沙土/草亮/草中/草深/水/暗缝）**不合并**；③ **决策 = ★ 每类都必须 ≥3:1**（不是"全图统一 `population` 的中位"）—— 理由：`1.4.11` 管"能不能识别出那个信息"，玩家站在草深上读不出边界即在彼处不可识别，**不能靠多数地形把那一类洗掉**；且比"统一中位"**更抗操纵**。**(2) 判词随之改写**：`(α)` **FAIL 依因 = 「草·深 2.33/2.43 ❌ + 暗缝 1.16/1.14 ❌」两类不过**（非"全局中位不过"）；`(ii)` 验收目标 = **每类都 ≥3:1**。**(3) 防误引**：逐类小数出自**未入库 ad-hoc 脚本**（`/tmp/alpha_bin.mjs`，桶定义异于 `tintab`）⇒ **待 `#124` 逐类复算锁定**，此前只引判词不引小数。**⛔ 本句「未入库 ⇒ 不得引小数」已被 V1.10 更正**（旧表**可复现**、确系**相位帧**；作废理由是**契约第 4 条**，非"未入库"）。**(4) `3f11752` 读法更正**：不是"换尺"，是"**判据须说清回答什么问题、两方法的数不能共用**"（team-lead 已按其更正窄表述）。触发：team-lead 钉契约 + `art-director` 改措辞 |
| **V1.9** | 2026-09-21 | **契约加第 4、5 条 + ④ 逐类表按身份帧重录（`f35b34f` 不静默替换）**：**(1) 加第 4 条「分类帧 = 身份帧（正午 `none`）」** —— 「地形」是**身份**；用**同相位**分类会让夜间「草·亮」`L*<55` 塌成 n≈0 ⇒ 契约第 3 条被"**该类在本相位不存在**"**真空通过** ⇒ 死区洗白。⇒ `reachmeas` **必带第 4 参**（`0fc66e6`）。**(2) 加第 5 条「每格必带 `population(n)`；`n<200` 不得判过、不计入通过」** —— 理由 = 血案：旧表夜间「草·亮 `5.43` / 100%」实际 **`n=5`**。**(3) 重录状态行**：新表逐格带 `n`（**夜间 6 类里 5 类不过，只沙/土过；正午 草·深/暗缝 不过**）⇒ `(α)` FAIL，**死格不止两个**；旧表挂 **⛔ 取代横幅**保留作史（`0fc66e6` 逐字复现 ⇒ **口径确证 = 相位帧**）。**(4) 记两个未锁边界**（`#124` 前置）：草·深 样本薄（午 n=100/夜 n=90）；跨相位分类帧需 noon/night **同页同相机**（现仅间接旁证）。触发：team-lead 裁「现在就重录」+ `art-director` 执行 |
| **V1.10** | 2026-09-21 | **更正我（`art-director`）引入的两处错说**：本人此前说过 ①「旧表 `草·深 夜 1.42` 是跨相位错桶产物、**不可复现**」、②「**未入库**口径不得引用」——**两处都错**。**机制（`494e95b` 三腿互校，`art-director` 用数据钉死）**：`0fc66e6` 已把旧表**逐字复现** ⇒ **可复现**（正午身份帧 ≡ 旧正午列 / 夜间**相位帧** ≡ 旧夜列 / 夜间身份帧 = 草三行**错开一格**）⇒ 旧表**确系相位帧**所作，**与"未入库"无关**。**(1)** §4.6 状态行「防误引」句改写：作废的是**夜列**、理由是**契约第 4 条（分类帧）**；**旧表正午列 = 身份帧正午列**（正午 ≡ 身份帧）。**(2)** `shots/README.md` 同句同步改写。**(3)** V1.8(3) 的「未入库 ⇒ 只引判词不引小数」加 **⛔ 更正标**（不静默替换）。触发：`494e95b` 定位机制后我自请更正 |
