# 可访问性要求（A11y Baseline）· V1

> **【owner-rev 2026-09-21T10:35】`art-director`（team-lead 指认的 a11y owner 实例）** —— 本文档的唯一写者。
> **机制（team-lead 立，与 `tintab` 同法）**：**谁写谁换这一行**；**发现你上次写的那行不在了 ⇒ 你已被取代 ⇒ 报告，别抢回**。
> **同名的另一实例保留「④ 取数 / 读数」角色（= 生产）**，**写任何文档之前须先在群里报一句**。

| 项 | 值 |
|---|---|
| 状态 | **定稿 V1**（2026-09-19；A-1~A-4 已拍板，见 §7） |
| 作者 | 林绘澄（美术 / 视觉 / **可访问性**） |
| 触发 | **D-39**（`production/roadmap.md`）：`cartoon-style.md §7` 早就声明「完整分级矩阵是**独立交付物** `design/accessibility-requirements.md`，等待主理人排期」，**却从未排期、文件至今不存在** → 规则只能在每份 UI 规格里各自重申（漂移风险）。本档落成分级矩阵并**收编已发现的实项** |
| 前置 | `cartoon-style.md §7`（色板硬约束）· `design/ux/in-game-ia.md` §4.4 / §5 / Q11 · `design/art-bible/asset-spec.md §2.3.1 / §7` |
| 目标平台 | OPPO PMA110 / Android 16 / DPR 3 / **CSS 视口 792×360 横屏**（手机、触屏、常无实体键盘） |

> ## 0. 一句话
> **本项目目标档 = `Standard`（L2），并额外拉入 2 项 AAA 级**（焦点外观 2.4.11、触控目标 2.5.5）—— 手机上这两项成本近乎为零、收益最大。
> **本文档是 a11y 的唯一权威基线**：任何 UI 规格（含 `in-game-ia.md`）**不得各自重申 a11y 规则**，只能**引用本文档的分级**（如「满足 L2 + 本文档 §4.1 焦点口径」）。
> **a11y 是基础要求，不是锦上添花。**

---

## 1. 分级定义（4 级）

| 档 | 名称 | 一句话 | 基线 |
|---|---|---|---|
| **L1** | **Basic** | 「能用」——不出现阻断性障碍 | WCAG 2.1 **A** 中与游戏相关的子集 |
| **L2** | **Standard** ✅ **本项目目标** | 「能用得舒服」——主流残障场景可**独立**完成核心循环（探索 + 经营） | WCAG 2.1 **AA** + 游戏适配（触控 / 色盲冗余 / 读屏标签 / 减少动效） |
| **L3** | **Comprehensive** | 「深度可配置」 | L2 + 选定 **AAA**（焦点外观 / 44px 目标 / 关键文本 7:1）+ 可重映射 + 字幕自定义 |
| **L4** | **Exemplary** | 「面向辅助技术深度集成」 | L3 + 全键位重映射 + 认知辅助 + 完全冗余提示 |

> **为什么要分级而不是「一档搞定」**：分级让**每份 UI 规格的验收可判定**（「这个按钮满足 L2 的 2.5.5 吗？」），也让**成本可预算**（每上一档要付什么）。没有分级的 a11y 只能变成一堆互不引用的散装规则 —— 这正是 D-39 要根治的病。

---

## 2. 特性矩阵（分级 × 特性）

`✅`=该档包含 · `＋`=该档新增 · `—`=不在该档范围。最后一列是**本项目**的取值。

| # | 特性 | 判据（WCAG） | L1 | L2 | L3 | L4 | **本项目** |
|---|---|---|---|---|---|---|---|
| 1 | 文字对比 ≥ 4.5:1（大文本 ≥ 3:1） | 1.4.3 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 2 | 非文本对比 ≥ 3:1（图标 / 边框 / 焦点） | 1.4.11 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 3 | **焦点可见** | 2.4.7 (AA) | ✅ | ✅ | ✅ | ✅ | **L2（现状部分缺，见 §4.1）** |
| 4 | **焦点外观 ≥ 2px 且 ≥ 3:1** | 2.4.11 (AAA) | — | — | ＋ | ✅ | **⚡ 额外拉入** |
| 5 | 触控目标 ≥ 24×24 | 2.5.8 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 6 | **触控目标 ≥ 44×44** | 2.5.5 (AAA) | — | — | ＋ | ✅ | **⚡ 额外拉入（本项目取 48 主 / 44 常规）** |
| 7 | 键盘可达 + 焦点顺序合理 | 2.1.1 / 2.4.3 (A/AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 8 | 名称-角色-值（图标 / 按钮有可读名） | 4.1.2 (AA) | ✅ | ✅ | ✅ | ✅ | **L2（现状全缺，见 §4.3）** |
| 9 | 标签即名称（可见文字 ∈ 可访问名） | 2.5.3 (A) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 10 | 颜色非唯一手段（色盲 / 灰度可辨） | 1.4.1 (A) | ✅ | ✅ | ✅ | ✅ | **L2（见 §4.6）** |
| 11 | 文本缩放 ≥ 200% 不丢内容 | 1.4.4 (AA) | — | ✅ | ✅ | ✅ | **L2** |
| 12 | 减少动效 / 无高频闪烁 | 2.3.1 / 2.3.3 | ✅ | ✅ | ✅ | ✅ | **L2（现状缺，见 §4.5）** |
| 13 | 音频：字幕 / 文字大小 | 1.1.1 / 1.2.x | — | ✅ | ✅ | ✅ | **L2 档位；本项目 N/A**（音频线确认，见 §6 / §7 A-3） |
| 14 | 可重映射输入（键位 / 手势） | 2.5.1 / 2.1.4 | — | — | ＋ | ✅ | **L3+（暂缓）** |
| 15 | 昼夜光照可关（光敏 / 偏好） | — | — | ✅ | ✅ | ✅ | **L2（已有 `lightingOn()`）** |

> **注**：#4 与 #6 是**主动拉入的 AAA 项**（见 §3）。#14 暂缓（触屏优先、无复杂键位）。

---

## 3. 本项目目标档与「额外拉入」的理由

| 项 | 决策 | 理由 |
|---|---|---|
| **基线** | **L2 Standard** | 覆盖 ~8% 男性红绿色盲、老年低视力、触屏误触三大主要场景，成本可控 |
| **额外 #4 焦点外观 2px** | **拉入** | 现状**已有单点缺陷**（1px，见 §4.1）；统一 `:focus-visible` 是一次改、长期受益，成本 ≈ 0 |
| **额外 #6 触控 44/48** | **拉入** | 真机实测 **p90 = 23px** 落点散布（`in-game-ia.md §5.1`）→ 40px 宽按钮横向余量仅 1px；44/48 是**可用性刚需**，非锦上添花 |
| **暂缓 #14 可重映射** | 不进 L2 | 本作触屏优先、无复杂键位；待有键盘/手柄需求再评估 |

---

## 4. 已发现的实项（D-39 收编 · 逐项带代码证据）

> 证据口径：`git grep` / `rg` 于 `homm-web/src/`。定位一律用**符号 / 选择器 / 关键字**（**不写行号** —— 行号是「会腐烂的坐标」，见 `production/roadmap.md` **D-52**）。

### 4.1 焦点可见性 —— 「对比达标、厚度不足」

| 项 | 内容 |
|---|---|
| **现状** | 全项目**只有一条** `:focus` 规则：`style.css` 的 `.ss-field input[type='text']:focus { outline: none; border-color: var(--gold); }`。焦点指示 = 把默认 `1px` 边框从 `--frame` 换成 `--gold`，**厚度不变（1px）**，并**移除了** outline |
| **量化** | `--gold #f0d488` vs `--frame #8a6a34` → 对比 **≈ 3.5:1**（≥ 3:1 ✅）；厚度 **1px < 2px** ❌ |
| **判据** | **2.4.7（AA）本身满足**；不满足 **2.4.11（AAA 焦点外观）**。即「**对比达标、厚度不足**」 |
| **补充事实** | 60+ 按钮**没有自定义 focus 样式** → 现靠浏览器默认 focus ring；`outline:none` **只加在输入框**、**未全局清除** → 属「**未统一**」而非「被删光」 |
| **处置** | 加全局 `:focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }`（或与既有输入框规则统一，避免两套）。**归属工程**（`style.css` 单写者 = engineering-lead），时机 = UI 重构批 |

### 4.2 既有 emoji 的读屏播报不一致

| 项 | 内容 |
|---|---|
| **现状** | `HUD.ts` 的 `muteBtn` `🔇` / `🔊`（音效）、`lightBtn` `🌗` / `☀️`（光照）。读屏（TalkBack）**播报各家不一**：有的念「静音 / 扬声器」，有的直接跳过，有的念错；`☀️` 含变体选择符 **U+FE0F**，部分平台渲染成缺字框 □ |
| **判据** | 违反 **4.1.2（名称-角色-值）**；且 emoji 全彩 / 抗锯齿 / 尺寸随 OS 变 → 违反 `cartoon-style.md §3.2 色板 / §1.1 描边 / §1.3 整数缩放`（= **G-17**） |
| **处置** | ① 换**图集精灵** `ic_sound_on/off`、`ic_light_on/off`（**已登记** `asset-spec.md §2.3.1`）；② 每个按钮补 `aria-label`（如 `aria-label="音效"`、`role="switch" aria-checked`）。时机 = UI 重构批（与 setup 搬迁同片 DOM）。**归属 = 美术出帧（已登记）+ 工程接线** |

### 4.3 图标不得为唯一语义载体（+ `aria-label`）

| 项 | 内容 |
|---|---|
| **现状** | 顶栏资源项是「**图标 + 数字**」（`HUD.ts` 资源项 `icon.src = atlas.url(\`ic_${k}\`)`），**无 `aria-label`** → 读屏只念出一个孤零零的数字。**`src/` 全目录 `aria-label` 数量 = 0**（`rg` 证据） |
| **判据** | **4.1.2 / 2.5.3** |
| **处置** | ① 「图标 + 数字」项：`aria-label="<资源名> <值>"`（如 `aria-label="金币 12"`）；② 图标按钮**必须有文字标签或 `aria-label`**；③ 拇指带已是文字标签（`in-game-ia.md §4.4 / Q10`）→ **天然达标**，仍建议补 `aria-label` 覆盖缩写（如「日志2」→ `aria-label="事件日志，2 条未读"`）。时机 = UI 重构批 |

### 4.4 触控目标（vs 实测 p90 = 23px 落点散布）

| 项 | 内容 |
|---|---|
| **现状** | 真机实测：误触 0/8，但**落点偏差 p50 = 13px / p90 = 23px**。最小 `宝物▾` = **40×32**（半宽 20px，手指落 19px 处 → 横向余量 **1px**） |
| **判据** | **2.5.8（AA，24px）** / **2.5.5（AAA，44px）**。本项目取 **≥ 44（常规）/ ≥ 48（主操作）/ ≥ 40（密集区，且相邻按钮间距足够）**（与 `in-game-ia.md §5` 一致） |
| **处置** | 尺寸批已把 `.btn.tiny` **32 → 40**（commit `028ec4a`）；其余按 IA §5。⚠️ **判定口径**：`.btn.tiny.tap::after` 用负 offset 扩**点击热区**（仅垂直向）——扩的是**热区**不是**可见尺寸**；对 2.5.5 的判定**以热区为准时 OK、以可见为准时不足**，验收时须写明用哪一种 |

### 4.5 减少动效 / 无高频闪烁

| 项 | 内容 |
|---|---|
| **现状** | **`prefers-reduced-motion` 在 `src/` 中数量 = 0**（`rg` 证据）；现有动画均为 **≤ 4 帧循环**（低闪），**无高频闪烁** |
| **判据** | **2.3.1（三次闪烁）/ 2.3.3（交互动效）**；本项目动效均为低幅（±1px 整数位移 + `squash & stretch`） |
| **处置** | 补 `@media (prefers-reduced-motion: reduce)`：关掉 idle 浮动、缩短面板过渡（**不改整数位移本身**，那是可读性手段）。**归属工程** |

### 4.6 色盲 / 灰度（色板硬约束，引自 `cartoon-style.md §7`）

| 约束 | 值 | 归属 |
|---|---|---|
| 灰度可辨 | 4 族明度台阶相邻差 **≥ 12 L\***（= §2.4 明度签名） | 美术（色板） |
| 色盲可辨 | **禁止**靠 p2 红 / p3 绿的色相差传递**唯一**信息；每族同时具备**形状母题** | 美术（§5 母题是形状） |
| 战斗队伍辨识 | 队伍色环（`cbase_*` 蓝 / 红）对红绿色盲失效 → 建议**形状区分**（我方圆环 / 敌方方环或带缺口） | 美术 + 工程 |
| 光照可关 | `lightLayer.ts` 已有 `lightingOn()` ✅ | 工程（保留 + 设置面板暴露） |
| **地图可达染色（④）** | 见下方专段。**归 §2 #2（1.4.11，信息承载描边）+ §2 #10（1.4.1 冗余）** | 美术（色）+ 工程（常量） |

> **④ 地图可达染色的 a11y 判定（2026-09-21 · 触发：`engineering-lead-2` 实测「纯色相线索、灰度 ~1.1–1.4:1」并问 scope）**
> - **在范围内**；SC 归属 **= §2 #2（WCAG 1.4.11 非文本 3:1）+ §2 #10（1.4.1 非色彩唯一）**。
>   ⚠️ **本行 2026-09-21 归一**（原写「归 #10、**不归 #2**」，与 team-lead 裁决冲突 —— `ux-ia` 报、`68abf37`）：**1.4.11 的 3:1 适用** —— 可达 / 不可达是**信息承载的 UI 元素**（不读它下不了令），**不是画面层**（team-lead 裁定 · `in-game-ia.md §15.4` / `6bb923c`）。
>   **但"量的对象"要分清**（这是原写法的正确内核，保留）：3:1 量的是**承载信息的"边界描边" vs 其相邻色** —— 描边的相邻色**是确定的**（线两侧那两格地形），**不是**"半透明整格填充 vs 无界地形"。**蓝填充**只是**冗余"区域水洗"**（色觉正常者读得到；色觉障碍者另有描边兜底）⇒ **不单独套 3:1**。⇒ 两条不矛盾：**3:1 打"描边"、不打"填充"**。
> - **判据**：可达 / 不可达**两态在灰度下彼此可辨** —— 量化锚在 WCAG 1.4.11 的 **3:1**（量「状态 vs 其底」），**不套**「vs 无界地形 3:1」。
> - ★ **验收契约（team-lead 钉死 2026-09-21；「怎么测」不再是可选项）**：
>   1. **口径 = A/B「状态 vs 其底」**（工具 `tools/reachmeas.mjs`）—— **不是「空间口径」**（那套已被"被染格恰好是亮地形"污染 ⇒ **已弃**，仅存历史）。
>   2. **`population` = 按地形逐类**（沙土 / 草亮 / 草中 / 草深 / 水 / 暗缝），**不合并**。
>   3. **决策规则 = ★ 每一类都必须达 WCAG 非文本 3:1（中位）** —— **不是「全图统一 `population` 的中位」**。
>      **理由**：`1.4.11` 管的是「**能不能识别出那个信息**」；**玩家站在草深上读不出边界 = 在那里它就是不可识别的** ⇒ **不能靠「多数地形过」把那一类洗掉**；且它**比「统一中位」更抗操纵**（中位可被大片亮地形拉过去）。
>   4. ★ **分类帧 = 身份帧（光照恒等帧 · 正午 `none`）** —— 「**地形**」是**身份**，不是"此刻看起来多亮"。
>      ⇒ `reachmeas.mjs` **必须带第 4 参**（正午 `none` 的 before 底图）；不给则退回同相位并**告警**（**与 `tintab` 不可拼表**）。
>      **理由（实测）**：用**同相位**分类时，夜间 multiply 把亮草压到 `L*<55` ⇒「草·亮」在夜间**几乎不存在** ⇒ 第 3 条会被「**该类在本相位不存在**」**真空通过** ⇒ 那正是"死区洗白"。
>   5. ★ **每个数必须自带 `population(n)` + 中位 + `≥3:1` 占比**；**`n<200` 不得判「过」**，**且汇总里 `n<200` 的类不许计入「通过」**。
>      **理由（血案）**：本表夜间「草·亮 `5.43` / `≥3:1` 100%」曾被读成**过关**，实际 **`n=5`** —— **没有 `n` 一列，就把真空桶记成了通过。**
>   ⇒ **`population(n)` 与统计量必须逐条写在数旁边**（不许脱离 `population` 引用）。
>   6. ★ **(ii) 起：判据下沉到「边带族」**（team-lead 2026-09-21 定 · `12d99d1` / 全文 `production/roadmap.md`）
>      —— **④ 的判据 = 每一类 × 每一相位，边带族中至少一条带 ≥3:1**，**逐族出数**；
>      **禁用「混合 population 的中位」** —— 它**按构造**杀死任何正确的 halo（`沙/土` 正午**混合读 `1.10`**，而真实可读性 = **墨 `7.43`**）。
>      **四条附加 + 一条硬约束（②′ 为 2026-09-21 team-lead 增补）**：① **population = 类 × 带**，由 **draw-time 族标记**产生（`tintab` 的 `fill/edge/glow/ink`），**不用颜色窗**；
>      ② 判据取**四带中位里的最大值**，**但四带中位必须全部列出**（赢家 + 输家都报 —— 否则"最大值过线"会盖住"另一极完全不可读"）；
>      ②′ **★ 硬约束：该"最大值"必须落在「亮芯」或「暗墨」上 —— 若落在红 / 蓝fill，该格记「未判」并报告。**（team-lead 2026-09-21 加）
>         **理由**：`(ii)` 的设计承诺**只针对两个极点**（「任何底色下两极其一必 ≥3:1」）；**最大值若落在红/蓝，说明设计意图没兑现、我们在靠一个"碰巧"过线。**
>         这是**"可执行口径"与"可测口径"的分工**：team-lead 的口头表述「两极其一」**不可直接测** ⇒ 正文用可测量形（②），**但把"哪一极"以约束形式加回来（②′）**。
>         ✅ **在现有 21 格上核过：最大值全部落在亮芯或暗墨** ⇒ **此约束不改任何现有判词，只封住未来的洞。**
>      ③ **守恒断言 `Σ(四带 n) == 该类变化像素总数`**，不等 ⇒ **FAIL 并报缺口**；
>      ④ **逐带 `n<200` ⇒ 该带「未判」，既不判过也不判败**（**对称** —— 给"没有样本的带"写 ❌，与"空集通过"是同一个病的镜像）。
>      **另禁用 `isInk` 单选**：它**结构上看不到亮芯** ⇒ 拿它当验收人口 = 又一种"按构造杀 halo"。
>      **来源分工（team-lead 裁 · 取代本文档此前「`reachmeas` = 唯一出处」的写法）**：**逐类 × 逐带的验收数（中位 WCAG）→ `tintab.mjs`**；**全图 A/B 旁证腿 + 「`(α)` 基线可比列（墨单带）」→ `reachmeas.mjs`**。两者**都保留**（`reachmeas` 那列**就地标注、留痕不删**，且写明它**结构上量不到 (ii)**）。
>      ⚠️ **守恒断言的实测读法（`engineering-lead-2` `eefa9bb`/`e4686cf` 实跑；**读法待 team-lead 认**）**：
>      字面 `Σ == 右端` **跑不过** —— 四带在 **1px 交界**上重叠（`edge+glow 2351 · glow+ink 2337 · fill+edge 1182 · edge+ink 481 …`）⇒ `Σ` 会**多算**。
>      ⇒ **正确读法 = 「覆盖形」**：**`缺源(全族变·无单族) == 0` ⇒ 过**（这才是 `15` vs `11 094` 那个病的判据）；**`多源(≥2 族)` 作为「重叠量」如实报出、不当 FAIL**。⇒ 现状 = **覆盖过、无漏算**。
>      ⚠️ **独立腿的用词纪律（team-lead 定 · 硬约束）**：**同工具换视口 / 换抓帧通道 = 「通道独立」，不得写成「独立复核」**；只有跨工具（`tintab` ↔ `reachmeas`，不同族定义/population/视口）才配叫独立，且**不逐格对齐**。
>      **⛔ 该腿当前阻塞**：`tintab` 只暴露 `--vh`，**无 `--vw` / `--dpr`** ⇒ **跑不了真机视口 792×320@DPR3**；**已请 owner 加**。**不拿别的通道冒充它。**
> - **现状 = FAIL**（`engineering-lead-2` 真 Chrome CDP，seed 20260917；**两个口径都记，量化锚用 A/B 口径**）：
>   - **A/B 口径（★ 唯一验收口径 · 契约第 1 条）** —— 染色开/关、**同页同相机、同像素同地形**，只差染色层（**去掉"被染区恰好是亮地形"的混淆**）：**WCAG 对比度中位 1.15(午) / 1.12(夜)**，max 2.93 / 2.43，**≥3:1 的像素 = 0.0%**；染色占屏 **19.4%(午) / 19.3%(夜)**。
>   - **⛔「空间口径」（已弃 · 仅存历史，勿再引用作判据）** —— 真实被染格 vs 紧邻未染格（钩子记录被染集、不靠猜）：**被染蓝 vs 未染底 1.74(午) / 1.53(夜)**；**红格缘 vs 未染底 1.18(午) / 1.12(夜)**；逐对 **min 1.00**（有格完全分不开）。
>   - **⇒ FAIL 成立**：两态在灰度下都远低于 3:1。非色线索现为「可达 = 蓝填充 / 不可达 = **红格缘**」= *填 vs 缘* 的形状差 —— **部分**满足 1.4.1；但**"不可走"侧的唯一载体（红缘）自身只有 1.12–1.18** ⇒ 工程侧删掉 `0.07` 红填充**没解决夜读**，反而**坐实"锚必须打在边上"**（纯色相：b−r≈86–100，亮度分量仅 ~7）。
>   - **复算对照（`engineering-lead-2` 直连口径 · 旁证 · **非契约口径**）**：蓝 **1.14** · 红缘 **1.05** ⇒ 与 A/B 口径（1.15 / 1.12）**同量级、差在口径**。**以本节为全仓唯一权威出处**（`in-game-ia.md §15.4` 已指向本处、不另存第二份）；**来源分工见契约第 6 条**：**逐类 × 逐带的验收数 → `tintab`；全图 A/B 旁证腿 + `(α)` 墨单带基线列 → `reachmeas`**。
> - **「两把尺子相反」的断法**：`cartoon-style.md §3.1.1` 的 ΔL\* 尺子（0.49 / 2.22）答的是「**纹理看不看得见**」；WCAG 3:1 答的是「**两个状态分不分得开**」。对**二值状态线索**，**以 WCAG 为准** —— ΔL\* 只证「变化是真的」，不证「状态可辨」。⇒ `engineering-lead-2` 的发现**成立，非误报**。
> - **处方（team-lead 2026-09-21 裁定取 **(α)**）**：给"可达区**边界**"一个**亮度锚**，而不是在色相上再叠色：
>   **(α) 已落** —— 边界改成「**墨色 `ink0` `#2a1a12` 外描边**（复用 `cartoon-style.md §1.1`，**不透明** ⇒ 灰度高对比）+ **内侧红**（色相锚）」。落点 = `MapRenderer.ts` 的 `NOGO_EDGE_INK` / `NOGO_EDGE` / `EDGE_W`（**未动别处**）。**位置选"边"而非"填充"，正是被"红缘 1.12–1.18 是最弱一环"倒推出的**（填充那侧 1.53–1.74 已略强于边，且填充面积大、改了更伤观感）。
>   未采纳：**(β)** 可达侧也描同类格缘（格缘本身无亮度分量 ⇒ 解决不了）；**(γ)** 可达格叠 cost 数 / 点纹（最稳但最贵、要改管线）。
> - **状态**：✅ **(α) 已落（2026-09-21）**；**数值复测已入 `shots/`**（真机视口 **792×320@DPR3** · **A/B 口径** · **绑构建 `68abf37`→`3e8084f`**）：
>   **★ 判词（契约第 3 条「每一类 × 每一相位都必须 ≥3:1」）＝ `(α)` FAIL。**
>   **依据（**身份帧** · 契约口径 · 同构建 `i_reach_*` · 三跑互为校验 · 工具 `0fc66e6`）：**
>
>   | 类（`population` 全称） | 正午 n | 正午中位 | 正午 `≥3:1` | 深夜 n | 深夜中位 | 深夜 `≥3:1` | 判 |
>   |---|---|---|---|---|---|---|---|
>   | 沙/土（暖亮） | 7580 | **8.98** | 100.0% | 6834 | **5.12** | 99.4% | ✅ ✅ |
>   | 草·亮 | 873 | **4.72** | 100.0% | 727 | **2.92** | 42.4% | ✅ ❌ |
>   | 草·中 | 2798 | **3.72** | 84.1% | 2322 | **2.43** | 0.0% | ✅ ❌ |
>   | 草·深 | 100 ⚠️ | **2.33** | 0.0% | 90 ⚠️ | **1.69** | 0.0% | ❌ ❌ |
>   | 水（蓝主导） | 5378 | **3.04** | 59.5% | 5379 | **2.14** | 4.5% | ✅ ❌ |
>   | 暗(物件/接缝) | 15 ⚠️ | **1.16** | 0.0% | 517 | **1.13** | 0.0% | ❌ ❌ |
>   | 其他（兜底桶） | 230 | 3.34 | 89.1% | 312 | 2.26 | 7.4% | — |
>
>   ⚠️ = `n<200` ⇒ **不得判「过」、也不计入「通过」**（契约第 5 条）。
>   ⇒ **结论：`(α)` FAIL —— 夜间 6 类里 5 类不过（只有「沙/土」过）**；正午「草·深 / 暗缝」两类也不到 3。
>   ⇒ **`(ii)` 的验收目标 = 让每一类 × 每一相位都 ≥3:1**（**不是**把中位抬上去）。**⇒ 死格不止两个。**
>
>   **⛔ 取代横幅（`f35b34f`：不静默替换）—— 本节曾有的那张夜间逐类表已失效，勿再引用：**
>   旧记：`沙/土 5.12 · 草·亮 5.43 · 草·中 2.92 · 水 2.14 · 草·深 2.43 · 暗(物件/接缝) 1.14`（午）：`沙/土 8.98 · 草·亮 4.72 · 草·中 3.72 · 水 3.04 · 草·深 2.33 · 暗 1.16`。
>   **失效原因 = 它用「相位帧」分类**（`0fc66e6` 已把该表**逐字复现** ⇒ 口径**确证**，非推测），而契约**第 4 条**要求**身份帧**。
>   **最要命的一格**：旧表夜间「**草·亮 `5.43` / `≥3:1` `100%`**」看着**过关**，实际 **`n=5`** —— 夜间 multiply 把亮草压到 `L*<55` ⇒ 该类在夜里**几乎不存在** ⇒ **5 个像素的"真空通过"**。
>   ⇒ **"为什么当初会出这个错" = 缺 `n` 一列 + 用了相位帧。** 这两条现已是**契约第 4、5 条**（本节上方）。
>
>   **同一事实的另一种说法（保留，勿当结论）**：「状态 vs 其底」**全图统一 `population` 的中位 = 2.93（午）/ 2.08（夜）**、≥3:1 像素 44.6% / 25.3%
>   —— **「中位不过」会让人误以为是全局性问题**，真因**逐类不过** ⇒ **以逐类判为准**。
>   ⚠️ **防误引（更正我此前两处错说 · V1.10）**：早前那批逐类小数**可复现**（`0fc66e6` 已逐字复现 ⇒ 它就是**相位帧**读数）—— **不是「跨相位错桶产物」、也不是「未入库即无效」**；其**正午列与身份帧逐格相同**（正午 ≡ 身份帧），**只有夜列错桶**（草三行整体下移一格）。⇒ **作废的是「夜列」、理由是契约第 4 条（分类帧），不是「不可复现 / 未入库」** ⇒ **别引旧表夜行当验收数**。
>   ⚠️ **原「两个未锁边界」的处置（2026-09-21 · 我复算后；`engineering-lead-2` 同证）**：
>   ① **`草·深` 样本薄 —— 可撤销**：`n=100/90` 是**只数 `±3` 墨核**时的数。按契约「状态 vs 其底」的**全变化人口**，`草·深` **`n=475`(午)/`416`(夜) ≥ 200`**，且**仍 FAIL（`1.58` / `1.65`）** ⇒ **样本薄不再挡结论**；**两种人口同判 FAIL**。
>   ② **跨相位分类帧「同页同相机」—— 已闭**：`engineering-lead-2` 的**维度守卫**（身份帧/相位帧尺寸不等即 `throw`）+ **同桶 `n` 跨相位恒定**（`grass-bright` 42256/42256/42256）证明同坐标系；我侧独立旁证：**水 `n` 跨相位近乎恒定（`5 378`/`5 379`）**（水是帧无关类）。⇒ **「用正午 `before` 当夜间分类帧」成立，不必再跑第二遍。**
>
>   ⚠️ **新增未锁（我实测 · 待 team-lead 裁）：`i_reach` (α) 对**疑非单变量**。**
>   **非墨色、且不与墨核 8 邻接**的变化占非墨变化的 **81%**（午 `17 346 / 21 460`），且其 `after` **比「墨」与「底」都更暗**（如 `rgb(24,18,17)`）⇒ **抗锯齿解释不通**（混合不可能比两端都暗）。
>   **最可能根因** = `before`/`after` 出自**两个不同构建**（`68abf37` ↔ `3e8084f`）⇒ 其间任何其它漂移都会落在**高频区（物件 / 投影 / 岩）**而被记进变化集。
>   **建议**：加**默认为空操作**的 `?devtint=0`（跳过可达染色层；惯例同 `?devshade=0` / `?devregion=0`）⇒ 出**同构建 / 同会话 / 同帧 / 冻帧**的真 A/B ⇒ 这条与 `#128` 的 (ii) 素材**一并解决**。
>   ⚠️ 该疑点**不改现行判词**（判据在**两种人口下同判**：暗 `1.16` vs 全变化 `1.20`；夜 `1.13` vs `1.11`），但**削弱 `i_reach` 对的「干净单变量」资格** ⇒ 引用其逐类数**须连此保留一起说**。
>   ⚠️ **推及全文的读法**：`reachmeas` 逐类那列的人口 = **`±3` 墨核**，**对暗底偏置**（暗底上墨达不到纯墨 ⇒ 暗类墨核 `15/517`，而实际变化 `11 094`/`10 785`）⇒ **「某格 `n=15`」≠「该类只有 15 个像素」**。
>
>   ⚠️ **水的量级更正（回应 `engineering-lead-2` 的核数请求）**：水**不是** `1.80`/`1.55`（那是**动画污染**；`5ed22d3` 已冻帧）。我的冻帧对（身份帧）：**水面积 `283 098`、水上变化 `7 537`/`7 535` = 该类的 `2.7%`**、WCAG 中位 **`2.97`(午) / `2.10`(夜)** ⇒ **冻结后仍 `< 3` ⇒ 水仍判 FAIL**（**是真死角，但量级不是 40k**）⇒ 见 `production/roadmap.md`「水/暗缝待重测」，**重测目标值 ≈ `2.97`/`2.10`**。
>   **复算（契约口径 · 必带第 4 参 = 正午 `none` 底图）**：`node tools/reachmeas.mjs ../design/art-bible/shots/i_reach_after_noon.png ../design/art-bible/shots/i_reach_before_noon.png 正午 ../design/art-bible/shots/i_reach_before_noon.png`（全表 = `shots/README.md`「★ `shots/` 判定」）
>   **⛔ 作废**：早前「Δ>40 核心墨像素」子选得 4.37 / 2.86 —— **拿结果筛人口**，作废。
>
> - **★ `(ii)` 双色 halo 复核（`#124` / `#128` · 2026-09-21 · 判定方）—— 仍**未达**「每类 × 每相位 ≥3:1」**：
>   **口径/来源**：`tintab`（**冻结版**，输出自印「动画: 已冻结 performance.now」）+ **身份帧** + 三相位；**统计量 = WCAG 中位**。**来源分工（team-lead 已确认）**：**逐类 = `tintab`；全图 A/B = `reachmeas`（旁证腿）**。
>   **绑构建**：`src f971db7a` / `dist 13d3b420` / `main ecbfa1e2` / `HEAD ec3ae85c`（`tintab` 自证三件套①②③ 全 OK：启动即核 `src≠dist` 一致、测前=测后指纹未变、**测前=测后 HEAD**）⇒ **非历史读数**。
>   **判据**：亮芯/暗墨**两锚落在同一格地形** ⇒ 设计自述「暗底吃亮芯、亮底吃暗边」⇒ **两极其一 ≥3:1 即算过**（§4.6 (a-1)/(ii)）。**⚠️ 不是**「四条线全 ≥3:1」—— 半透明蓝填 `α.16` / 红 `α.42` **天生低**（V1.4 已定为「冗余水洗」），那样读任何方案都过不了。**✅ team-lead 已确认（2026-09-21）：整张表不用重读。**
>   **统计量**：**钉 WCAG 中位**（✅ team-lead 裁；均值列只作参考 —— 本轮两次栽在"均值被分布带偏"）。**中位旁须固定保留「`≥3:1` 占比」**（中位 + 占比同看，才分得清"整类压线"与"半类过"）。⚠️ **占比列 `tintab` 现无** ⇒ 已请 owner 补（additive）；**补到之前，本表只报中位**。
>   **★ 判据正文的最终措辞（team-lead 合成 `ux-ia` 与我的两说 —— 一次定死，勿再分叉）**：
>   - **可测量形（正文）**：**每一类 × 每一相位，四带（红 / 亮芯 / 暗墨 / 蓝fill）中位里的最大值 ≥3:1**。
>   - **★ 硬约束（team-lead 加）**：**那个"最大值"必须落在「亮芯」或「暗墨」上** —— **若落在红 / 蓝fill，该格记「未判」并报告。**
>     理由：**`(ii)` 的设计承诺只针对两个极点**（「任何底色下两极其一必 ≥3:1」）；**最大值若落在红/蓝，说明设计意图没兑现、我们在靠一个"碰巧"过线**。
>     这是**"可执行口径"与"可测口径"的分工**：`team-lead` 的口头表述「两极其一」**不可直接测** ⇒ 正文用我的形；**但把"哪一极"这一条以约束形式加回去**。
>     ✅ **在现有 21 格上核过：最大值全部落在亮芯或暗墨** ⇒ **此约束不改任何现有判词，只封住未来的洞。**
>   ★ **方向信息由「闭包轮廓」承载 —— 不由"哪一极可见"承载**（team-lead 定，必须写清，否则后人会问同一个问题）：
>   **可达区 = 落在墨色闭合轮廓内**（`§1.4.1` 明列的 "shape or boundary"）。**因为暗地上只有亮芯可见、亮地上只有暗墨可见 ⇒ "可见的那一极"随地面变，不能拿它编码方向。**
>   ⇒ **轮廓闭合性是本判据的前提**，见下「⚠️ 轮廓闭合性核验（有缺口）」。
>
>   | 类 | 正午 亮芯/墨 | 晨昏 亮芯/墨 | 深夜 亮芯/墨 | 判 |
>   |---|---|---|---|---|
>   | 草·亮 | 2.53 / **5.18** | 2.52 / **4.07** | 2.30 / **3.18** | ✅✅✅ |
>   | 草·中 | 3.43 / 3.52 | 3.34 / 2.86 | **2.94** / 2.38 | ✅✅**❌** |
>   | 草·深 | **6.79** / 1.81 | **6.06** / 1.60 | **4.74** / 1.44 | ✅✅✅ |
>   | 沙/土 | 1.72 / **7.43** | 1.64 / **6.07** | 1.62 / **4.32** | ✅✅✅ |
>   | 水 | **4.71** / 3.05 | **4.79** / 2.38 | **3.64** / 2.18 | ✅✅✅ |
>   | 暗(物件/接缝) | **4.00** / 1.32 | 3.46 / 1.24 | **2.77** / 1.18 | ✅✅**❌** |
>   | 岩(rock) | 3.18 / 1.56 | **2.83** / 1.44 | **2.36** / 1.29 | ✅**❌❌** |
>
>   失败格**全部 `n≥200`**（晨昏岩 `n=239`、夜草中 `n=1239`、夜暗 `n=1579`、夜岩 `n=239`）⇒ **可判、非腾空桶**；`墨(岩) n=153 <200` ⇒ 按契约第 5 条记**样本不足、不得据此判过**；`蓝fill(水) n=0`（水不可达 ⇒ 无蓝填，非缺陷，按第 5 条亦**未判**）。
>   ⇒ **`(ii)` 判 FAIL**（正午**全过**；晨昏 + 夜各有过不了的类）。
>   ⇒ **但 `(ii)` 把 `(α)` 的「草·深」死格救活了**（亮芯三相位 `6.79/6.06/4.74` 全过）⇒ `-2` 的预判成立。**死格换位**：`(α)`「草·深 + 暗缝」→ `(ii)`「**岩（晨昏+夜）/ 草·中（夜）/ 暗缝（夜）**」。**「死格不止两个」仍成立，只是集合变了。**
>
>   **⚠️ 机制线索（给 owner，未定论）**：亮芯三相位**单调走低**（暗 `4.00→3.46→2.77`、岩 `3.18→2.83→2.36`）—— **同一对色、只因相位变暗就掉** ⇒ 选色解释不了。`MapRenderer.ts:462-468` 自述「世界坐标在 `ctx.restore()` 结束，`applyLighting` 是屏幕空间且在实体循环之后」，而 halo 画在世界坐标里 ⇒ **疑 halo 落在光照乘之前**（夜间整条线被乘暗 ⇒ 亮极被压）。**若属实 = 管线层，不是色板层。**
>
>   **`#128` 观感（真机图已抓 · 人眼过）**：`SHOT_FREEZE=1` + `devzoom=2`（真机视口，canvas 1584×480）。
>   ① 总宽 `3×EDGE_W = 6px` ✓（= 上限）｜② 三色均在既有 `REACH/NOGO` 常量内 ✓｜③ 同格双极（亮芯+暗墨同格）✓｜④ 见上表。
>   ⑤ **语义可读**：日间尚可（亮芯+暗墨成明确断线）；**夜间那几格按 ④ 即"读不出"** ⇒ **⑤ 随 ④ 一并不过**。
>   ⑥ **无内部网格线**：放大图里染色格内部**未见**网格线（只在可达/不可达前沿描边）✓。
>   ⚠️ **本观感读数只覆盖 `devzoom=2` 一档（= 可达区放大的最有利视角），且未做 `(ii)` 的"染色开/关"同构建 A/B** ⇒ 待 `?devtint=0` 那类入口到位后补（见上「新增未锁」）。
>
>   **⛔ 不移除旧表（不静默替换）**：本块是**新增**，**不改**上面 `(α)` 那张表的判词 —— `(α)` / `(ii)` 是**两次不同实现、各自绑各自构建**的读数，**两张表并存**。
>   **★ 表头必读：死格集合已换位。（ii）救活了 `(α)` 的「草·深」（亮芯 `6.79/6.06/4.74` 三相位全过），新死格 = 岩（晨昏+夜）/ 草·中（夜）/ 暗缝（夜）。** ⇒ 「死格不止两个」**成立、只是集合变了** —— **不是没进展，是敌人换了个地方**。
>
>   **⚠️ 轮廓闭合性核验（按 team-lead ② 要求）—— 如实报：有缺口（代码确证 + 像素实测，两腿都做完了）。**
>   ⚠️ **行号引用纪律（本文件此后按符号锚定）**：`MapRenderer.ts` 被并行改写 ——
>   **实测同一段代码在 10:09 与 10:14 两次读落在不同行号**（工作树 md5 `9b79241a` 那一版带 `?devhalocomp` 原型、
>   `git status` 显示 `M`；该原型随后由主理人裁定**认领并回退**（`e0885e1`）⇒ 写此句时该文件已回落到干净 HEAD）。
>   ⇒ 本节一律按**符号**引用（`const inRegion =`、绘制循环首句 `isRevealed … continue`、`computePaths()`），**不引行号**。
>   （与 `production/roadmap.md` 立的「守卫必须写在**性质**上，不能写在**会变的值**上」同族：**引用也一样**；
>   唯一保留的行号 = `pathfinding.ts:86`，因为已复核**该文件行号未漂**。）
>   - **结构上确证**：`core/map/pathfinding.ts:86 computePaths()` **没有 `isRevealed` 闸**（只按 `stepCost` + `maxCost=hero.movePoints` 展开；已复核 `pathfinding.ts` **全文零 `isRevealed`**，且该文件此行号**未漂**）⇒ **`vm.reachable` 的有限 cost 会落在"未探索"格上**。
>     而 `MapRenderer.ts` 的 ④ 绘制循环首句是 `if (!isRevealed(state, player, x, y)) continue;` ⇒ **未探索格既不填蓝、也不描 halo**；且 `inRegion()` **也要求 `isRevealed`**（其相邻格判据）⇒ **可达区那一侧面对未探索格时也不描线**。
>     （⚠️ **此处原写 `MapRenderer.ts:480` / `inRegion()（:473-477）` 的行号已删** —— 该文件**正被并发改写**，行号会漂；**引用一律锚在符号上**（这是本会话"守卫要写在性质上、不写在会变的值上"的同族）。）
>   - ⇒ **结论：在「可达区 × 未探索区」交界处，轮廓是开的**（既无蓝填、也无 halo）⇒ **"可达区 = 墨色闭合轮廓内"这条前提不处处成立**。
>     （可达区被"已揭开的不可达地形"包住时闭合；**一旦可达区伸进迷雾，那一侧就没有边界标记**。）
>   - **✅ 像素实测（原来只写"依据是代码、不是图"，现已补上像素证明）**：同构建 `dist-139`（`?devtint=0` 那版）、
>     同会话同帧、`SHOT_FREEZE=1`、真机视口（792×320@DPR3）、`devzoom=1.6`、**不带 `devreveal`**（= 正常迷雾）
>     ⇒ 一对「染色开 / 关」的 A/B，用像素探针 **`homm-web/tools/closureprobe.mjs`**（**已入库**；**旁证尺、非验收数**）逐像素查
>     「**雾像素（`#0b0d10±2`）4 邻接"被染色层碰过"的像素**」：
>     **n=1020 个雾像素有这种邻居，其中邻接 `fill`（水洗）的 = 1020（100%），邻接任意带（红/亮芯/暗墨）的 = 0。**
>     ⇒ **可达区贴雾处，一条带都没画** —— 缺口**确实存在于渲染结果里**，不只是代码里的可能性。
>     ⚠️ **这条证明的边界（必读）**：它证明的是**"未探索交界处的缺口存在且其中 100% 是水洗贴雾"**；
>     **不**证明"缺口总长占轮廓多少"（`fog` 还包含图外底色，两者同色 `#0b0d10` 分不开）。
>     ⚠️ 另：该构建目录 `dist-139/` **在我取数后被删除**（`ls -d dist*` 现只剩 `dist`）⇒ 抓图当时可核的指纹 = 
>     `dist-139/render/MapRenderer.js` **md5 `02152006f00a010f94e863f775e7580e`**、`dist-139/main.js` **md5 `cfdb30d396b28c82b3a0a497e878a71e`**；
>     **现在无法再核**。⇒ 这正是 `production/roadmap.md`「状态绑定」那条反复在打的病：**数绑上了一个后来不存在的状态**。
>     **处置：本结论以代码腿为准（代码是 HEAD 里可复现的那份），像素腿作旁证并如此标注。**
>   - **✅ 要不要修 / 算不算问题 —— team-lead 已判（`368f68f`）：真缺陷、「要修」，但排在 `#141` 之后。**
>     **排序理由（team-lead）**：把「轮廓围住什么」的**语义改动**与「带画在光照哪一侧」的**渲染改动**捆在一起，**会让验收不可归因**。
>     ⇒ **修完由我用同一把尺（`closureprobe`）+ 同构建出前后对比**（本节即该尺子的定义处）。
>     **★ 通用化规矩（team-lead 刻，值得全文引用）**：**同一个量在「可见性闸」与「可达性闸」上口径不一致时，两者的交集处必然出洞** ——
>      要么**统一口径**，要么把那处差异**当成显式设计并测出来**。（本处 = `computePaths()` 不看 `isRevealed`、而绘制与 `inRegion()` 都看 ⇒ 交集 = 可达区贴雾那一段 = 洞。）
>     **⚠️ 状态定义（按 team-lead 新规矩「状态必须用可复现的定义表达」，`368f68f`）**：本节像素腿所绑的 `dist-139/` **已被清理** ⇒ 只写产物哈希**不合新规**；
>      **可复现的定义 = `commit` + 构建命令（+ 可选指纹）** ⇒ 补记：**源 = `commit 6dd5631`**（唯一含 `?devtint=0` 的提交）**+「当时的出目录构建命令」（该命令未记录，本节如实标注为不完整）**；指纹 `md5 02152006…` / `cfdb30d3…` 仅作辅助。
>
>   **★ `#138` 同构建单变量 A/B（`?devtint=0`）—— 已做（`#139` 解锁后的第一跑）；读数 + 两条边界如下。**
>   - **做法**：同一构建、同一次会话、同场景同相机、`SHOT_FREEZE=1`、真机视口 792×320@DPR3，**只差 `&devtint=0`**
>     ⇒ population = **整层 ④**（蓝填 + 红 + 亮芯 + 暗墨），**四族齐全**（这正是 `i_reach_*` 那对做不到的）。
>   - **读数（族占比，正午 → 深夜）**：fill `657 531 (88.2%)` → `658 861 (88.6%)`；edge `28 764 (3.9%)` → `35 142 (4.7%)`；
>     **glow `19 140 (2.6%)` → `1 562 (0.2%)`**；ink `39 958 (5.4%)` → `47 965 (6.5%)`。
>   - **★ 关键发现**：**亮芯族夜间塌了约 12×**（2.6% → 0.2%），且**归族残差中位 `8.77` → `119.59`（暴涨 ~14×）**
>     ⇒ 夜间的"亮芯"**已经不是那个颜色**（被整屏 multiply 拉离模型色）⇒ 这是「★ 管线检查①」的**第二个独立旁证**
>     —— 与"亮芯三相位单调走低"同向，但这次是**空间上的塌缩**（像素数），不只是比值下降。
>   - ⚠️ **不能当逐类验收数（population 被水洗灌满）**：fill 占 **88%** ⇒ 逐类中位被水洗主导。
>     ⇒ **用途 = ①证明整层 ④ 的存在与四族占比 · ②给"夜间亮芯塌缩"像素级证据**；
>     **逐类 × 逐带验收数仍只由 `tintab` 出**（契约第 6 条）。
>   - ⚠️ **副产物（一条陈旧判据的当庭判决）**：该 A/B 下 `reachmeas` 的「逐类」列**读到的是水洗而不是墨**
>     （正午「沙/土」`n=26 986`、中位 **`1.02`**）⇒ 再次证明该列的人口谓词（**众数变化色 ±3**）**是 A/B 相关的**、
>     只在 `(α)`「墨单带」那种 A/B 下才有意义 —— 与文件头「只作 `(α)` 基线可比列」互证。
>   - ⚠️ **证据图未入库**：按裁定「(ii) 证据图等 `#141` 的同构建 A/B 一起归档（现在归档 = 再进一次"草稿图"）」
>     ⇒ 本轮 6 张抓图**留在 `/tmp`**、只报数（`ad_tintON/OFF_noon`、`ad_night_ON/OFF`、`ad_fog_noon_ON/OFF`）。
>   - **✅ 观感 ⑤ 的覆盖 —— 已关闭（team-lead `368f68f` 核 `src/render/camera.ts:30`：`zoom = 2;`）**：
>     **默认视角 = `zoom = 2`** ⇒ **`#128` 已判的 `devzoom=2` 就是默认档** ⇒ ⑤ 覆盖的正是默认视角，**不是"只覆盖最有利视角"**。
>     ⚠️ **并更正一条我自己的方向错读**：我把 `?devzoom=1.6` 当成"放大" —— **错**；`devzoom` 取的是**绝对 `zoom` 值**、默认 `2` ⇒ **`1.6 < 2` 是缩小**（**更苛刻**的一档，不是更有利）。
>     要取更有利的档应 **`> 2`**。⇒ 本轮 `1.6` 那一档的读数**只加难度、不加覆盖**。
>
>   **★ 管线检查①：halo 确在光照乘之前 —— 已用像素实测钉死（team-lead 已批准做 (i) 预补偿原型）。**
>   - 同一像素、正午 vs 深夜（`devzoom=2` 真机视口，`SHOT_FREEZE=1`）：
>     **墨 `#2a1a12`：正午实测 `(38.2, 25.4, 17.1)` → 夜 `(26.6, 18.7, 13.7)`；"正午 × 夜光 tint(178,182,205)/255" 预测 `(26.7, 18.1, 13.7)` ⇒ 逐通道吻合（≤0.6 LSB）。**
>     亮芯 `#fff3c8`：正午 `(245.9,234.2,194.5)` → 夜 `(182.2,176.4,162.2)`，预测 `(171.6,167.2,156.4)`（差 ~10，**疑采样把亮沙也框进来了**，方向一致）。
>   - ⇒ **halo 整条线（两极）都被相位光照乘暗** ⇒ **亮芯三相位单调走低（暗 `4.00→3.46→2.77`、岩 `3.18→2.83→2.36`）是管线产物，不是选色问题**（WCAG 比值有 `+0.05` 底 ⇒ 两侧同乘小 ⇒ 比值必降）。
>   - **批准的做法 = 变体 (i)**：**只对 halo 的色值做光照预补偿**（按当前光照因子反算，使乘光后落回目标亮度）—— **零管线改动、零层序改动**。**⛔ 禁止变体 (ii)**（把光照挪到实体之前 = 让实体不再被光照，是行为变更）。
>     **预期只救「亮极」**（暗锚上限那条已证：草·中夜里纯黑也只到 `2.80`，挡的是地面亮度）——**而这次失败的正是亮芯** ⇒ 预补偿**正打在失败点上**。
>   - **门控先行**：**先只做原型 + 量那 4 个不过的格**（晨昏岩 / 夜草·中 / 夜暗 / 夜岩）⇒ **过了才做完整实现**；**不过就如实收档**。**完整实现先停**（等原型结果 + 暗缝那一类的判）⟹ 见 `production/roadmap.md`。
>   - **⚠️ `?devhalocomp` 的状态（2026-09-21 · team-lead ③ 修正 + 我独立复核）—— 写进文档以免后人误当仪器**：
>     ① **它不得当 A/B 仪器**：**层一级 A/B 用 `?devtint=0`**（整层 ④）、**带一级隔离用 `tintab` 的族开关** ⇒ **不要再为它加第四个旗标去量"层"**。
>     ② 工作树里已有人把它实现成**"预补偿候选"的开关**（`this.haloLightComp`）—— **那是另一件事**（改色值 vs 改层），**team-lead 已放行该实验**。
>     ③ **★ 硬伤（team-lead 报 · 我读码复核成立）⇒ 已修**：**我读到的那一版**（工作树 md5 `9b79241a`）把取值写在 `if (cost && this.tintEnabled) {` **守卫内部**
>        ⇒ **`?devtint=0` 会把整块跳过、连预补偿一起关** ⇒ **"量测仪器"与"被测对象"共用一个开关，永远说不清测到哪一层。**
>        ⇒ team-lead 把它列为**硬条件 (i)** 要工程侧修；**现行工作树已解耦**（`MapRenderer.ts` 自述「与 `tintEnabled` 解耦（硬条件 i）」、`compOn` 已移到守卫**之外**；工作树 md5 `aa703345`、**未落库**）
>        ⇒ **本行此后不再按"不可用于 A/B"处理**；**但硬条件 (ii)「缺省逐像素空操作」仍须按 `#139` 那套判据证明后才可用**（别拿"解耦了"当"可以取数了"）。**⛔ 更正（2026-09-21，team-lead ③）：该证明「已存在」** —— `engineering-lead` 用 **`git worktree` 建 HEAD 版 vs 本版**逐像素对比 = **0 / 2 280 960 像素、maxd 0** ⇒ **硬条件 (ii) 已满足**。**我此前写"仍须证明"是漏读**：那份证明只落在**消息**里、没落进**文档** ⇒ 我按"看不见=不存在"处理了。这正是 team-lead 据此新立的规矩：**窗口宣告与关键证明必须落在「广播通道」（`roadmap.md` / 契约文档），不能只发给被执行人**（`roadmap.md` `1565f85`；见 V1.19）。
>   - **⚠️ team-lead 的"天花板"论证 + 他的自我更正 + 我对该更正的复核（三段都留，别只读一段）**：
>     **原话**：「屏幕空间乘暗给任何画出的颜色设了**天花板** ⇒ 数学上不可能（纯色值提亮救不回来）」。
>     **team-lead 自我更正**：「那条只对**纯乘法**成立 ⇒ 预补偿未必不可能；它的天花板由算子决定、**必须实测**。」
>     **★ 但"不是纯乘法"这一半，我复核后认为不成立（数字摆出来判，别靠直觉）**：
>     **①「暖味没了 / 色相变冷」不是反证** —— **纯乘法本身就会变冷变灰**（乘 `(0.698,0.714,0.804)` 后 G/R `0.952→0.974`、B/R `0.791→0.911` ⇒ **"暖味没了"正是纯乘法的预期结果**）。
>     **② 真正的判别量 = 每通道比值是否恒定**：**墨** `(38.2,25.4,17.1)→(26.6,18.7,13.7)`，比值 `(0.696,0.736,0.801)` ≈ 预测 tint `(0.698,0.714,0.804)`（**吻合 ≤0.6 LSB ⇒ 墨上"纯乘法"成立**）；
>     **亮芯**比值 `(0.741,0.753,0.834)` **三通道一致地比预测高 `+0.03~+0.04`** ⇒ 是**整体偏亮**、**不是"通道间不守恒"** ⇒ **与"采样框进了更亮的底（亮沙）"一致**（该采样我此前已自标此疑）。
>     ⇒ **净结论**：**「halo 在光照乘之前」两腿不变**；**「`applyLighting` 不是纯乘法」目前没有证据支持** ⇒ **⛔ 别拿它当"预补偿未必不可能"的理由**；
>     **预补偿行不行，靠原型实测（已放行、见下"修法优先级"），不靠这条论证。** 完整算式 = `shots/README.md`「对上面那条"不是纯乘法"的论证的复核」。
>     ⇒ **两条修法（`(ii)` 双色 halo / 变体 (i) 预补偿）**都**放行**、**不预判、谁过谁上**。
>     **★★ 定案（2026-09-21 · team-lead 实测 + 裁定；本文档据此收口 —— 上面「不预判、谁过谁上」已被本条取代，留痕不删）**：
>       - **修法 (2) 预补偿（变体 (i)）= 判掉。** 实测（`?devhalocomp=1`）：**只救暗极** —— 暗墨 `#2a1a12` 命中 `228 → 1 740`，而**亮芯 `#fff3c8` 仍 `0` 命中** ⇒ **天花板是结构性的**（"画在纯乘法之前"这一点决定）⇒ 与 team-lead **撤回又被放回台面**的那条论证**一致**。**处置**：**不采用、但保留旗标**，并在文档写明"**这是被实测判掉的一条路**"（**留痕不删**）。
>         ⇒ **本文档此前对预补偿的两组说法据此收口**：**「天花板由算子决定、必须实测」已实测完毕**；**「`(ii)` 双色 halo 与预补偿谁上」的问题已答**（预补偿出局）。
>       - **修法 (1) 三条带画在光照之后（`?devhaloafter=1`）= 采用。** 实测：**三个锚色恢复到标称色** —— 亮芯 `0 → 56 016`、暗墨 `228 → 61 200` ⇒ **与算子无关的结构性修法** ⇒ **通过验收后由 owner 把旗标翻成默认**。
>       - **★ 一条判据纪律（team-lead 定，本文档采纳）**：**(1) 必须过同一套判据** —— **"锚色恢复了"不能代替判据**（锚色恢复只证明**乘暗消失了**、不证明**对比度过线**）⇒ **"证据形态" ≠ "判据"**。④ 的验收数仍按 §4.6 契约第 6 条：**逐类 × 逐相位、四带中位里的最大值 ≥3:1，且该最大值必须落在亮芯/暗墨**。
>       - **⚠️ 已知代价 + 一项待我出的判定（尚未做）**：修法 (1) 下**三条带落在实体之上**（旧版里实体按 y 排序会遮挡它）⇒ team-lead **接受为有意设计**，但要求**观感确认"一条线横穿一个单位"不造成误读** ⇒ **归 `art-director`**（与 `#128` 同一把尺：⑤ 语义可读 / ⑥ 不在内部描边）。**状态：未做** —— 需在**含该旗标的构建**上抓 `?devhaloafter=1` 的图；**我不自行取数，等 team-lead 给窗口/一句线**。
>       - **★ 硬条件 (ii)「缺省逐像素空操作」= 已证**（team-lead 用 `git worktree` 建 HEAD 版对比本版：**`0 / 2 280 960` 像素、`maxd 0`**）⇒ **本文档 V1.17(3) 的「仍未证明」据此更正为「已证」**；**但取数纪律不变**（仍须过"仪器与被测解耦 + 默认空操作 + 四格判据"三条）。
>       - **★ 归属裁定（team-lead，2026-09-21）**：**出数 = `engineering-lead-2`（`tintab` owner —— 避免"一个量两个生产者"）；判定 = `art-director`（我）；`engineering-lead` 不出数。** ⇒ **我此后不自行出 ④ 的验收数**（`#145` 那条腿属**旁证**性质，team-lead 已**降级为"有余力再做"**，不阻塞任何人）。
>       - **★ 另一个坑（team-lead 逮到、记为新失效模式，与我这类"绑构建"同族）**：`tinytargetaudit` 等门控**会复用已经在 `127.0.0.1:5173` 上跑的服务**，而那个服务端的是**真 `dist`** ⇒ **静默量错对象**（它先得到 22 条 FAIL，量的是真 `dist` 的探针）。⇒ **临时构建做验证时必须给门控一个空闲 `PORT` 让它自起服务，并在工具头注打印"我服务的目录 + 指纹"。**
>     **★ team-lead 已按此撤回"不是纯乘法"并「把天花板论证放回台面」（`368f68f`）** —— **若 `applyLighting` 是纯乘法，预补偿就有硬天花板**：
>       **亮极要"夜里仍亮" ⇒ 须先除以该帧的乘子 `k` ⇒ 要求某个通道值 `> 255` ⇒ 不可能**。⇒ **预补偿的先验期望低、修法 (1)「画在光照之后」仍优先**；
>       **实测仍放行（判据同 `#141`），但不得再拿"也许不是纯乘法"当它的理由。**
>   - **★★ 修法优先级正式定序（team-lead `e0885e1`，2026-09-21）—— 与本文档此前的"不推荐"相反，故挂取代横幅：**
>     **(1) 把三条带画在光照之后**（= 原 **(a-2)** 的做法）—— **优先**。理由：它是**结构性修法、与 `applyLighting` 的算子无关**（不依赖"反算得对不对"）。
>     **(2) 预补偿**（变体 (i)，`?devhalocomp=1`）—— **并行候选**：**其天花板由算子决定 ⇒ 必须实测**（见下「team-lead 自我更正」）。
>     **两条判据完全相同**：**让 晨昏`岩 2.83` / 深夜`草·中 2.94` / 深夜`暗 2.77` / 深夜`岩 2.36` 四格转绿，且不许弄坏已过的格** ⇒ **谁过谁上。**
>     **⛔ 本文档 (a) 处置里 (a-2) 的「【不推荐】」已被本条取代**（留痕不删）—— **理由改写**：原列为不推荐的两条**实现坑仍然有效、必须被解掉**（① 屏幕空间要带相机变换，否则**错坐标**；② 带会画在**实体之上**，须防**盖住单位**），
>       但它们**不是否决理由，而是实现要求** ⇒ **处置：从"不推荐"改为"优先候选 (1)，两坑由工程侧负责解掉并须验证"**（验证 = 与光照前的版本做 A/B，证明**带的位置逐像素不偏、且未遮住任何单位**）。
>     **落地状态（我读码）**：工程侧已同时做了两个**默认关**的旗标 —— `?devhaloafter=1`（候选 (1)：边带只"登记"到一个 `BandRect[]`、推迟到光照后落笔）与 `?devhalocomp=1`（候选 (2)）；**是否可用须过其各自的空操作证明 + 四格判据**，**本文档不预判**。**⚠️ 状态更正（2026-09-21 `10:27` 起，取代原写的"两者均为未落库 WIP"）**：**两者已落库 = `commit ad4ffb4`**（已核为 HEAD 祖先），`dist` 亦于 `10:28:50` 重建为 **`1c718db31f6a`**（含 `?devtint` / `?devhaloafter` / `?devhalocomp`）⇒ **"未落库"作废**；**但"落库 ≠ 验过"** —— 可用性仍以**空操作证明 + 四格判据**为准。
>     **★ V1.16 状态更新（2026-09-21 · `art-director` 读码复核）—— 「未落库 WIP」这句已过时，但「可用」仍未被证明**：
>     **① 已落库**：含这两个旗标的那一版 `MapRenderer.ts` **已落库于 `ad4ffb4`**（`10:27:07`）—— 其内容哈希与我 V1.13 记的**工作树版一致**（md5/8 `aa703345`、sha1/12 `f4c71d970b4f`；工作树 `≡ HEAD` 已核）⇒ **落库内容 = 我当时读到的那份，未被改写**。
>     **② `dist` 已重建**（`10:28:50`）**且 `android/app/src/main/assets/public/` 同步拷贝**（`10:29:15`）⇒ **现行 `dist` 已含两旗标**（见下 `#145` 段的「构建已换」条）。
>     **③ 空操作证明 = 已满足；仍待判的只有「四格判据」（更正）** —— 硬条件 (ii) 已由 `engineering-lead` 用 **`git worktree` HEAD 版 vs 本版** 证明：**0 / 2 280 960 像素、maxd 0** ⇒ **已证逐像素空操作**；**但四格判据仍未跑**（判据 = 让 晨昏岩 2.83 / 深夜草·中 2.94 / 深夜暗 2.77 / 深夜岩 2.36 **四格转绿、且不弄坏已过的格**）。**⛔ 更正"仍未证明"**：根因同一句 —— **证明只落在消息里**；依 team-lead 新规矩**关键证明必须落在广播通道**（`roadmap.md` / 本文档）。**四格结果本文档不预判。**
>     **④ 归属**：两旗标**非 `engineering-lead` 所落**（他已声明，作者 = 另一具名实例）⇒ **要做 A/B 须先由 team-lead 明确归属**（他说「要 A/B 请让 team-lead 明确归属」）。

> - **★ `#145` ④ `(ii)` 的「通道独立腿」（真机视口 792×320@3）—— ⛔【预跑 · 已作废】不作 `#145` 验收数。**
>   **⛔ 作废理由（team-lead 令「先别跑」+ 现场实证）**：本次跑于 `10:23–10:24`（**在收到「先别跑」之前**），当次被测物 = `dist/` **`13d3b4200709`**（mtime `09:54:34`）。**该产物随后被重建** —— **`10:28:50` 起盘上已是 `render/MapRenderer.js` `1c718db31f6a` / `main.js` `64b123137afa`**（含 `?devtint` 6 / `?devhaloafter` 5 / `?devhalocomp` 4 处，已 grep 计数），其间 HEAD 又前进两次（`ad4ffb4` `10:27:07` → `74157c8`）⇒ **盘上的 `dist` 已不是被测量的那一个** ⇒ **本腿数据不作 `#145` 验收数，等冻结窗口内重跑**。
>   **★ 教训（通道参数不解决"何时可取数"）**：`--vw/--dpr` 只换**通道**；**取数必须落在已声明的冻结窗口内**，否则数会绑到**很快被覆盖的产物**上 —— 本轮即现场实证：**数出后 4 分半，被测产物即被重建**。
>   **▼ 以下为预跑记录：⛔ 不作 `#145` / `(ii)` 的验收数；留痕不删**（供与冻结后那组对照；其中**岩/暗两条诊断**与**两条给 `tintab` owner 的建议**与构建版本无关，仍有效）。
>   **口径纪律**：此腿**必须写成「通道独立」**（同一把 `tintab`、同一族定义，只换视口与抓帧通道），**不写「独立复核」**；**换视口 = 换 population** ⇒ **逐格数不得与桌面比，只比方向 / 判定**。
>   **绑定（文件 + 算法 + 值）**：`dist/render/MapRenderer.js` **sha1/12 `13d3b4200709`**（mtime `2026-09-21T01:54:34Z`）· `dist/main.js` **sha1/12 `ecbfa1e279f2`** · `src/render/MapRenderer.ts` **sha1/12 `f4c71d970b4f`**；`tintab` 自证三件套 **①②③ 全 OK**（`src≡dist` 常量一致 / 跑前跑后指纹未变 / HEAD 测前=测后）。两腿跑动间 **HEAD 标签前进过一次**（`368f68f` → `36becdf`），但**被测物 `dist` 两腿完全相同**（`13d3b4200709`、mtime 未变）⇒ **当时**并列有效；**HEAD 标签的差异如实记在此**。**⛔ 但该"并列有效"已被 `10:28:50` 的重建推翻（见上方横幅）—— 两份 `dist` 现在只剩盘上这一份，且它不是被测的那一份。**
>
>   | 类 | 桌面 1280×677：`max(亮芯, 暗墨)` 0.22/0.5/0.68 | 真机 792×320@3：同栏 | 同判？ |
>   |---|---|---|---|
>   | 草·亮 | 5.18 / 4.07 / 3.18 ✅✅✅ | 5.41 / 4.54 / 3.27 ✅✅✅ | ✓ |
>   | 草·中 | 3.52 / 3.34 / **2.94 ❌** | 3.80 / 3.21 / **2.92 ❌** | ✓（**含唯一失败的草类格**） |
>   | 草·深 | 6.79 / 6.06 / 4.74 ✅✅✅ | 6.99 / 5.87 / 4.77 ✅✅✅ | ✓ |
>   | 沙/土 | 7.43 / 6.07 / 4.32 ✅✅✅ | 7.99 / 3.23 / 4.47 ✅✅✅ | ✓（0.5 值差大 ⇒ population 不同，非矛盾） |
>   | 水 | 4.71 / 4.79 / 3.64 ✅✅✅ | 4.54 / 4.56 / 3.49 ✅✅✅ | ✓ |
>   | 暗 | 4.00 / 3.46 / **2.77 ❌** | 8.96 / 7.74 / 5.50 —— **⚠ 不可用** | ✗ |
>   | 岩 | 3.18 / **2.83 ❌** / **2.36 ❌** | **0.22 与 0.68 全带 n=0；0.5 仅 ink `1032`** | ✗ |
>
>   **桌面腿同时是 `#124` 那张 `(ii)` 表的可复现性检查**：**7 类 × 3 相位 × 亮芯/暗墨 = 21 格逐格复现**（如 草·深 `6.79/6.06/4.74`、岩 `3.18/2.83/2.36`）⇒ 该表**在其绑定构建上可复现**（**可复现性 ≠ 独立性**）。
>   **⚠️ 「暗 / 岩」在这条腿上为什么不可用（如实报）**：
>   - **岩测不到**：`rock` 在真机视口**看不到该地形** —— 三相位四带 `n` 全 0（0.5 仅 ink `1032`，而桌面同相位 `5541`）⇒ **`(ii)` 的「晨昏岩 / 夜岩」两格无法由这条腿复核**。
>   - **暗不稳定**：`(a)并集` 与 `(b)全族` **在 0.5 相位差 21×**（`32607` vs `1548`；0.22 差 11×：`1548` vs `17260`）⇒ **守恒断言（契约第 6 条③）不成立** ⇒ 该类的"全量"本身没定住 ⇒ **数不可用**（`tintab` 头注已预告此病，此处**独立复现**）。**桌面视口无此病**（`dark` 三相位 `4431/4431`、`4428/4428`、`4426/4426`）。
>   ⇒ **结论**：该腿**佐证 5/7 类**（含唯一失败的草类格 草·中 夜 `2.92 ❌`），但 **`(ii)` 的两个死格（岩 / 暗）在真机视口既测不到、又不稳定** ⇒ **本腿不能替代桌面腿承担 `(ii)` 的验收**。
>   **⚠️ 一条自检发现（给 `tintab` owner）**：同一次真机跑动里**画布 dpr 逐相位不同**（0.22 相位 canvas `1584×480 dpr2`、0.5 与 0.68 相位均 `2376×720 dpr3`；未覆写 dprCap ⇒ 由画质档自动探测决定）⇒ **同一跑动内跨相位比较不干净**（0.22 那组是 1.5× 上采样）。⇒ 建议加 `--tier` 固定档位（`--devdpr` 只改 dprCap，且真机 mid 档本来就是 2，**用 `--devdpr=3` 反而不是真机条件**）。**§4.6 的验收数仍以桌面 1280×677 腿为准。**（桌面腿三相画布恒 `1280×677 dpr1` ⇒ **不受此病影响**。）
>
>   **✅ 测后复核（本次落档时重跑，2026-09-21）—— 回答"这些数还算不算数"**：`dist/render/MapRenderer.js` sha1/12 **`13d3b4200709`**、`dist/main.js` **`ecbfa1e279f2`**、mtime **`2026-09-21T01:54:34Z`（= 本地 `09:54:34`；本机 `stat -f %Sm` 会按**本地时区**打印却仍带 `Z` 字面量 ⇒ 勿把 `09:54:34Z` 与 `01:54:34Z` 误判为"时区矛盾"）** ⇒ **（⛔ 此句已被 `10:28:50` 的重建推翻 —— 它只描述"落档那一刻"；此后 `dist` 已变为 `1c718db31f6a` / main `64b123137afa`。见上方作废横幅。留痕不删）**。**HEAD 已前进**（`368f68f` → `36becdf` → 今 `74157c8`）⇒ **本读数不是当前 HEAD 的读数 —— 但它绑的是 `dist/`、不是 HEAD** ⇒ **结论仍成立、且可复核**。按 `368f68f` 新规矩（状态须用**可复现的定义**表达）：**该 `dist` 的构建命令未记录**（如实标注）⇒ 此处只能绑指纹。
>   **★ 顺带把"src 侧"也钉到 commit（原先只能写指纹）**：出数时 `src/render/MapRenderer.ts` sha1/12 **`f4c71d970b4f`**、mtime `2026-09-21T02:21:51Z`（**比 `dist` 的构建晚 26 分钟 ⇒ 当时 `src` 是 `M`、未落库**）。该内容**现已落库 = `commit ad4ffb4`**（`74157c8` 未再改它；`git show ad4ffb4:homm-web/src/render/MapRenderer.ts | shasum -a 1` = `f4c71d970b4f`）⇒ **当时那个"不可复核的工作树"现在可复核了**。**★ 但这救不了整条腿** —— `src` 可复核 ≠ 整条腿可复核：① 盘上的 `dist` 已被重建（见横幅）；② 更根本的是 **`dist ≢ src`**（见下条：被测 `dist` 早于 `#139`，根本不含 `?devtint`）。
>   **⚠️ 一条必须写明的边界：`src ≢ dist`（整文件级）** —— 出数时的 `dist/`（09:54 构建）**不含** `?devtint` / `?devhalocomp` / `?devhaloafter`（我在 `dist/` 里 grep 计数 **0 / 0 / 0**），而 `src` 含（**4 / 5 / 6** 处）。`tintab` 头部那句 `src!=dist: 已核一致` **只比 ④ 的四个常量**（我独立复核：`rgba(120,200,255,0.16)` / `rgba(255,96,80,0.42)` / `#fff3c8` / `#2a1a12` 在 `dist` 与 `src` 中**逐字一致**）⇒ **该检查"通过" ≠ "同构"**。⇒ **引用本条读数请写「绑 `dist/` `13d3b4200709`（09:54 构建）」**，**不要写成"绑 `src` `f4c71d970b4f`"**（后者是 26 分钟之后的代码）。**⛔ 且该 `dist` 现已不在盘上（`10:28:50` 重建为 `1c718db31f6a`）⇒ "绑 `dist` 指纹"这条路也断了；而该 `dist` 的构建源 commit 同样未钉住（`09:54:34` 的构建早于 `6dd5631` = #139 `?devtint`（`10:12`）⇒ 不含 `?devtint`，与 `src` 的 4/5/6 处不同）⇒ 构建命令 + 源 commit 双缺 ⇒ 该状态不可重建 ⇒ 依 team-lead 令：作废、冻结后重跑。**
>   **⚠️ `tintab` 头注一处过时（给 owner）**：头部「统计量」行仍写「**WCAG 取均值**」，而列头与本轮口径是「`WCAG中位`」（另有 `WCAG均值(参考)` 列）⇒ **该行与列头自相矛盾**，后人易据此把工具口径**误记为均值**；team-lead 已把统计量钉为**中位** ⇒ 建议改行（本轮我一律读 `WCAG中位` 列）。
>
>   **★ 同轮补记：守恒断言在真机腿上的逐类拆解 —— 回答「那不稳定的到底是哪几类、能不能定位」**
>   读法（契约第 6 条③，V1.11）：**并集 < 全族 ⇒ 缺源**（有变化像素不属于任何带）⇒ **覆盖失败**；**并集 > 全族 ⇒ 逻辑上不可能**（各带并集不可能大于该类总变化）⇒ **两列不在同一帧/同一像素网格上**。
>   · **桌面腿全部 ≤0.8%**：`grass-*` 三相逐字相等；`rock 5541/5498`（差 43 = 1px 交界重叠的「多源」量，**恰如契约预期**）⇒ 桌面腿的守恒读法**可信**。
>   · **真机腿只在 0.68 相位全类成立**，另两相位共 4 类破：

| 相位 | 破的类（并集 / 全族） | 读法 |
|---|---|---|
| 0.22（`dpr2`） | `rock 0 / 3759` · `dark 1548 / 17260` · `water 52416 / 76969` | **并集 < 全族 ⇒ 缺源** ⇒ 覆盖失败 |
| 0.5（`dpr3`） | `rock 1032 / 0` · `dark 32607 / 1548` · `sand 198821 / 192852` · `water 52561 / 52416` | **并集 > 全族 ⇒ 逻辑不可能** ⇒ **两列不同帧/不同网格** |
| 0.68（`dpr3`） | 无（全类并集 == 全族） | — |

>   **★ 更好用的判决器 = 「类总量三相位必须恒定」**（分类帧 = 身份帧 ⇒ 该类像素集不随相位变）：
>   · **桌面腿**：`grass-bright 48481/48481/48481`、`grass-mid 24440/24440/24432`、`rock 5541/5541/5541`、`dark 4431/4428/4426` ⇒ **恒定（波动 ≤0.1%）** ✓
>   · **真机腿**：`rock 3759 / 1032 / 0`（**三相全不同**）、`dark 1548 / 32607 / 1548`、`sand 192852 / 198821 / 192852`、`water 52416 / 52561 / 52416` ⇒ **不恒定** ⇒ 分类帧在该视口**不是常数**。
>   **假说 H1（可证伪、低成本）**：真机腿里**画布 dpr 逐相位不稳**（0.22 = `dpr2`/`1584×480`；0.5 与 0.68 = `dpr3`/`2376×720`）⇒ 分类帧与抓帧**像素网格不一致 ⇒ 桶索引错位** ⇒ **最小的类（`rock`）整类落空**（`3759 → 1032 → 0`）、0.5 的 ink 族暴涨（`dark ink 1080 → 31455`、`all ink 10934 → 71546`）。
>   **判决性实验（给 `tintab` owner）**：加 `--tier`（或显式钉住 dprCap）**固定画布 dpr** 后重跑三相 ⇒ **若 `rock` 类总量三相位变成同一个常数、且守恒全相位成立 ⇒ H1 成立**；否则另找根因。
>   ⇒ **H1 未决前，真机腿的 `dark` / `rock` 一律记「未取」**（口径：**不拿一个自身没定住的量去判过/不过**）。
>   **⛔⛔ 根因已被实测定位并修复（`engineering-lead-2` · `82ad4b5` · `10:33`）—— 并修正本段两条结论的「成因」（`art-director` 补记，2026-09-21）**
>   · **根因（实测，非猜测）**：**①「首帧未 settle」** —— 同 mode 连抓三张，`#1` 与 `#2/#3` 差 `10w+` 像素、**`#2 == #3` 逐位相同**，且**与 mode 无关**（`none` 先则 `none#1` 出格、`full` 先则 `full#1` 出格）⇒ 修法 = **录制前丢弃首帧**（**与 G-15 同族**）。**②「启动 toast `#hint`」**（`main.ts:604`）约载入后 `1.5s` 出现在地图区下缘（792@3 bbox device `412×93`）、`2s` 淡出 ⇒ **落在录制窗内即污染**（曾把 `dark` 的 `(a)并集` 抬高 `31059`）⇒ 修法 = 注入 `#hint{display:none}` + **录制前核验 `computed display:none`**。
>   · **修复后实测**（792×320@3、`--phase=0.22`）：`dark` / `sand` / `grass-dark` / `water` **全部 `(a)==(b)`、`缺源==0`**；**`dark` Σ=(a)=(b)=`1548`**（此前 `32607/1548` 互换）⇒ **`#150` 已闭**。**桌面 1280×757 回归与既有基线逐格一致** ⇒ 修复未动桌面腿（回归数：`sand` `+719` vs 多源 `683`、`grass-dark` Σ=38286、`water` 缺源=2）。
>   · **★ 对本段的修正（重要，防被误引）**：**「暗在真机视口不可用」的成因不是"该视口属性"，而是捕获端缺陷**（首帧 + DOM 覆盖层）⇒ **不要把「真机视口 = 不可用」当结论引用**。同理 **「岩 `n=0`」也不再能归给视口** —— 但**它是否同样由这两条根因造成，仍未验**（修复验证**只跑 0.22 单相位、且未报 `rock`**）⇒ **`rock` 仍记「未判 / 未取」，成因留空**（不猜）。
>   · **★ 我的 H1 至此被实测证伪**（他定位的两条根因里**没有 dpr**）—— 我此前已自请降级，**此处补上「被证伪」**（两段并存留痕）。**唯一仍独立成立**的是那条卫生项：**同一次跑动内画布 dpr 逐相位漂移**（0.22 `dpr2` vs 0.5/0.68 `dpr3`）⇒ 跨相位比较不干净、**桌面腿无此病**。
>   · **⇒ 对 `#158` 的影响**：**捕获端已修** ⇒ 真机腿**有可能真的能承担一部分 `(ii)` 旁证**（此前"不能"的**理由已被抽掉**）⇒ 重跑的**信息增益上升**；**但仍等 team-lead 给窗口 + 归属**（**不自行开跑、不自行改阈值**）。
>
>   **⛔ 我自请更正 H1：它作为"唯一根因"不成立 ⇒ 降为「一条待验的子假说」（同日 · `art-director`）**。**反证就在本段的表里**：**0.5 与 0.68 都是 `dpr3` / `2376×720`，却一个破、一个全类成立** ⇒ **dpr 错位解释不了 0.5 相位的 ink 族暴涨**（`dark ink 1080 → 31455`、`all ink 10934 → 71546`）。⇒ **不要把「加 `--tier`」当成已定根因的修法**；它只解掉「**同一跑动内跨相位不干净**」这一条 —— 而**那一条独立成立**（0.22 那组是 1.5× 上采样）、且**桌面腿无此病**（恒 `1280×677 dpr1`）。
>   **★ 现行裁定（team-lead，同期，效力高于本段）**：
>   · **`#150` 不再诊断** —— 捕获端**钉一个预热帧**（我此前已证「**792 有预热即逐字节确定**」）；**若仍互换 ⇒ `dark` 的归属记为「tie 敏感、给区间」、只标在重叠列**（**不影响契约的 `缺源 == 0` 判据**）。
>   · **`#145` 降为「旁证、不是门、有余力再做」**，**不阻塞任何人**。
>   ⇒ **本段定性（照 team-lead）**：**`#145` = 旁证**；**`dark` / `rock` 在真机视口的数一律「未取」**；**`(ii)` 的验收仍走桌面 `1280×677` 腿**；**本段读数绑的是「已消失构建」⇒ 引作验收须在现行构建上重跑**。
>   **⛔ 一处与其他文档冲突的事实，必须并读（防"两文互相矛盾"）**：本文档 **V1.17(3)** 与上方 §4.6 两处写「**硬条件 (ii)「缺省逐像素空操作」仍未证明**」；而 `production/roadmap.md` 同日一条（team-lead 记）写 **`ad4ffb4` 的两条硬条件都满足**，其证据 = **`git worktree` 建 HEAD 版对比本版：`0 / 2 280 960` 像素、`maxd 0`**。⇒ **两处并存、不删、不静默改写**；**以 roadmap 那条为准**（它有可复算的证据）—— 那两句应读作「**落档当时我未见到证据**」。**仍待办的只有「四格判据」**（晨昏岩 `2.83` / 深夜草·中 `2.94` / 深夜暗 `2.77` / 深夜岩 `2.36` 四格转绿、且不弄坏已过格）—— **「空操作已证」与「四格转绿」不是一回事，不得互相顶替**。
>
>   **⛔⛔ 「构建已换」—— 落档后约 90 分钟发生的既成事实（**自 `#124` 表落档起算；自 `#145` 预跑"数出"起算则为 4 分半 —— 同一事件、起点不同，非矛盾**），必须与本段所有读数并读（2026-09-21 10:30 复核 · `art-director`）**：**上面那句「结论仍成立、且可复核」的尾句已被本条取代**（留痕不删）。
>   **上表所绑的那份 `dist` 已被重建覆盖，且旧构建在仓内已无副本 ⇒ 本段读数（含 `#124` 的 `(ii)` 表）不再可复核。**
>   - **旧（= 本段的绑定）**：`dist/render/MapRenderer.js` sha1/12 **`13d3b4200709`** · md5/8 `962f3558`；`dist/main.js` sha1/12 **`ecbfa1e279f2`** · md5/8 `d7b1f55f`；mtime **`09:54:34`（本地）= `01:54:34Z`**。
>   - **新（现行）**：`dist/render/MapRenderer.js` sha1/12 **`1c718db31f6a`** · md5/8 `76415ae9` · `44 077` bytes；`dist/main.js` sha1/12 **`64b123137afa`** · md5/8 `ff653bce` · `66 013` bytes；两份 mtime 均 **`10:28:50`（本地）**。**同时** `android/app/src/main/assets/public/` **已同步拷成新构建**（`10:29:15`、两文件同哈希）；`dist-el/` 与 `dist-139/` **一并消失**（现只剩 `dist`）。
>   - **旧构建为何救不回来（我找过了，别重复找）**：仓内另有两处 `render/MapRenderer.js` —— `android/app/src/main/assets/public/`（= 新构建）、`android/app/build/intermediates/assets/release/mergeReleaseAssets/public/`（sha1/12 `219fa492631e`、mtime `07:20:34` ⇒ **是更早的构建，不是我们量过的那份**）⇒ **`13d3b4200709` 在仓内已无副本**。
>   - **⇒ 处置（`a35f923` 同构建纪律 + `368f68f` 状态定义纪律）**：**①** 本段与上表**照原样留痕、不删、不改数** —— 它们是**「已消失构建」上的历史读数**；**②** 若要引作 §4.6 的 `(ii)` 验收数，**必须在现行构建上重跑**（我**未**在新构建上跑过、**不冒充**）；**③** **不得**把新构建的数与上表数并列成"同一个量的两个数"（**换构建 = 换参照**，跨构建只比**方向 / 判定** —— 与「换视口只比方向 / 判定」同一条纪律）。
>   - **⚠️ 这同时是一条流程事件（已上报 team-lead）**：按 team-lead 自己的规矩（dev 构建用自有 `outDir`；`dist/` 只服务**已宣告的冻结测量窗口**；**写 `dist/` 须事先报备 + 确认**），本次 `dist` 写入**我未收到任何宣告** ⇒ 我的在途绑定被**静默作废**，**正是该规矩要防的失效模式**。**我已停手不取数**（避免与可能正在进行的 ④ 原型测量撞车 = 本会话已吃过两次的"同一个量两个数"）；**要我在新构建上重跑本腿 / 出四格判据，请 team-lead 给一句线**。
>   - **✅ 上面「`src ≢ dist`」那条边界，其真实含义现在可以说得更准了**：旧 `dist` 不含那三个旗标 —— **这不是 grep 假阴性，而是构造上不可能**：`?devtint=0` 落库于 **`10:12:02`**、两候选旗标落库于 **`10:27:07`**，而旧 `dist` 构建于 **`09:54:34`** ⇒ **三者都晚于该构建**。⇒ 本段读数因此是**未含任何候选修法的"纯基线"**（作 `(ii)` 基线是干净的）；**但它同时钉死了硬条件 (ii) 仍未被证明** —— **"默认关" ≠ "已证逐像素空操作"**，**不得互相顶替**。
>   - **⚠️ 一条工具陷阱（今日踩到，写入防后人误判「无匹配」）**：本环境的 `grep` 是 **toybox 0.8.13（非 GNU）**，**不支持 `\|` 这种 BRE 交替** ⇒ `grep -c "A\|B" f` **恒返回 0**（假阴性、退出码 1）。**今日我第一次核对时就它被骗出过"全 0"**。⇒ 规矩：**核对源码 / 产物里的旗标或常量，一律用 `grep -E "A|B"`（或 Grep 工具），不用 `\|`**；**本会话所有"含 / 不含"结论均按此复核**（上面那条 0 / 0 / 0 另有构造性旁证，故不受此陷阱影响）。
>
> ### ★ 两条 a11y 裁定（2026-09-21 · 应 team-lead 请裁；**裁定权在本文档**）
>
> **(a) 夜间是否仍以 WCAG 3:1 为判据？ —— 是：判据不随相位变，夜间不豁免、不降尺。**
> - **1.4.11 无「夜间」豁免**：它管的是**渲染出来的状态**的可辨性；夜间是渲染结果的一部分，**不是**另一种可豁免的呈现。
> - **降夜间尺 = 正被禁的「调到达标」**（`production/roadmap.md` `bd4ebb0`）。若因夜间过不去就把夜间阈值降到能过的水平，等于绕开与 `a35f923`（阈值必须与所守护代码同构建）同源的纪律。
> - **`ΔL* 16.8` 与 `1.56:1` 不矛盾、也不互替**：前者是「**看得出**」（感知量），后者是「**达不达标**」（规范量）。按 `3f11752`（★ **不是"把 ΔL\* 换成 ΔE\*ab"，而是"判据必须说清它回答什么问题；两个方法的数不能共用"** —— 此读法经 team-lead 确认、并更正了他先前"尺子改 ΔE\*ab"的窄表述），**判据仍是 WCAG 3:1**；`ΔL*` 只作**旁证列**，**不能顶替 1.4.11**。
> - **⇒ 夜间墨线对草地 ≈ 1.56:1 时，「夜读 FAIL」如实成立**（与我 V1.2 的 **1.12(夜)** 同向；(α) 已改善但仍不足）。**不换尺子、不叠下一层。**
>   - **⚠️「不叠下一层」禁的对象（写死，防误禁标准技法）**：禁的是「**为凑一个标定值而叠加的装饰性色层**」（= `5.09` / 「调到达标」那一类）。**不在禁列**：(a-1) 的 **双色 halo（亮芯 + 暗边）** —— 它是**同一个「边」语义载体**在**两个底色方向**上各给一条，**不是两层装饰**（1.4.11 本就要求对**两侧**相邻色**各** ≥3:1）。⇒ **判 (a-1) 可做，不算"叠层"。**
> - **处置（要求，本轮不实现）**：④ 是**信息层**，不该被氛围层调暗。三条路：
>   - **(a-1) 「双色 halo（亮芯 + 暗边）」【推荐】** —— 原写「相位感知描边」，2026-09-21 按 `ux-ia` 提法**归一命名**（同 = team-lead 的 **(ii)**，勿再当两套处方）：**边界同时给亮芯 + 暗边**，使描边对**亮底与暗底各自 ≥3:1**（暗底吃**亮芯**、亮底吃**暗边**）。**成本小**（④ draw 块再画一条更亮的线）；归属 `MapRenderer.ts`（engineering-lead）。
>     - **为什么主句必须是"双色"而不是"整条换亮色"**（`ux-ia` 指出 · 我按**绑构建读数**确认）：**同一相位内"极亮底"与"极暗底"同时存在** ⇒ **整条换成任何单一颜色，必把其中一类留在失败侧**。实测（见 §4.6 状态行）：单条暗墨在**亮底过**（沙/土 **8.98**）、**暗底败**（草·深 **2.33** / 暗 1.16 / 夜 **2.08**）⇒ **"整条换亮色"只会把失败从暗底挪到亮底**（= team-lead 判 (i)「换极性」为伪修法的同一逻辑）。⇒ **必须双向。**
>     - **「相位感知」降为可选增益**（按相位微调亮芯亮度或可让某侧更稳），**不靠它救夜读**。
>   - **(a-2) 让 ④ 免于 `applyLighting` 的 multiply 【原写「不推荐」—— ⛔ 已被 team-lead `e0885e1` 取代：现为该问题的**优先候选 (1)**，见上方「修法优先级正式定序」】**：④ 画在**世界坐标**、光照是**屏幕空间**且在**实体循环之后**（源码注释已写明）⇒ 搬到光照之后会**既错坐标、又盖住单位** —— **这两条仍是必须解掉的实现要求**（现由工程侧负责解并须验证），**不再是"不推荐"的理由**。
>   - **(a-3) 不修、如实记 FAIL**：`#15 光照可关` 仅作**缓解**，**不是** 1.4.11 的合规替代（1.4.11 没有「关掉功能即合规」的机制）。
>
> **(b) 「全色相」是否构成 1.4.1 失败？墨线的「边界形状」够不够当非色兜底？ —— 不构成失败；(β)/(γ) 都不需要。**
> - 现编码**不是「全色相」**，它有**两条非色腿**：
>   1. **形式腿（面 vs 边）**：可达 = 整格**面**水洗（`REACH_TINT`）；不可达 = 只在**邻接可达区那一侧**画**线**（`NOGO_EDGE` + `NOGO_EDGE_INK`，`EDGE_W`）。灰度下「一片被洗过的**面**」与「一条**暗边**」是**结构差**，与色相无关。
>   2. **明度腿**：外侧墨 `#2a1a12` **不透明** ⇒ 自带明度分量（= (α) 的贡献）。
> - ⇒ 「纯色相线索、灰度 1.1–1.4」这句**只对「水洗」那一半成立** —— **这正是 (α) 把明度锚改打在「边」上、而不再加在「填」上的原因**。把「水洗灰度低」读成「整个编码色相唯一」，是**把半个线索当了全部**。
> - ⇒ **1.4.1（非色彩）满足**（形式腿**相位不变**）。**不要 (β)**（可达侧也描格缘：格缘本身无亮度分量，与 (a) 同病，**解决不了夜读**）；**不要 (γ)**（点纹 / cost 数：最稳但**改管线**、伤观感，**留备选** —— 仅当 (a-1) 仍推不到 3:1 才上）。
>   **成本**：(β) ≈ **0 但无效**；(γ) ≈ **高**（图集点纹 + 逐格覆盖 pass + 读 `cost`；跨 `atlas.ts` / `MapRenderer.ts`；需冻结 + 复测）。
> - **边界（诚实）**：明度腿的**强度**受 (a) 制约 —— 夜间 1.56:1 ⇒ 该腿夜间变弱。故 **(b) 的「够」以 (a) 的处置落地为前提**：**1.4.1（结构性）现在就满足**；**1.4.11（定量）待 (a) 处置**。
>
> **⚠️ 口径警示（判定方必写，防「并排即同源」）**：本轮流转的 **1.56 / 2.41 / 3.46:1** 与 **ΔL\* 16.8** 系 **team-lead 转述**；我在**仓库与临时产物里未定位到其「绑构建」记录**，且并行实例的两次 `tintab` 运行**墨族 `ink×0`（一次都没画到、非零退出）**、其中一次的 `NOGO_EDGE_INK` 被解析为 `rgba(0,0,0,0)`（= 正在跑 **ink-off 负测**的中间态）。
> ⇒ **上面两裁定只依赖「判据本身」，不依赖该数**；**该数在「G1/④ owner 于冻结窗口内按 A/B 口径复算并入库」之前，不作判据、不入验收**。
> - **✅ 复算已做（2026-09-21 · `art-director` 生产方）**：绑构建 `68abf37`→`3e8084f`、真机视口、A/B 口径 ⇒ **2.93（午）/ 2.08（夜）**（见上方状态行）。**转述的 1.56 / 2.41 / 3.46 仍不入验收**（未定位到绑构建出处）；**以本节 + `shots/README.md` 的绑构建数为准**（仍受「阈值须与所守护代码同构建」约束：④ 代码再改须重测）。
> - **✅ `tintab.mjs` 墨族 gap —— 已修（`6867cff`）**：原解析器只认 `'rgba(...)'`、会**静默漏掉 `#hex` 的墨**（`NOGO_EDGE_INK = '#2a1a12'`）⇒ `none`/`edge` 都漏不掉墨 ⇒ 净贡献是对**含墨的底**算的 ⇒ **系统性错归因**（与上面「被解析为 `rgba(0,0,0,0)`」同源）。**owner `engineering-lead-2` 已修**（补 hex / 不透明色解析 + `src≡dist` 前置校验 + 0 命中硬守卫）。
>   **修复后实跑交会**（桌面 1280×757 emulation；墨 vs 底）：uniform-grass **正午 3.40 / 夜 2.58**、sand **6.15 / 3.82** ⇒ **与 `shots/` 的 A/B 口径（2.93 / 2.08）同向、同判 FAIL（夜不过）**。**口径分工（`4bd1851` 登记 → **2026-09-21 起以契约第 6 条为准**）**：**逐类 × 逐带的验收数（中位 WCAG）= `tintab.mjs`**；**全图 A/B 旁证腿 + 「`(α)` 基线可比列（墨单带）」= `reachmeas.mjs`**。**⛔ 取代横幅 —— 本行此前写的「`tools/reachmeas.mjs` … = ④ 验收数唯一出处」已被 team-lead 裁定（逐类×逐带 → `tintab`）取代**，且它与本节上方新块的「来源分工」**自相矛盾** ⇒ **现一次性收口、两张表都保留（不静默替换）**。⛔ 另：`reachmeas` 逐类那列人口 = `±3` 墨核 ⇒ **结构上量不到亮芯**，按第 6 条**只作 `(α)` 基线可比列，不作 (ii) 判据**。
>   **⛔ `imagecmp.mjs` 的 `CMP_EDGE=<hex>` 已撤**（`f740ec3` 加 · `bf92be4` 裁撤）：它量该色**整图所有出现**、**不是**只量 ④ 格缘 ⇒ **当不了验收数** ⇒ 既然当不了，就**不留第二把尺**（**撤的是代码；下表那行数留痕备查、工具不再提供**）。
> - **🔖 每个数必须自带 `population`（`ux-ia` 报「3.40 与 2.93 正午相反」后自行复现并撤回 ⇒ 写成硬性口径）**：**同一个量（墨描边 vs 其底）、两个 population，后者低是应该的、不是矛盾**：
>   | 数 | population（必写） | 来源 |
>   |---|---|---|
>   | 正午 **3.40** / 夜 2.58 | **uniform-grass**（受控图：全草、无物件） | `tintab.mjs` |
>   | 正午 **2.93** / 夜 **2.08** | **真机试玩图**（全地形 + 物件；population = **全部变化像素**） | `reachmeas.mjs`（`shots/` A/B 口径，**本节权威数**） |
>   | 正午 **1.29** | 真机试玩图；population = **全部 `ink0` 像素**（含物件描边） | `imagecmp.mjs CMP_EDGE=2a1a12` **【该模式已撤 · `bf92be4`】** —— 数留痕备查 |
>   ⇒ **不许把任一数脱离 population 引用**；三数**都判 FAIL（夜不过）**，结论一致。

---

## 5. 验收口径

**可机检（自动化，进既有探针批）**
1. **UI 壳**对比度：前景 / 背景对 ≥ 4.5:1、非文本（图标 / 边框 / 焦点）≥ 3:1（抽查木框 + 羊皮纸配色，`cartoon-style.md §7` 已提示可能不足）。**地图覆盖层**：**可达性「边界描边」套此 3:1**（WCAG 1.4.11，量「描边 vs 其相邻色」）；**蓝填充是冗余水洗、不单独套**（见 §4.6 专段）。
2. **无 emoji 当图标**：`rg "🔇|🔊|🌗|☀️" src/` 应为 0（G-17 防回归）；图标**只来自** `atlas.url()`。
3. **每个 `.icon` / 图标按钮有可访问名**：`rg 'class="[^"]*icon'` 的每处须有 `aria-label` 或相邻文字。
4. **焦点环厚度**：`focus-visible` 规则存在且 `outline-width ≥ 2px`。
5. **减少动效**：存在 `@media (prefers-reduced-motion: reduce)` 块。
6. **无全局 `outline: none`**（除已声明的受控例外）。

**手检（真机走查）**
7. 键盘 / TalkBack 走查：顶栏（图标 + 数字）、拇指带（文字标签）、setup（音效 / 光照开关）—— 逐项播报**有意义**且**不重复**。
8. 焦点环截图（≥ 2px、金对棕 ≈ 3.5:1）。
9. 文本缩放 200%：不裁切、不重叠。
10. 色盲模拟（灰度 + 红绿）：4 族可辨、队伍可辨；**地图可达染色两态在灰度下可辨**（可达 vs 不可达 ≥ 3:1，**判据全天候含夜、不随相位放宽**，判据与现状见 §4.6 专段 + 裁定 (a)/(b)）。

---

## 6. 与其它文档的接口（避免重复重申）

| 来源 | 内容 | 本文档角色 |
|---|---|---|
| `cartoon-style.md §7` | 色板硬约束（灰度 / 色盲 / 对比 / 闪烁） | **本文档 §4.6 收编**；§7 保留为「色板视角」 |
| `design/ux/in-game-ia.md` §4.4 / §5 / **Q11** | 图标非唯一语义载体；尺寸 44/48/40；缺 a11y 基线 | **Q11 由本文档闭合**；IA 规格改为**引用本文档分级** |
| `asset-spec.md §2.3.1` | `ic_sound_on/off`、`ic_light_on/off` 帧名 | §4.2 引用的图标来源 |
| `homm-web/src/style.css` | 焦点 / 触控 / 动效实现 | **单写者 = engineering-lead**；本文档只提**要求与判据**，不写实现 |
| `design/audio-direction.md` | 音频 / 字幕 | #13 的字幕项**已对齐（音频线 = `audio-lead`）**：结论「**否 / N/A**」，依据 `audio-direction.md §14` |

---

## 7. 决策记录（A-1 ~ A-4 · **已拍板**）

| # | 问题 | 选项 | **裁决（team-lead / 用户）** |
|---|---|---|---|
| **A-1** | 目标档定哪一档？ | (a) L2 Standard；(b) L3 Comprehensive | ✅ **(a) L2 + 2 项 AAA**（焦点外观 2.4.11 / 触控目标 2.5.5）。理由加强：那 2 项 AAA 是本项目**已实测暴露**的，不是按规范大全凑的 |
| **A-2** | 色盲支持 = 可切换开关 还是 设计层冗余？ | (a) 开关；(b) 冗余 | ✅ **(b) 设计层冗余**（形状母题 + 明度签名 + 数值本就双通道）。加开关会引入第三种配色状态、成本更高 |
| **A-3** | 字幕（#13）是否要做？ | (a) 全套；(b) 仅关键音；(c) 不做 | ✅ **(c) 不做 = N/A** —— 音频线已回：**全 11 个音效 / 12 个触发点都已有同步视觉反馈，缺陷清单为空** ⇒ 字幕**不必要**（**明确 N/A，不是「未满足」**）。依据 = `design/audio-direction.md §14`（音频线 = `audio-lead`） |
| **A-4** | 验收探针并入既有探针批 还是 另开？ | 并入 / 另开 | ✅ **并入** engineering-lead 既有探针批（D-42 批）。§5 的可机检项与「顶栏纵向守卫（`max(child.offsetHeight) ≤ topbar.offsetHeight`）」**同批**，不另开 |

> **A-3 的「L2 却 N/A」不矛盾**：#13 在 §2 矩阵里属 L2 档（基线**包含**字幕项），但本项目经音频线证实**不适用** → 标 **N/A**。**档位是基线，N/A 是经证实的不适用**，两者不冲突。

---

## 8. 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| — | — | ⚠️ **编号说明（2026-09-21 起）**：本表由**多个同名实例并发写入** ⇒ **撞号已发生三次**（`V1.18`×2、`V1.19` 亦曾被两队各用一次）。⇒ **编号只作"话题索引"，不保证唯一** ⇒ **引用时请带"行主题 + 提交号"**（如「`V1.18a` `#145` 收口 · `0e6fc6f`」）。**撞号行一律"加后缀 / 改号 **加行首注**"，内容不静默改写。** |
| **V1** | 2026-09-19 | 首版（D-39 交付）：建立 L1–L4 分级矩阵；目标档 L2 + 2 项 AAA；收编 6 项已发现实项（焦点 / emoji / 图标语义 / 触控 / 动效 / 色盲）；定义验收口径与文档接口 |
| **V1（定稿）** | 2026-09-19 | **A-1~A-4 拍板**（见 §7）：A-1 目标档 **L2 + 2 项 AAA**；A-2 色盲 **设计层冗余**；A-3 字幕 **N/A**（依据 `audio-direction.md §14`，音频线 = `audio-lead`）；A-4 探针**并入** D-42 批。状态：建议稿 → **定稿**。**归属更正**：音频项一律写「音频线 / `audio-lead`」（`audio-director` 在本环境工具表失效） |
| **V1.1** | 2026-09-21 | **§2 #2 的 3:1 范围写死 = UI 壳**（原只写"抽查木框+羊皮纸"，未声明是否含地图覆盖层 → 正是本轮 scope 争议根因）。**新增 §4.6「④ 地图可达染色」专段**：归 **§2 #10（1.4.1）**、**不归 #2**；判据 = **两态灰度下 ≥3:1**（非「vs 无界地形 3:1」）；现状 **FAIL**（灰度 1.25–1.44:1）；处方 **(α) 边界加 `ink0` 亮度锚**。同步改 §5 #1（写死 UI 壳）+ #10（增地图两态灰度可辨）。触发：`engineering-lead-2` 真 Chrome 实测 + 问 scope |
| **V1.2** | 2026-09-21 | **§4.6 专段的量化细化**（`engineering-lead-2` 补测）：① 记**两个口径** —— **A/B 口径**（同像素同地形、只差染色层）**中位 1.15(午)/1.12(夜)、≥3:1 像素 0.0%**、染色占屏 19.4%/19.3%；**空间口径** 被染蓝 vs 未染底 **1.74/1.53**、红缘 vs 未染底 **1.18/1.12**、min **1.00**；② **写死量化锚 = A/B 口径**（去掉"被染区恰好是亮地形"的混淆）；③ 结论 **FAIL 不变**，**红缘为最弱一环** ⇒ 进一步支撑 (α)「锚打在**边**上」。触发：`engineering-lead-2` 补齐数并接受 scope 裁定 |
| **V1.3** | 2026-09-21 | **team-lead 裁定取 (α) 并落地**：§4.6 处方改为「已落」——`MapRenderer.ts` 的 `NOGO_EDGE_INK`/`NOGO_EDGE`/`EDGE_W`：**内侧红（色相锚）+ 外侧不透明墨色 `ink0`（明度锚）**，把"靠色相"改成"靠明度"；**未采纳 (β)/(γ)** 及理由入档。**状态 ✅ 已落**；数值复测按 **A/B 口径**、真机视口、**绑构建指纹**，**入 `shots/`**（若仍未过 3:1 → 如实记，不换尺子）。触发：team-lead 裁定 + `art-director` 落地 |
| **V1.4** | 2026-09-21 | **SC 归属归一**（`ux-ia` 报跨文档冲突 · `68abf37`）：§4.6 原写「归 #10、**不归 #2**」，与 team-lead 裁决（尺子 = **WCAG 非文本 3:1**；可达染色是**信息承载 UI 元素**、不是画面 · `in-game-ia.md §15.4` / `6bb923c`）**冲突** ⇒ 改为「**归 §2 #2（1.4.11）+ §2 #10（1.4.1）**」，并写清 **3:1 量的是「边界描边 vs 其相邻色」、不是「填充 vs 无界地形」**（保留原写法的正确内核：填充是冗余水洗、不单独套）。§4.6 表行 + §5 #1 同步。另并入 `engineering-lead-2` 直连复算（蓝 **1.14** / 红缘 **1.05**，**同量级、口径略异**）作对照，**写明本节为全仓唯一权威数**（`§15.4` 已指向本处）。触发：`ux-ia` 报冲突 + 请归一 |
| **V1.5** | 2026-09-21 | **落两条 a11y 裁定**（应 team-lead 请裁）：**(a) 夜间仍以 WCAG 3:1 为判据** —— 不豁免、不降尺（1.4.11 无夜间豁免；降尺 = `bd4ebb0` 禁的「调到达标」；`ΔL* 16.8` 是感知旁证、**不能顶替**规范判据）⇒ **夜间 1.56:1 ⇒ 夜读 FAIL 如实成立**；处置 **推荐 (a-1) 相位感知描边**（(a-2) 免 multiply 有「错坐标/盖单位」坑、(a-3) `#15` 仅缓解非合规）。**(b) 不构成 1.4.1 失败、(β)/(γ) 都不需要** —— 编码有**两条非色腿**（**面 vs 边** 形式差 + 墨线**明度**差），「纯色相」只对「水洗」那一半成立。并加**口径警示**：流转的 1.56/2.41/3.46 + 16.8 **未绑构建、出处未入库** ⇒ **不作判据**，待冻结窗口复算。§5 #10 同步。触发：team-lead 请裁（两条列为「唯一未交项」） |
| **V1.6** | 2026-09-21 | **（1）收口 `ux-ia` 报的两处张力**：**(a-1) 归一命名 = 「双色 halo（亮芯 + 暗边）」**（原「相位感知描边」= team-lead 的 (ii)，同一件事，勿再当两套处方）；**「不叠下一层」的对象写死** —— 禁的是「**为凑标定值叠加的装饰性色层**」，**双色 halo 不在禁列**（= 同一「边」语义载体在两个底色方向各一条，1.4.11 本就要求对**两侧**相邻色各 ≥3:1）；**(a-1) 主句改为「双色 halo」**、「相位感知」降为可选增益 —— 依据 = **同一相位内极亮底与极暗底并存 ⇒ 整条换单一颜色必留一类失败**（`ux-ia` 按 WCAG 公式指出 · 我按读数确认）。**(2) 落绑构建复算读数**：`68abf37→3e8084f`、真机视口、A/B 口径 ⇒ **WCAG 中位 2.93（午）/ 2.08（夜）**、≥3:1 **44.6% / 25.3%** ⇒ **两相位均未过**（§4.6 状态行 + `shots/README.md`）；**转述的 1.56/2.41/3.46 仍不入验收**。**(3) `tintab.mjs` 墨族 gap** —— 复现后已由 owner `engineering-lead-2` 修（`6867cff`）；修复后实跑交会：墨 vs 底 uniform-grass **3.40（午）/ 2.58（夜）**、sand **6.15 / 3.82** ⇒ **与本节 A/B 口径同向同判**。触发：`ux-ia` 请收口 + ④ owner 复算 |
| **V1.7** | 2026-09-21 | **撤 `imagecmp CMP_EDGE`（team-lead 裁「一个验收数一处产」· 加于 `f740ec3`、撤于 `bf92be4`）**：该模式量该色**整图所有出现**（物件描边也是 `ink0`）、**不是**只量 ④ 格缘 ⇒ **当不了 §4.6 验收数** ⇒ **撤代码、不留第二把尺**。**④ 空间口径验收数唯一出处 = `tools/reachmeas.mjs`** ⛔ **该分工已被 V1.11（契约第 6 条）取代** —— **逐类×逐带 → `tintab`；`reachmeas` = 全图 A/B 旁证腿 + `(α)` 墨单带基线列**（**留痕不删**）；`tintab.mjs`（净贡献）为唯一源旁证。§4.6 分工句 + population 表那行（正午 1.29）同步加「**该模式已撤**」注 —— **数留痕、工具不再提供**（加口径、不重写）。触发：team-lead 裁 + `art-director` 执行 |
| **V1.8** | 2026-09-21 | **钉死 ④ 验收契约（team-lead 裁定）+ 口径措辞更正**：**(1) 契约三条写死** —— ① **口径 = A/B「状态 vs 其底」**（`reachmeas.mjs`），**「空间口径」已弃**（被"被染格恰好是亮地形"污染，仅存历史）；② **`population` 按地形逐类**（沙土/草亮/草中/草深/水/暗缝）**不合并**；③ **决策 = ★ 每类都必须 ≥3:1**（不是"全图统一 `population` 的中位"）—— 理由：`1.4.11` 管"能不能识别出那个信息"，玩家站在草深上读不出边界即在彼处不可识别，**不能靠多数地形把那一类洗掉**；且比"统一中位"**更抗操纵**。**(2) 判词随之改写**：`(α)` **FAIL 依因 = 「草·深 2.33/2.43 ❌ + 暗缝 1.16/1.14 ❌」两类不过**（非"全局中位不过"）；`(ii)` 验收目标 = **每类都 ≥3:1**。**(3) 防误引**：逐类小数出自**未入库 ad-hoc 脚本**（`/tmp/alpha_bin.mjs`，桶定义异于 `tintab`）⇒ **待 `#124` 逐类复算锁定**，此前只引判词不引小数。**⛔ 本句「未入库 ⇒ 不得引小数」已被 V1.10 更正**（旧表**可复现**、确系**相位帧**；作废理由是**契约第 4 条**，非"未入库"）。**(4) `3f11752` 读法更正**：不是"换尺"，是"**判据须说清回答什么问题、两方法的数不能共用**"（team-lead 已按其更正窄表述）。触发：team-lead 钉契约 + `art-director` 改措辞 |
| **V1.9** | 2026-09-21 | **契约加第 4、5 条 + ④ 逐类表按身份帧重录（`f35b34f` 不静默替换）**：**(1) 加第 4 条「分类帧 = 身份帧（正午 `none`）」** —— 「地形」是**身份**；用**同相位**分类会让夜间「草·亮」`L*<55` 塌成 n≈0 ⇒ 契约第 3 条被"**该类在本相位不存在**"**真空通过** ⇒ 死区洗白。⇒ `reachmeas` **必带第 4 参**（`0fc66e6`）。**(2) 加第 5 条「每格必带 `population(n)`；`n<200` 不得判过、不计入通过」** —— 理由 = 血案：旧表夜间「草·亮 `5.43` / 100%」实际 **`n=5`**。**(3) 重录状态行**：新表逐格带 `n`（**夜间 6 类里 5 类不过，只沙/土过；正午 草·深/暗缝 不过**）⇒ `(α)` FAIL，**死格不止两个**；旧表挂 **⛔ 取代横幅**保留作史（`0fc66e6` 逐字复现 ⇒ **口径确证 = 相位帧**）。**(4) 记两个未锁边界**（`#124` 前置）：草·深 样本薄（午 n=100/夜 n=90）；跨相位分类帧需 noon/night **同页同相机**（现仅间接旁证）。触发：team-lead 裁「现在就重录」+ `art-director` 执行 |
| **V1.10** | 2026-09-21 | **更正我（`art-director`）引入的两处错说**：本人此前说过 ①「旧表 `草·深 夜 1.42` 是跨相位错桶产物、**不可复现**」、②「**未入库**口径不得引用」——**两处都错**。**机制（`494e95b` 三腿互校，`art-director` 用数据钉死）**：`0fc66e6` 已把旧表**逐字复现** ⇒ **可复现**（正午身份帧 ≡ 旧正午列 / 夜间**相位帧** ≡ 旧夜列 / 夜间身份帧 = 草三行**错开一格**）⇒ 旧表**确系相位帧**所作，**与"未入库"无关**。**(1)** §4.6 状态行「防误引」句改写：作废的是**夜列**、理由是**契约第 4 条（分类帧）**；**旧表正午列 = 身份帧正午列**（正午 ≡ 身份帧）。**(2)** `shots/README.md` 同句同步改写。**(3)** V1.8(3) 的「未入库 ⇒ 只引判词不引小数」加 **⛔ 更正标**（不静默替换）。触发：`494e95b` 定位机制后我自请更正 |
| **V1.11** | 2026-09-21 | **契约加第 6 条 + ④ 来源分工一次性收口 + `(ii)` 读数入档**（team-lead 裁定 `12d99d1`+`e4686cf`；`art-director` 执行）。**(1) 加第 6 条**（本文档 §4.6 上方）：**④ 判据 = 每一类 × 每一相位，边带族中至少一条带 ≥3:1**，**逐族出数**；**禁用「混合 population 的中位」**（按构造杀 halo：沙/土 正午混合 `1.10` vs 墨 `7.43`）；附加四条（**population = 类 × 带**、draw-time 族标记、**不用颜色窗** ／ **四带中位取最大**但**四带全列** ／ **守恒断言** ／ **`n<200` ⇒ 未判、对称**）；**另禁用 `isInk` 单选**（结构上看不到亮芯）。**(2) 来源分工（取代 V1.7/V1.8 的「`reachmeas` = 唯一出处」）**：**逐类 × 逐带（中位 WCAG）→ `tintab.mjs`；全图 A/B 旁证腿 + `(α)` 墨单带基线列 → `reachmeas.mjs`** —— **两处冲突句一次性收口、两表都保留**（§4.6 分工句 + V1.7 行挂 ⛔ 取代标）。**(3) 守恒断言的实测读法**（`-2` `eefa9bb`：字面 `Σ == 右端` 因四带 1px 交界重叠而跑不过；**正确读法 = 覆盖形，`缺源(全族变·无单族) == 0` 才算过**，`多源` 作重叠量如实报）—— **读法待 team-lead 认**。**(4) `(ii)` 逐类 × 逐相位读数入档**（`tintab` 冻结版 + 身份帧，`i2_` 造图进行中）：**正午全过；FAIL = 晨昏岩 `2.83` / 夜 草·中 `2.94` / 夜 暗 `2.77` / 夜 岩 `2.36`（四格 `n≥200`）** ⇒ `(α)` 的「草·深」死格被亮芯救活、**死格换位**为「岩 / 夜草·中 / 夜暗」。**(5) `#128` 观感**（①②③⑥ PASS / ④⑤ FAIL）+ **多档观感**（halo 几何档无关、low 档只是清晰度低 —— 无新增失败）。**(6) 记阻塞**：**「通道独立腿」跑不了**（`tintab` 无 `--vw`/`--dpr`）⇒ 已请 owner 加，**不拿别的通道冒充**。触发：team-lead 四条裁定 + `-2` 表定版 |
| **V1.12** | 2026-09-21 | **闭合性核验补像素腿 + `#138` 同构建 A/B（`?devtint=0`）第一跑**（`art-director` 执行）。**(1) 轮廓缺口：从"只有代码腿"升级为"代码腿 + 像素腿"** —— 一次性探针 `homm-web/tools/closureprobe.mjs` 在同构建（`dist-139`）迷雾版正午 A/B 上测得：雾像素 4 邻接"被染色层碰过"的像素 **n=1020，其中 100% 邻接水洗、0 个邻接任意带** ⇒ **缺口确实存在于渲染结果里**；并写明该证明的**边界**（`fog` 含图外底色、同色不可分 ⇒ 只证"存在"、不证"缺口占比"）。**(2) 行号引用纪律**：`MapRenderer.ts` 并行改写导致同一段代码两次读落在不同行号 ⇒ 本节此后**按符号引用**（`inRegion`/`computePaths`），不引行号。**(3) `#138` 读数**：同构建同会话同帧只差 `&devtint=0` ⇒ 四族齐全；**亮芯族夜间 `19 140 (2.6%) → 1 562 (0.2%)`（塌 ~12×）**、归族残差 `8.77 → 119.59` ⇒ **「halo 在光照乘之前」的第二个独立旁证**（空间塌缩，非仅比值下降）；**但 fill 占 88% ⇒ 不得当逐类验收数**（逐类仍归 `tintab`）；副产物 = 该 A/B 下 `reachmeas` 逐类列**读到水洗**（沙/土 n=26986 / 中位 1.02）⇒ 印证该列"A/B 相关、只作 `(α)` 基线列"。**(4) 诚实边界**：该 A/B 所绑的构建目录 `dist-139/` **取数后被删除**（现只剩 `dist`）⇒ 指纹有记录、**现已不可复核** ⇒ 该结论**以代码腿为准，像素腿作旁证**。**(5) 证据图未入库**（按裁定等 `#141` 同批）。触发：team-lead ② 要求 + `#139` 解锁 + `#138` 执行 |
| **V1.13** | 2026-09-21 | **判据正文一次定死（team-lead 合成两说）+ 三条文档收口**（`art-director` 执行；team-lead 指令「现在就做，不要再等」）。**(1) 判据加硬约束 ②′**：**可测量形** = 四带（红/亮芯/暗墨/蓝fill）**中位里的最大值 ≥3:1**；**★ 该最大值必须落在「亮芯」或「暗墨」上 —— 落红/蓝fill ⇒ 该格记「未判」并报告**。理由 = `(ii)` 的设计承诺只针对两个极点；最大值落红/蓝 = **设计意图没兑现、在靠"碰巧"过线**。team-lead 已核**现有 21 格最大值全落在亮芯或暗墨** ⇒ **不改任何判词、只封住未来的洞**。**(2) 口头形留痕不删**：`§4.6` 与 `shots/README` 里「两极其一 ≥3:1」原句**挂 ⚠️ 注**（"不可直接测 ⇒ 可执行形见下一条"）⇒ 两行并存、**以上一条为准**（不静默替换）。**(3) `reachmeas.mjs` 头注补齐**：**「逐类列动画免疫、全图行不免疫」**（逐类列的谓词是**窄颜色窗** ⇒ 水波/脉冲那些小 Δ 动画像素**进不了窗**；全图行是**帧的函数** ⇒ 引用须写明"已冻帧"）+ 该列**正名**「`(α)` 基线定义 · 墨单带」（**结构上看不到亮芯** ⇒ 判 `(ii)` 用 `tintab` 逐类×逐带）+ 给 `i_reach_*` 那条标注**「跨构建对」**（`68abf37`↔`3e8084f` ⇒ 只说明谓词人口结构、**不作同构建 A/B 量级**）。**(4) `?devhalocomp` 状态入档 + team-lead 一条自我更正**：① **不得当 A/B 仪器**（层 A/B 用 `?devtint=0`、带隔离用 `tintab` 族开关）；② 工作树里有人已把它做成**预补偿候选开关**、team-lead 已放行该实验；③ **★ 硬伤（team-lead 报、`art-director` 读码复核成立）⇒ 已修**：该开关写在 `if (cost && this.tintEnabled) {` **守卫内部**（md5 `9b79241a`）⇒ **`?devtint=0` 会把整块跳过** ⇒ **取不到"染色关"侧的对照**；team-lead 列为**硬条件 (i)** 要工程侧修，**现行工作树已解耦**（md5 `aa703345`、未落库）⇒ **不再按"不可用于 A/B"处理**，但**硬条件 (ii)「缺省逐像素空操作」仍须证明**；④ **team-lead 更正自己的"天花板"论证**（**只对纯乘法成立** ⇒ 天花板由算子决定、必须实测）**+ 我对该更正的复核（结论相反，两段都留）**：**"变冷"不是反证**（纯乘法本身就会变冷变灰）；**墨的每通道比值吻合预测 ≤0.6 LSB**；**亮芯三通道一致偏亮 `+0.03~0.04` = 采样框进亮沙** ⇒ **「`applyLighting` 不是纯乘法」目前没有证据支持** ⇒ **⛔ 别拿它当"预补偿未必不可能"的理由；行不行靠原型实测**。**两条修法都放行、谁过谁上**（定序见 V1.14）。触发：team-lead 「#124 现在就做」+ ②③⑥ 三条裁定 |
| **V1.14** | 2026-09-21 | **修法优先级正式定序 —— 本文档 `(a-2)`「不推荐」被取代（team-lead `e0885e1`；`art-director` 落档）**。**(1) 定序**：**(1) 把三条带画在光照之后 = 优先**（**结构性、与 `applyLighting` 算子无关**，不依赖"反算得对不对"）；**(2) 预补偿（变体 (i)，`?devhalocomp=1`）= 并行候选**（**天花板由算子决定 ⇒ 必须实测**）。**两条判据相同**：**晨昏`岩 2.83` / 深夜`草·中 2.94` / 深夜`暗 2.77` / 深夜`岩 2.36` 四格转绿、且不弄坏已过的格** ⇒ **谁过谁上、不预判**。**(2) 取代处置（挂横幅、留痕不删）**：原 **(a-2)【不推荐】** 的两条理由（**错坐标** / **盖住单位**）**仍然有效，但性质从"否决"改为"必须解掉的实现要求"** —— 验证方式写死 = **与光照前版本 A/B，证明带的位置逐像素不偏、且未遮住任何单位**。**(3) 落地状态（我读码）**：工程侧已同时加两个**默认关**旗标 —— `?devhaloafter=1`（候选 (1)：边带只登记 `BandRect[]`、推迟到光照后落笔，默认路径与原实现逐像素一致）与 `?devhalocomp=1`（候选 (2)）；**均为未落库 WIP**，**是否可用须过各自空操作证明 + 四格判据**。**(4) 前置解除**：`MapRenderer.ts` 那段脏已认领（= 工程侧 `?devhalocomp` 原型，**非 `#141`**）、`dist` 重建前置解除；**但树仍含未落库 WIP ⇒ 任何读数必须写明"绑哪份构建/哪份工作树"**。触发：team-lead `e0885e1` + 我读码复核 |
| **V1.15** | 2026-09-21 | **team-lead `368f68f` 四条裁定入档（`art-director` 落）**。**(1) 天花板论证"放回台面"**：team-lead **撤回**其"不是纯乘法"，并据我的算术复核恢复原论证 —— **若 `applyLighting` 是纯乘法，预补偿有硬天花板**（亮极要夜里仍亮 ⇒ 须先除 `k` ⇒ 需通道值 `>255` ⇒ 不可能）⇒ **预补偿先验期望低、修法 (1) 仍优先**；**实测仍放行，但不得再拿"也许不是纯乘法"当理由**（两条修正**都留痕**，见 §4.6「三段都留」）。**(2) ⑤ 关闭**：`camera.ts:30 zoom = 2;` ⇒ **默认视角 = `devzoom=2`** ⇒ `#128` 覆盖的正是默认档；**并更正我的方向错读** —— `devzoom` 是**绝对 zoom 值**，**`1.6 < 2` 是缩小（更苛刻）**，要更有利须 `> 2`。**(3) 闭合缺口判「真缺陷 · 要修 · 排 `#141` 之后」**（理由 = 语义改动与渲染改动捆在一起会让验收不可归因）+ **新通用规矩：「可见性闸」与「可达性闸」口径不一致 ⇒ 交集处必出洞**（要么统一、要么当显式设计并测出来）。**(4) 新规矩「状态必须用可复现的定义表达」** = **`commit` + 构建命令（+ 可选指纹）**，**不能只写瞬时产物哈希**（起因 = 团队删 scratch `dist` ⇒ 我 `#138` 那组指纹已不可复核）⇒ 本节相关状态行补 `commit 6dd5631` 并如实标注"构建命令未记录"。触发：team-lead `368f68f` |
| **V1.16** | 2026-09-21 | **`#145`「通道独立腿」已跑 + 落档时测后复核（`art-director` 执行）**。**(1) 结果 = 佐证 5/7 类、两个死格拿不到数**：真机视口 **792×320@3** 与桌面 **1280×677** 并列（同 `dist`）—— **草·亮 / 草·深 / 沙·土 / 水 同向同判**（含唯一失败的草类格 **草·中 夜 `2.92 ❌`**，桌面 `2.94 ❌`）；**岩**：三相四带 `n` 全 `0`（0.5 仅 ink `1032`，桌面同相位 `5541`）⇒ **该类不在该视口可见范围、测不到**；**暗**：`(a)并集`/`(b)全族` 在 0.5 差 **21×**（`32607` vs `1548`）、0.22 差 11× ⇒ **守恒断言（契约第 6 条）不成立 ⇒ "全量"未定住 ⇒ 数不可用**（桌面无此病：`4431/4431`、`4428/4428`、`4426/4426`）⇒ **本腿不能替代桌面腿承担 `(ii)` 验收**。**(2) 用词纪律**：写**「通道独立」**（同工具、同族定义，只换视口与抓帧通道），**不写「独立复核」**；**换视口 = 换 population ⇒ 逐格数不与桌面比，只比方向/判定**（如 沙·土 0.5 相位 `6.07` vs `3.23` —— 不同 population，非矛盾）。**(3) 桌面腿兼作 `#124` 表的可复现性检查**：**7 类 × 3 相位 × 亮芯/暗墨 = 21 格逐格复现**（草·深 `6.79/6.06/4.74`、岩 `3.18/2.83/2.36`）⇒ **可复现性 ≠ 独立性**（真独立性由 `reachmeas` 墨单带列承担）。**(4) 测后复核（回答"这些数还算不算数"）**：`dist` `13d3b4200709` / `main` `ecbfa1e279f2` / mtime `2026-09-21T01:54:34Z` **逐字节未变** ⇒ **仍成立、可复核**；**HEAD 已由 `368f68f`→`36becdf`→今 `74157c8`** ⇒ 本读数**非当前 HEAD 的读数**（它绑 `dist/`、不绑 HEAD）；**该 `dist` 的构建命令未记录**（按 `368f68f` 规矩如实标注）。**(5) 把"当时不可复核的工作树"钉到 commit**：出数时 `src` sha1/12 `f4c71d970b4f`、mtime `02:21:51Z`（**比 dist 构建晚 26 分钟 ⇒ 当时 `src` 为 `M`**）—— 该内容**现已落库 = `commit ad4ffb4`**（已用 `git show ad4ffb4:… \| shasum -a 1` 复算确认）。**(6) ⚠️ 边界 `src ≢ dist`**：出数时 `dist` **不含** `?devtint`/`?devhalocomp`/`?devhaloafter`（grep `0/0/0`）、`src` 含（`4/5/6`）⇒ **引用须写「绑 `dist/` `13d3b4200709`」，不得写"绑 `src`"**；`tintab` 头部 `src!=dist: 已核一致` **只比 ④ 四常量**（四者 dist≡src，已独立 grep 复核）⇒ **通过 ≠ 同构**。**(7) 两条自检发现（给 `tintab` owner）**：① 同一次真机跑动内**画布 dpr 逐相位不同**（0.22 `1584×480 dpr2`；0.5 与 0.68 均 `2376×720 dpr3`）⇒ 跨相位比较不干净（0.22 是 1.5× 上采样；桌面腿三相恒 `1280×677 dpr1` 无此病）；② 头部「统计量」行仍写「**WCAG 取均值**」，与列头/裁定（**中位**）矛盾，易被误引。触发：`#145`（team-lead 定的通道独立腿）。**⛔ 其中 (4) 的尾句「逐字节未变 ⇒ 仍成立、可复核」已被 V1.17 取代**（该 `dist` 于 `10:28:50` 本地被**重建覆盖**、旧构建**仓内已无副本**）—— **留痕不删、只作史**；**(1)(2)(3)(5)(6)(7) 不受影响**。|
| **V1.17** | 2026-09-21 | **「构建已换」事件入档 —— V1.16 (4) 的「仍可复核」被推翻 + 两个候选旗标的状态更新（`art-director` 复核并上报 team-lead）**。**(1) 事件**：`dist/` 于 **`10:28:50`（本地）= `02:28:50Z`** 被**重建**，`android/app/src/main/assets/public/` 于 `10:29:15` **同步拷成新构建**；**旧构建被覆盖、且仓内已无副本** ⇒ `dist/render/MapRenderer.js` **`13d3b4200709` / md5 `962f3558`（mtime `09:54:34` 本地 = `01:54:34Z`）→ `1c718db31f6a` / md5 `76415ae9`（`44 077` bytes）**；`dist/main.js` **`ecbfa1e279f2` / md5 `d7b1f55f` → `64b123137afa` / md5 `ff653bce`（`66 013` bytes）**；`dist-el/` 与 `dist-139/` 一并消失（现只剩 `dist`）。⇒ 按 `a35f923`（同构建）+ `368f68f`（状态须用可复现定义）：**`#124` / `#145` 两张 `(ii)` 表与 `#138` 的 A/B 全部成为「已消失构建上的历史读数」** ⇒ **照原样留痕、不改数**；**要引作验收必须在现行构建上重跑**；**不得把新构建的数与旧数并列成"同一个量的两个数"**（**换构建 = 换参照**，与「换视口只比方向/判定」同一条纪律）。**(2) 旧副本我找过了（避免后人重复找）**：`android/app/build/intermediates/assets/release/mergeReleaseAssets/public/render/MapRenderer.js` = `219fa492631e`、mtime `07:20:34` ⇒ **更早的构建，不是被量过的那份** ⇒ **不可救**。**(3) 两个候选旗标状态更新（推翻 V1.14 (3) 的「未落库 WIP」）**：含两旗标的 `MapRenderer.ts` **已落库 `ad4ffb4`**（`10:27:07`；工作树 md5/8 `aa703345` / sha1/12 `f4c71d970b4f` **与落库版一致** ⇒ 内容未被改写）；**现行 `dist` 已含**它们（`?devhaloafter`/`?devhalocomp` 求值就在 `dist` 里）；**V1.16 (6) 那条 `src ≢ dist` 边界已消失**（旧 `dist` 不含三旗标**是构造上必然**：`?devtint` 落库 `10:12:02`、两候选旗标落库 `10:27:07`，均晚于旧 `dist` 的 `09:54:34` ⇒ **不是 grep 假阴性**）。**但「是否可用」仍未被证明** ⇒ 硬条件 (ii)「缺省逐像素空操作」+ 四格判据**仍未过**，**「默认关」≠「已证空操作」，不得互相顶替**。**(4) 流程提请（给 team-lead，待回）**：按 team-lead 自己的规矩（写 `dist/` 须先报备 + 确认；dev 构建用自有 `outDir`），本次 `dist` 写入**我未收到任何宣告** ⇒ **我的在途绑定被静默作废** —— **正是该规矩要防的失效模式** ⇒ 我已**停手不取数**（避免与可能正在进行的 ④ 原型测量撞车），**并请 team-lead 给线**：是否由我在现行构建上重跑两条腿 / 出那 4 格判据（原型变体 (i) 只量 4 格；**归属须先明确** —— `engineering-lead` 已声明两旗标非其所落）。**(5) 工具陷阱入档**：本环境 `grep` = **toybox 0.8.13（非 GNU）**，**不支持 `\|` 这种 BRE 交替** ⇒ `grep -c "A\|B" f` **恒返回 0**（假阴性）—— **今日我第一次核对时就被它骗出过一次"全 0"** ⇒ **规矩：核对旗标/常量一律用 `grep -E "A|B"` 或 Grep 工具，不用 `\|`**。触发：取数后 `dist` 被重建（复核 V1.16 (4) 时发现）。**⛔ 其中 (3) 的「硬条件 (ii) 仍未证明」已由 V1.18 更正为「已证」**（team-lead `git worktree`：`0 / 2 280 960` 像素、`maxd 0`）；**(4) 里"静默作废 / 未收到任何宣告"的定性也已由 V1.18 收敛**（roadmap 载有「重建窗口：开 · 一人重建一次并广播」）—— **留痕不删**。|
| **V1.18** | 2026-09-21 | **④ 两条修法实测定案 + 归属/判据纪律 + 对 V1.17 的两处自我更正（`art-director` 收口；依据 team-lead 的实测与裁定，见 `production/roadmap.md`）**。**(1) 修法 (2) 预补偿 = 判掉**：实测 `?devhalocomp=1` **只救暗极**（暗墨 `228 → 1 740` 命中），**亮芯仍 `0` 命中** ⇒ **天花板是结构性的**（"画在纯乘法之前"决定）⇒ **不采用、保留旗标、写明"被实测判掉的一条路"**。**(2) 修法 (1) `?devhaloafter=1` = 采用**：**三个锚色恢复到标称色**（亮芯 `0 → 56 016`、暗墨 `228 → 61 200`）⇒ 与算子无关的结构性修法 ⇒ **过验收后翻默认**。**(3) 判据纪律（采纳）**：**(1) 必须过同一套判据** —— **"锚色恢复"不证明"对比度过线"** ⇒ **"证据形态" ≠ "判据"**（④ 验收数仍按契约第 6 条）。**(4) 已知代价 + 我的待办**：修法 (1) 下**三条带落在实体之上**（旧版实体按 y 排序会遮挡）⇒ team-lead **接受为有意设计**，但要求**观感确认"一条线横穿一个单位"不造成误读** ⇒ **归我（与 `#128` 同尺）**，**状态：未做**（需含该旗标的构建 + 窗口）；**我不自行取数**。**(5) ★ 归属裁定**：**出数 = `engineering-lead-2`（`tintab` owner，避免"一个量两个生产者"）；判定 = `art-director`；`engineering-lead` 不出数** ⇒ **我此后不自行出 ④ 验收数**；`#145` 腿**降级为旁证（"有余力再做"，不阻塞任何人）**。**(6) ⛔ 两处自我更正（本条取代，均留痕不删）**：① **V1.17(3) 的「硬条件 (ii) 仍未证明」错** —— team-lead 已用 `git worktree` 建 HEAD 版对比本版证明**缺省逐像素空操作**（**`0 / 2 280 960` 像素、`maxd 0`**）⇒ 改为**「已证」**；② **V1.17(4) 的「静默作废 / 未收到任何宣告」定性过重** —— `production/roadmap.md` 载有「**`dist` 重建窗口：开**（前置 `ad4ffb4` + `74157c8` 均落库）⇒ **一人重建一次并广播**（commit + 构建命令 + 指纹）」，**且 team-lead 逐条复核了新指纹**（`main.js` `d7b1f55f → ff653bce`、`render/MapRenderer.js → 76415ae9`、`ui/StartScreen.js → 0a45aa51`，与我实测一致）⇒ **「窗口未被宣告」这个说法撤回**；**收敛为**：**该次广播未进我的收件箱**（请 team-lead 确认投递），**而实质后果不变** —— 旧构建已覆盖、两张表成为历史读数、须在现行构建重跑。**(7) 记入一条 team-lead 逮到的新失效模式**：门控**复用"碰巧在跑"的 `127.0.0.1:5173` 服务** ⇒ 静默量到真 `dist`（与 `src≠dist` 同族）⇒ **临时构建验证须给门控空闲 `PORT` + 头注打印"服务的目录 + 指纹"**。**(8) 与我的 `#145` 的关系**：该腿结论（佐证 5/7 类、岩 n=0、暗守恒差 21×）**不变**，但它**不是门、是旁证**；**引用时必须带"绑已消失构建 `13d3b4200709`"这一条**。触发：team-lead `ad4ffb4` 实测定案 + 我复核发现的两处过时 |
| **V1.18a** | 2026-09-21 | **（⛔ 原号误挂 `V1.18` —— 与并发实例撞号，现加后缀 `a`；**内容未改**，其原文见上一行同号者。本条 = `art-director` 的 `#145` 收口。）**`#145` 定性为「旁证」+ 我自请更正 H1 + 一处跨文档冲突并读（`art-director`）**。**(1) 现行裁定入档（team-lead，效力高于 V1.16）**：**`#150` 不再诊断** —— 捕获端**钉一个预热帧**（我已证「792 有预热即逐字节确定」），**若仍互换 ⇒ `dark` 记「tie 敏感、给区间」、只标重叠列**（**不影响 `缺源 == 0`**）；**`#145` 降为「旁证、不是门、有余力再做」，不阻塞任何人**。⇒ §4.6 该段定性同步为：**`#145` = 旁证 / 真机视口 `dark`·`rock` 记「未取」/ `(ii)` 验收仍走桌面 `1280×677` / 本段绑的是已消失构建、引作验收须重跑**。**(2) ⛔ 我自请更正 H1**（原"dpr 逐相位不稳 ⇒ 桶索引错位 ⇒ 最小类落空 + ink 暴涨"）：**作为唯一根因不成立** —— **0.5 与 0.68 同为 `dpr3`/`2376×720`，却一个破（`dark ink 1080 → 31455`）、一个全类成立** ⇒ dpr 解释不了 ⇒ **降为「待验子假说」**，**不得把「加 `--tier`」当已定根因的修法**（它只解「同一跑动内跨相位不干净」，而那条独立成立、桌面腿无此病）。**(3) 一处跨文档冲突 ⇒ 并读、不删**：本文档（V1.17(3) + §4.6 两处）写「硬条件 (ii)「缺省逐像素空操作」未证」，而 `production/roadmap.md` 同日记 **`ad4ffb4` 两条硬条件都满足**（证据 = `git worktree` HEAD 版对比本版 **`0 / 2 280 960` 像素、`maxd 0`**）⇒ **以 roadmap 为准**，前两句读作「落档当时未见到证据」；**仍待办的是「四格判据」**（四格转绿且不弄坏已过格）—— **「空操作已证」≠「四格转绿」，不得互相顶替**。**(4) 归属留痕**：我本轮的 §4.6 补记与 `shots/README.md` 镜像**未经我提交**，被并发实例的 `5339b8b` 一并扫入（同批含其 V1.17 行）⇒ **内容已在库、属既有「双写」家族**，如实记此、不追责。触发：team-lead 的 `#145`/`#150` 两条裁定 + 我复核 H1 时发现自己的反证 |
| **V1.19** | 2026-09-21 | **team-lead 逐条回复 ①—⑦ 入档（`art-director` 落）—— 含一条新规矩与一处 ⛔ 更正**。**(1) ★ 新规矩（具名，已入 `roadmap.md` `1565f85`）**：**窗口宣告与关键证明必须落在「广播通道」（`roadmap.md` / 契约文档），不能只发给被执行人** —— **别人找不到的宣告就不是宣告**（与"引用一份没发出去的清单"同族）。**(2) `10:28:50` 重建 = 批准的窗口**（team-lead 给 `engineering-lead` 的 GO；`engineering-lead` 确已广播）⇒ **我未收到 = 通道失效，非越权**；**两组"矛盾"指纹实为同一构建的两种算法**（我报 sha1/12 `1c718db31f6a` / `64b123137afa` ≡ 广播 md5 `76415ae9` / `ff653bce`，已逐位吻合）⇒ **我的处置（停手 / 不取数 / 不撞车 / 留痕不改数）被确认正确**。**(3) ⛔ 更正：硬条件 (ii)「缺省逐像素空操作」= 已满足**（`git worktree` HEAD 版 vs 本版 = **0 / 2 280 960 像素、maxd 0**）—— 我 §4.6 两处（`#139` 解耦行 / 契约块 ③）写"仍未证明 / 仍未判"**已就地更正**；**仍待判的只剩「四格判据」**。**(4) `#145` 处置**：**重跑两条腿 = 要，但排在 `-2` 的 A/B 之后、不撞车**（`-2` 在现行构建上出「默认 vs `?devhaloafter=1`」与「默认 vs `?devhalocomp=1`」）；**4 格由 `engineering-lead-2` 出数、我判**。**★ 限定词（team-lead 收下，须写进报告）**：本条腿**只能回答「统计量是否同判」，不等于「缺省逐像素空操作」**。**(5) 保持**：`#145` = **旁证、不是门、不能替代桌面腿承担 `(ii)` 验收**；用词「通道独立」；旧构建读数是「已消失构建上的历史读数」，**可重建、不是丢失**（状态 = `commit + 构建命令`），**但绝不与新数并列成"同一个量的两个数"**。**(6) 工具陷阱（team-lead 第三次同族确认）**：本机 `grep` = **toybox 0.8.13（非 GNU）**，`\|` 交替不支持 ⇒ **一律 `grep -E "A\|B"` 或 Grep 工具**。触发：team-lead ①—⑦ 逐条回复 |
| **V1.20** | 2026-09-21 | **（原号误用 `V1.19` —— 与并发实例撞号，现改 `V1.20`；**内容未改**，见上一行 V1.19 = team-lead 逐条回复 ①—⑦。）`#150` 根因实测定位并修复 ⇒ 修正本段「岩 / 暗」两条结论的「成因」（`art-director` 补记；根因由 `engineering-lead-2` 于 `82ad4b5` 给出）**。**(1) 根因（实测、非猜测）**：**①首帧未 settle** —— 同 mode 连抓三张，`#1` 与 `#2/#3` 差 `10w+` 像素、**`#2==#3` 逐位相同**、且**与 mode 无关** ⇒ 录制前丢弃首帧（与 G-15 同族）；**②启动 toast `#hint`**（`main.ts:604`，载入后 `1.5s` 出现于地图区下缘、792@3 bbox device `412×93`、`2s` 淡出）**落在录制窗内即污染**（曾把 `dark` 的 `(a)并集` 抬高 `31059`）⇒ 注入 `display:none` + **录制前核验 computed style**。**(2) 修复后实测**（792×320@3、0.22）：`dark`/`sand`/`grass-dark`/`water` **全部 `(a)==(b)`、`缺源==0`**、**`dark` Σ=`1548`**（此前 `32607/1548` 互换）⇒ **`#150` 已闭**；桌面 1280×757 **回归与基线逐格一致**。**(3) ★ 结论成因修正（防误引）**：「**暗不可用**」的成因**不是"该视口属性"、而是捕获端缺陷** ⇒ **不得再引「真机视口 = 不可用」**；「**岩 `n=0`**」**同样不再能归给视口**，但**是否同因仍未验**（修复验证只跑 0.22 单相位、未报 `rock`）⇒ **仍记「未判 / 未取」，成因留空（不猜）**。**(4) ⛔ 我的 H1（dpr）被实测证伪**（两条根因里没有 dpr）；**唯一仍独立成立**的是那条卫生项 —— 同一次跑动内画布 dpr 逐相位漂移（`dpr2` vs `dpr3`）⇒ 跨相位比较不干净、桌面腿无此病。**(5) 对 `#158` 的影响**：捕获端已修 ⇒ 真机腿的信息增益上升（此前"不能承担验收"的**理由已被抽掉**），**但仍等 team-lead 给窗口 + 归属**，不自行开跑。触发：`82ad4b5`（`#150` 修复）|
