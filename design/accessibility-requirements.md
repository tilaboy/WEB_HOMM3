# 可访问性要求（A11y Baseline）· V1

| 项 | 值 |
|---|---|
| 状态 | **定稿 V1**（2026-09-19；A-1~A-4 已拍板，见 §7） |
| 作者 | 林绘澄（美术 / 视觉 / **可访问性**） |
| 触发 | **D-39**（`production/roadmap.md`）：`cartoon-style.md §7` 早就声明「完整分级矩阵是**独立交付物** `design/accessibility-requirements.md`，等待主理人排期」，**却从未排期、文件至今不存在** → 规则只能在每份 UI 规格里各自重申（漂移风险）。本档落成分级矩阵并**收编已发现的实项** |
| 前置 | `cartoon-style.md §7`（色板硬约束）· `design/ux/in-game-ia.md` §4.4 / §5 / Q11 · `design/art-bible/asset-spec.md §2.3.1 / §7` |
| 目标平台 | OPPO PMA110 / Android 16 / DPR 3 / **CSS 视口 792×360 横屏**（手机、触屏、常无实体键盘） |

> ## 0. 一句话
> **本项目目标档 = `Standard`（L2），并额外拉入 2 项 AAA 级**（焦点外观 2.4.11、触控目标 2.5.5）—— 手机上这两项成本近乎为零、收益最大。
> **本文档是 a11y 的唯一权威基线**：任何 UI 规格（含 `in-game-ia.md`）**不得各自重申 a11y 规则**，只能**引用本文档的分级**（如「满足 L2 + 本文档 §4.1 焦点口径」）。
> **a11y 是基础要求，不是锦上添花。**

---

## 1. 分级定义（4 级）

| 档 | 名称 | 一句话 | 基线 |
|---|---|---|---|
| **L1** | **Basic** | 「能用」——不出现阻断性障碍 | WCAG 2.1 **A** 中与游戏相关的子集 |
| **L2** | **Standard** ✅ **本项目目标** | 「能用得舒服」——主流残障场景可**独立**完成核心循环（探索 + 经营） | WCAG 2.1 **AA** + 游戏适配（触控 / 色盲冗余 / 读屏标签 / 减少动效） |
| **L3** | **Comprehensive** | 「深度可配置」 | L2 + 选定 **AAA**（焦点外观 / 44px 目标 / 关键文本 7:1）+ 可重映射 + 字幕自定义 |
| **L4** | **Exemplary** | 「面向辅助技术深度集成」 | L3 + 全键位重映射 + 认知辅助 + 完全冗余提示 |

> **为什么要分级而不是「一档搞定」**：分级让**每份 UI 规格的验收可判定**（「这个按钮满足 L2 的 2.5.5 吗？」），也让**成本可预算**（每上一档要付什么）。没有分级的 a11y 只能变成一堆互不引用的散装规则 —— 这正是 D-39 要根治的病。

---

## 2. 特性矩阵（分级 × 特性）

`✅`=该档包含 · `＋`=该档新增 · `—`=不在该档范围。最后一列是**本项目**的取值。

| # | 特性 | 判据（WCAG） | L1 | L2 | L3 | L4 | **本项目** |
|---|---|---|---|---|---|---|---|
| 1 | 文字对比 ≥ 4.5:1（大文本 ≥ 3:1） | 1.4.3 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 2 | 非文本对比 ≥ 3:1（图标 / 边框 / 焦点） | 1.4.11 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 3 | **焦点可见** | 2.4.7 (AA) | ✅ | ✅ | ✅ | ✅ | **L2（现状部分缺，见 §4.1）** |
| 4 | **焦点外观 ≥ 2px 且 ≥ 3:1** | 2.4.11 (AAA) | — | — | ＋ | ✅ | **⚡ 额外拉入** |
| 5 | 触控目标 ≥ 24×24 | 2.5.8 (AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 6 | **触控目标 ≥ 44×44** | 2.5.5 (AAA) | — | — | ＋ | ✅ | **⚡ 额外拉入（本项目取 48 主 / 44 常规）** |
| 7 | 键盘可达 + 焦点顺序合理 | 2.1.1 / 2.4.3 (A/AA) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 8 | 名称-角色-值（图标 / 按钮有可读名） | 4.1.2 (AA) | ✅ | ✅ | ✅ | ✅ | **L2（现状全缺，见 §4.3）** |
| 9 | 标签即名称（可见文字 ∈ 可访问名） | 2.5.3 (A) | ✅ | ✅ | ✅ | ✅ | **L2** |
| 10 | 颜色非唯一手段（色盲 / 灰度可辨） | 1.4.1 (A) | ✅ | ✅ | ✅ | ✅ | **L2（见 §4.6）** |
| 11 | 文本缩放 ≥ 200% 不丢内容 | 1.4.4 (AA) | — | ✅ | ✅ | ✅ | **L2** |
| 12 | 减少动效 / 无高频闪烁 | 2.3.1 / 2.3.3 | ✅ | ✅ | ✅ | ✅ | **L2（现状缺，见 §4.5）** |
| 13 | 音频：字幕 / 文字大小 | 1.1.1 / 1.2.x | — | ✅ | ✅ | ✅ | **L2 档位；本项目 N/A**（音频线确认，见 §6 / §7 A-3） |
| 14 | 可重映射输入（键位 / 手势） | 2.5.1 / 2.1.4 | — | — | ＋ | ✅ | **L3+（暂缓）** |
| 15 | 昼夜光照可关（光敏 / 偏好） | — | — | ✅ | ✅ | ✅ | **L2（已有 `lightingOn()`）** |

> **注**：#4 与 #6 是**主动拉入的 AAA 项**（见 §3）。#14 暂缓（触屏优先、无复杂键位）。

---

## 3. 本项目目标档与「额外拉入」的理由

| 项 | 决策 | 理由 |
|---|---|---|
| **基线** | **L2 Standard** | 覆盖 ~8% 男性红绿色盲、老年低视力、触屏误触三大主要场景，成本可控 |
| **额外 #4 焦点外观 2px** | **拉入** | 现状**已有单点缺陷**（1px，见 §4.1）；统一 `:focus-visible` 是一次改、长期受益，成本 ≈ 0 |
| **额外 #6 触控 44/48** | **拉入** | 真机实测 **p90 = 23px** 落点散布（`in-game-ia.md §5.1`）→ 40px 宽按钮横向余量仅 1px；44/48 是**可用性刚需**，非锦上添花 |
| **暂缓 #14 可重映射** | 不进 L2 | 本作触屏优先、无复杂键位；待有键盘/手柄需求再评估 |

---

## 4. 已发现的实项（D-39 收编 · 逐项带代码证据）

> 证据口径：`git grep` / `rg` 于 `homm-web/src/`。**行号以 2026-09-19 工作区为准**，实现后可能漂移。

### 4.1 焦点可见性 —— 「对比达标、厚度不足」

| 项 | 内容 |
|---|---|
| **现状** | 全项目**只有一条** `:focus` 规则：`style.css:1456-1459` `.ss-field input[type='text']:focus { outline: none; border-color: var(--gold); }`。焦点指示 = 把默认 `1px` 边框从 `--frame` 换成 `--gold`，**厚度不变（1px）**，并**移除了** outline |
| **量化** | `--gold #f0d488` vs `--frame #8a6a34` → 对比 **≈ 3.5:1**（≥ 3:1 ✅）；厚度 **1px < 2px** ❌ |
| **判据** | **2.4.7（AA）本身满足**；不满足 **2.4.11（AAA 焦点外观）**。即「**对比达标、厚度不足**」 |
| **补充事实** | 60+ 按钮**没有自定义 focus 样式** → 现靠浏览器默认 focus ring；`outline:none` **只加在输入框**、**未全局清除** → 属「**未统一**」而非「被删光」 |
| **处置** | 加全局 `:focus-visible { outline: 2px solid var(--gold); outline-offset: 2px; }`（或与既有输入框规则统一，避免两套）。**归属工程**（`style.css` 单写者 = engineering-lead），时机 = UI 重构批 |

### 4.2 既有 emoji 的读屏播报不一致

| 项 | 内容 |
|---|---|
| **现状** | `HUD.ts:86` `🔇` / `🔊`（音效）；`HUD.ts:101` `🌗` / `☀️`（光照）。读屏（TalkBack）**播报各家不一**：有的念「静音 / 扬声器」，有的直接跳过，有的念错；`☀️` 含变体选择符 **U+FE0F**，部分平台渲染成缺字框 □ |
| **判据** | 违反 **4.1.2（名称-角色-值）**；且 emoji 全彩 / 抗锯齿 / 尺寸随 OS 变 → 违反 `cartoon-style.md §3.2 色板 / §1.1 描边 / §1.3 整数缩放`（= **G-17**） |
| **处置** | ① 换**图集精灵** `ic_sound_on/off`、`ic_light_on/off`（**已登记** `asset-spec.md §2.3.1`）；② 每个按钮补 `aria-label`（如 `aria-label="音效"`、`role="switch" aria-checked`）。时机 = UI 重构批（与 setup 搬迁同片 DOM）。**归属 = 美术出帧（已登记）+ 工程接线** |

### 4.3 图标不得为唯一语义载体（+ `aria-label`）

| 项 | 内容 |
|---|---|
| **现状** | 顶栏资源项是「**图标 + 数字**」（`HUD.ts:41` `icon.src = atlas.url(\`ic_${k}\`)`），**无 `aria-label`** → 读屏只念出一个孤零零的数字。**`src/` 全目录 `aria-label` 数量 = 0**（`rg` 证据） |
| **判据** | **4.1.2 / 2.5.3** |
| **处置** | ① 「图标 + 数字」项：`aria-label="<资源名> <值>"`（如 `aria-label="金币 12"`）；② 图标按钮**必须有文字标签或 `aria-label`**；③ 拇指带已是文字标签（`in-game-ia.md §4.4 / Q10`）→ **天然达标**，仍建议补 `aria-label` 覆盖缩写（如「日志2」→ `aria-label="事件日志，2 条未读"`）。时机 = UI 重构批 |

### 4.4 触控目标（vs 实测 p90 = 23px 落点散布）

| 项 | 内容 |
|---|---|
| **现状** | 真机实测：误触 0/8，但**落点偏差 p50 = 13px / p90 = 23px**。最小 `宝物▾` = **40×32**（半宽 20px，手指落 19px 处 → 横向余量 **1px**） |
| **判据** | **2.5.8（AA，24px）** / **2.5.5（AAA，44px）**。本项目取 **≥ 44（常规）/ ≥ 48（主操作）/ ≥ 40（密集区，且相邻按钮间距足够）**（与 `in-game-ia.md §5` 一致） |
| **处置** | 尺寸批已把 `.btn.tiny` **32 → 40**（commit `028ec4a`）；其余按 IA §5。⚠️ **判定口径**：`.btn.tiny.tap::after` 用负 offset 扩**点击热区**（仅垂直向）——扩的是**热区**不是**可见尺寸**；对 2.5.5 的判定**以热区为准时 OK、以可见为准时不足**，验收时须写明用哪一种 |

### 4.5 减少动效 / 无高频闪烁

| 项 | 内容 |
|---|---|
| **现状** | **`prefers-reduced-motion` 在 `src/` 中数量 = 0**（`rg` 证据）；现有动画均为 **≤ 4 帧循环**（低闪），**无高频闪烁** |
| **判据** | **2.3.1（三次闪烁）/ 2.3.3（交互动效）**；本项目动效均为低幅（±1px 整数位移 + `squash & stretch`） |
| **处置** | 补 `@media (prefers-reduced-motion: reduce)`：关掉 idle 浮动、缩短面板过渡（**不改整数位移本身**，那是可读性手段）。**归属工程** |

### 4.6 色盲 / 灰度（色板硬约束，引自 `cartoon-style.md §7`）

| 约束 | 值 | 归属 |
|---|---|---|
| 灰度可辨 | 4 族明度台阶相邻差 **≥ 12 L\***（= §2.4 明度签名） | 美术（色板） |
| 色盲可辨 | **禁止**靠 p2 红 / p3 绿的色相差传递**唯一**信息；每族同时具备**形状母题** | 美术（§5 母题是形状） |
| 战斗队伍辨识 | 队伍色环（`cbase_*` 蓝 / 红）对红绿色盲失效 → 建议**形状区分**（我方圆环 / 敌方方环或带缺口） | 美术 + 工程 |
| 光照可关 | `lightLayer.ts` 已有 `lightingOn()` ✅ | 工程（保留 + 设置面板暴露） |

---

## 5. 验收口径

**可机检（自动化，进既有探针批）**
1. 对比度：UI 前景 / 背景对 ≥ 4.5:1、非文本 ≥ 3:1（抽查木框 + 羊皮纸配色，`cartoon-style.md §7` 已提示可能不足）。
2. **无 emoji 当图标**：`rg "🔇|🔊|🌗|☀️" src/` 应为 0（G-17 防回归）；图标**只来自** `atlas.url()`。
3. **每个 `.icon` / 图标按钮有可访问名**：`rg 'class="[^"]*icon'` 的每处须有 `aria-label` 或相邻文字。
4. **焦点环厚度**：`focus-visible` 规则存在且 `outline-width ≥ 2px`。
5. **减少动效**：存在 `@media (prefers-reduced-motion: reduce)` 块。
6. **无全局 `outline: none`**（除已声明的受控例外）。

**手检（真机走查）**
7. 键盘 / TalkBack 走查：顶栏（图标 + 数字）、拇指带（文字标签）、setup（音效 / 光照开关）—— 逐项播报**有意义**且**不重复**。
8. 焦点环截图（≥ 2px、金对棕 ≈ 3.5:1）。
9. 文本缩放 200%：不裁切、不重叠。
10. 色盲模拟（灰度 + 红绿）：4 族可辨、队伍可辨。

---

## 6. 与其它文档的接口（避免重复重申）

| 来源 | 内容 | 本文档角色 |
|---|---|---|
| `cartoon-style.md §7` | 色板硬约束（灰度 / 色盲 / 对比 / 闪烁） | **本文档 §4.6 收编**；§7 保留为「色板视角」 |
| `design/ux/in-game-ia.md` §4.4 / §5 / **Q11** | 图标非唯一语义载体；尺寸 44/48/40；缺 a11y 基线 | **Q11 由本文档闭合**；IA 规格改为**引用本文档分级** |
| `asset-spec.md §2.3.1` | `ic_sound_on/off`、`ic_light_on/off` 帧名 | §4.2 引用的图标来源 |
| `homm-web/src/style.css` | 焦点 / 触控 / 动效实现 | **单写者 = engineering-lead**；本文档只提**要求与判据**，不写实现 |
| `design/audio-direction.md` | 音频 / 字幕 | #13 的字幕项**已对齐（音频线 = `audio-lead`）**：结论「**否 / N/A**」，依据 `audio-direction.md §14` |

---

## 7. 决策记录（A-1 ~ A-4 · **已拍板**）

| # | 问题 | 选项 | **裁决（team-lead / 用户）** |
|---|---|---|---|
| **A-1** | 目标档定哪一档？ | (a) L2 Standard；(b) L3 Comprehensive | ✅ **(a) L2 + 2 项 AAA**（焦点外观 2.4.11 / 触控目标 2.5.5）。理由加强：那 2 项 AAA 是本项目**已实测暴露**的，不是按规范大全凑的 |
| **A-2** | 色盲支持 = 可切换开关 还是 设计层冗余？ | (a) 开关；(b) 冗余 | ✅ **(b) 设计层冗余**（形状母题 + 明度签名 + 数值本就双通道）。加开关会引入第三种配色状态、成本更高 |
| **A-3** | 字幕（#13）是否要做？ | (a) 全套；(b) 仅关键音；(c) 不做 | ✅ **(c) 不做 = N/A** —— 音频线已回：**全 11 个音效 / 12 个触发点都已有同步视觉反馈，缺陷清单为空** ⇒ 字幕**不必要**（**明确 N/A，不是「未满足」**）。依据 = `design/audio-direction.md §14`（音频线 = `audio-lead`） |
| **A-4** | 验收探针并入既有探针批 还是 另开？ | 并入 / 另开 | ✅ **并入** engineering-lead 既有探针批（D-42 批）。§5 的可机检项与「顶栏纵向守卫（`max(child.offsetHeight) ≤ topbar.offsetHeight`）」**同批**，不另开 |

> **A-3 的「L2 却 N/A」不矛盾**：#13 在 §2 矩阵里属 L2 档（基线**包含**字幕项），但本项目经音频线证实**不适用** → 标 **N/A**。**档位是基线，N/A 是经证实的不适用**，两者不冲突。

---

## 8. 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| **V1** | 2026-09-19 | 首版（D-39 交付）：建立 L1–L4 分级矩阵；目标档 L2 + 2 项 AAA；收编 6 项已发现实项（焦点 / emoji / 图标语义 / 触控 / 动效 / 色盲）；定义验收口径与文档接口 |
| **V1（定稿）** | 2026-09-19 | **A-1~A-4 拍板**（见 §7）：A-1 目标档 **L2 + 2 项 AAA**；A-2 色盲 **设计层冗余**；A-3 字幕 **N/A**（依据 `audio-direction.md §14`，音频线 = `audio-lead`）；A-4 探针**并入** D-42 批。状态：建议稿 → **定稿**。**归属更正**：音频项一律写「音频线 / `audio-lead`」（`audio-director` 在本环境工具表失效） |
