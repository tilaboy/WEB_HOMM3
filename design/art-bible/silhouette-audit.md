# 剪影矩阵合规审计（§5.6.1 / §5.6.2 / §5.6.3）

- **日期**：2026-09-20
- **审计**：主理人（代裁）
- **对象**：B0 + B1 + B2 已交付的 **16 单位 / 48 帧**（4 族 × T1–T4）
- **触发**：B2 四族齐后，按 `asset-spec.md` R-1 的止损条款（「B1 之后立刻做 20 格盲测」）回头做横向可辨核查。
- **状态**：**cu 通道已于 `9a108a1` 收口（见下方「结案状态」）；map 通道另有新冲突待裁（D-72）。**

---

## 结案状态（2026-09-20 更新）

| 通道 | 状态 | 证据 |
|---|---|---|
| **cu 帧（44×56）** | ✅ **已收口**（`9a108a1` / D-71） | 四族一致 **60.7 / 69.6 / 76.8 / 87.5**，步长 **+8.9/+7.2/+10.7**、跨度 **26.8pt**；**H2 倒挂 35 → 0**、**S1 越界 6 → 0**；H1/H2/S1 全 PASS |
| **map 帧（32×44）** | ⚠️ **新冲突，待裁**（D-72） | 目标已代裁（选项 a），但**实施会撞断言 8**：最近邻缩小丢掉 1px 材质分隔线 ⇒ 两条美术规则冲突 |

**本文 §0–§5 的 35 对倒挂 / 四条非单调阶梯等数字，是收口前的现场记录，予以保留。**
**最高危放大档（p3 T3 ×1.355）已由主理人导出改前/改后 PNG 逐帧核对：1px 描边干净，无灰边/断点/丢行。**

---

## 0. 结论

**§5.6.1 定义的两条正交通道里，Tier 体量通道在当前实现中并不存在。**

20 格矩阵的**纵轴**（「这是几级兵」）在**四族全部塌掉**：

| 判定 | 结果 | 规格要求 |
|---|---|---|
| 跨族「高 tier 反而更小」的等级倒挂对 | **35 / 120 对（29%）** | **0 对** |
| 四族体量阶梯严格递增 | **0 / 4 族** | 4 / 4 |
| 每族阶梯跨度 | 19.6 / 19.6 / 8.9 / 10.7 pt | ≥ 26 pt |

最严重的一对：

> **翠林 T3 藤蔓卫士 55.4%  <  晨曦 T2 号角弩手 82.1%**（−26.8pt）
> —— 一个三级兵，比别的族的**二级兵**小掉四分之一还多。

**这不是配色问题，也不是盲测运气问题。** `cartoon-style.md §5.6.5` 第 3 条把这个失效模式**逐字预言了，并且举 p3 为例**：

> 「如果某族的形状刚好在某些 tier 上『吃掉』了体量差（**比如 p3 的矮宽 T5 比 p1 的瘦高 T4 还小**），要回去改体量，不要改形状。」

实测确认：**翠林（p3）就是那个把体量差吃掉的族** —— 它的四档体量挤在 55.4%–64.3%（跨度 8.9pt），是四族里最平的。

按 §5.6.5 的处置表，本情形落在「**70–90% → 修体量**」一档（而非「<70% → 回退换皮」）。**修法是改体量，不是改形状**（第 3 条原文）。

---

## 1. 复算方法（可复现）

```bash
cd homm-web
npm run audit:b0 2>&1 | grep -E '^    (u|cu)_p[1-4]_' > /tmp/sil.txt
# 每行形如：
#   cu_p1_lampbearer  44×56  剪影 26×36  高宽比 1.38  顶宽 3px  左右对称 66%  占地 68%  脚底行 55
```

**体量定义 = 剪影高 ÷ 画布高。**

- §5.6.2 表头明写「体量（**占 44×56 战斗画布高**）」⇒ 定义画布是 **cu 帧 / 56**。下文「证据 A/B」以它为准。
- 另附 **map 帧 / 44**，因为 **§5.6.5 的 32px 盲测表用的是 map 帧** —— 若只修 cu 不修 map，盲测照样不过。

> ⚠️ **§5.6.2 只给了目标值（≤62 / ≤70 / ≈78 / ≈88），没给容差。**
> 因此下文把判定分成两档，请**只把「硬判定」当作结论**：
> - **硬判定**：只看**大小顺序与步长**，不依赖任何人设的容差。
> - **软判定**：用了我自设的 ±8pt 区间（**这是我的发明，不是规格**），仅作参考。

---

## 2. 证据 A —— 体量阶梯本身

### A-1 · cu 帧（44×56 · §5.6.2 的定义画布）

| 族 | T1 | T2 | T3 | T4 | 跨度 | T1→T2 | T2→T3 | T3→T4 |
|---|---|---|---|---|---|---|---|---|
| **p1 晨曦** | 64.3% | 82.1% | 83.9% | 82.1% | 19.6pt | **+17.9** | **+1.8** | **−1.8** |
| **p2 赤焰** | 66.1% | 66.1% | 60.7% | 80.4% | 19.6pt | **±0.0** | **−5.4** | **+19.6** |
| **p3 翠林** | 57.1% | 57.1% | 55.4% | 64.3% | 8.9pt | **±0.0** | **−1.8** | **+8.9** |
| **p4 紫晶** | 78.6% | 73.2% | 83.9% | 73.2% | 10.7pt | **−5.4** | **+10.7** | **−10.7** |
| *规格 §5.6.2* | *≤62* | *≤70* | *≈78* | *≈88* | *≥26pt* | *≥6* | *≥6* | *≥8* |

**加粗的每一格都是违规**：四族的阶梯**全部非单调**（p2/p3 的 T1 与 T2 体量完全相同，p1/p2/p3 都出现 T2→T3 下降）。规格要求一条 62→70→78→88 的**严格递增**阶梯，实测是四条各自乱序的折线。

### A-2 · map 帧（32×44 · 盲测表实际用的帧）

| 族 | T1 | T2 | T3 | T4 | 跨度 |
|---|---|---|---|---|---|
| **p1 晨曦** | 68.2% | 84.1% | 86.4% | 79.5% | 18.2pt |
| **p2 赤焰** | 68.2% | 68.2% | 63.6% | 84.1% | 20.5pt |
| **p3 翠林** | 61.4% | 59.1% | 63.6% | 65.9% | **6.8pt** |
| **p4 紫晶** | 77.3% | 75.0% | 88.6% | 79.5% | 13.6pt |

同一结论。**p3 跨度 6.8pt** —— 它的四级兵在地图上几乎一样高。

---

## 3. 证据 B —— 等级倒挂（矩阵塌掉的正式定义）

> **定义**：若存在某单位 A、B，`tier(A) > tier(B)` 而 `体量(A) < 体量(B)`，则 (A,B) 是一对**等级倒挂**。
> **规格要求 0 对**（否则「玩家先认大小再认形状」这条前提失效 —— §5.6.2 注释原文）。

| 画布 | 倒挂对数 | 占全部跨族对 |
|---|---|---|
| **cu（44×56，定义画布）** | **35 / 120** | **29%** |
| **map（32×44，盲测帧）** | **34 / 120** | **28%** |

最严重的 8 对（cu）：

```
❌ 翠林T3 藤蔓卫士   55.4%  <  晨曦T2 号角弩手   82.1%   ( −26.8pt)   ← 最严重
❌ 翠林T3 藤蔓卫士   55.4%  <  紫晶T1 石雕小怪   78.6%   ( −23.2pt)
❌ 翠林T3 藤蔓卫士   55.4%  <  紫晶T2 喷火学徒   73.2%   ( −17.9pt)
❌ 翠林T3 藤蔓卫士   55.4%  <  赤焰T1 捡破烂小鬼 66.1%   ( −10.7pt)
❌ 翠林T3 藤蔓卫士   55.4%  <  赤焰T2 投斧蛮子   66.1%   ( −10.7pt)
❌ 翠林T3 藤蔓卫士   55.4%  <  晨曦T1 提灯侍从   64.3%   (  −8.9pt)
❌ 翠林T3 藤蔓卫士   55.4%  <  翠林T1 浇水矮人   57.1%   (  −1.8pt)
❌ 翠林T3 藤蔓卫士   55.4%  <  翠林T2 荆棘射手   57.1%   (  −1.8pt)
```

**八对里七对的主角是翠林 T3。** 且它连**自家 T1/T2** 都比不过 —— 同族内部就已经倒挂。

**规格原文点名的两个失效情形，实测对照**：

| 规格原话 | 实测 | 判定 |
|---|---|---|
| 「赤焰 T3 比翠林 T2 还小」 | 60.7% vs 57.1% | ✅ 未发生 |
| 「p3 的矮宽比 p1 的瘦高还小」 | 翠林T4 64.3% vs 晨曦T1 64.3% | ⚠️ **持平**（差 0.0pt） |
| *（同类，规格未点名但同性质）* | **翠林T3 55.4% vs 晨曦T1 64.3%** | ❌ **−8.9pt** |
| *（同上）* | **翠林T4 64.3% vs 晨曦T2 82.1%** | ❌ **−17.9pt** |
| *（同上）* | **赤焰T3 60.7% vs 晨曦T1 64.3%** | ❌ **−3.6pt** |

⇒ 规格点名的**具体那一对**恰好没发生，但**同一失效模式在别的族对上发生了 35 次**。审计的价值就在这里：靠肉眼盯规格举的例子会漏，靠机检全量扫才看得见。

---

## 4. 附带发现 —— Faction 高宽比（§5.6.3）也有 6 处违规

| 族 | 规格 | 违规帧 |
|---|---|---|
| p1 晨曦 | ≥1.25 | `cu_p1_templar` **1.10** |
| p2 赤焰 | ≈1.0（容 0.85–1.15） | `cu_p2_axethrower` **1.23** · `cu_p2_wolfrider` **0.77** |
| p3 翠林 | ≤1.1 | （全部合规 ✅） |
| p4 紫晶 | ≥1.35 | `cu_p4_stoneimp` **1.16** · `cu_p4_hopgolem` **1.27** · `cu_p4_librarian` **1.28** |

**p4 三条全违** —— 紫晶的「菱形收尖 / 细长」形状家族在战斗帧上没立住。p3 四条全合 —— **翠林的形状通道是好的，塌的纯粹是体量通道**。这个对比很有用：说明修 p3 **只需动体量，不必碰形状**，正好是 §5.6.5 第 3 条的要求。

---

## 5. 根因（已确证，非推测）

`homm-web/src/render/unitArt.ts`，**2153 行，16 组 bespoke 绘制函数**：

```
function drawLampbearerMap(pb: PixBuf): void { … }
function drawLampbearerCombat(pb: PixBuf, atk: boolean): void { … }
function drawScavengerMap(pb: PixBuf): void { … }
… 共 16 单位 × 3 帧
```

```bash
$ grep -cE "tier|TIER|体量|volume|volScale" src/render/unitArt.ts
2          # 且这 2 处只是文件头注释里的「§5.6 逐 tier 剪影矩阵」字样
```

**每个单位的坐标是手写的常量，函数签名里没有任何 tier / 体量参数。**

于是：

- **Faction 通道（横轴）被实现了** —— 每单位独立画，形状确实按族分化（审计也证明 p3 的形状通道全合规）。
- **Tier 通道（纵轴）根本没有被实现** —— 每个单位画多高，是画的时候随手定的，没有任何东西约束它必须落在 §5.6.2 的那一档上。

**这正是 `asset-spec.md` §4.2 R5 / §9 里 ×4 之所以要求「全部走程序化」的原因** —— 程序化的价值在于「同一套骨架 + 参数化形状家族」能**保证** 4×5 正交成立。当前实现拿到了「程序化」这个手段，却没拿到它要保证的那个性质：**画得是程序化的，但矩阵是手凑的。**

---

## 6. 修法（§5.6.5 第 3 条：改体量，不要改形状）

### 6.1 第一步：把主观盲测变成机械前置 —— 先补断言

当前 `b0audit.mjs` 的 8 条断言全部关于**颜色 / 描边 / 基线**（色板归属、ΔL*、描边干净、脚底行），**没有一条关于剪影矩阵本身**。而剪影矩阵恰恰是 ×4 花 4 倍成本要买的那个东西。

必须补的断言：

- **硬（不依赖容差）**
  - `H1`：四族各自的 T1→T4 体量**严格递增**，且最小步长 ≥6pt。
  - `H2`：跨族 tier 倒挂对 **= 0**。
- **软（用自设容差，可调）**
  - `S1`：每族每 tier 落在 §5.6.2 目标 ±8pt。
  - `S2`：§5.6.3 高宽比合规（p1≥1.25 / p2∈[0.85,1.15] / p3≤1.1 / p4≥1.35）。

> ★ **执行纪律**：这条断言必须**先跑红**（证明它测得出当前的 35 对倒挂），**再**改代码让它变绿。
> 这正是本项目已验证过的方法 —— 「先试哪一处改动能让断言变红」，只有真病根被停用才会红。

### 6.2 第二步：体量归一化，且必须插在描边 pass **之前**

不要在 16 个 `drawXxx` 里逐一手改坐标（那是复现同一个错误：**又变成手凑**）。要加一个**共享的后置步骤**：

```
绘制本体 → 【体量归一化：按 tier 目标把剪影缩放到目标高度】 → 描边 pass → 交付
```

**为什么必须在描边之前**：`b0audit.mjs` 的断言 4「描边重跑干净」实现方式是「透明像素填成 ink0，即一圈 1 次膨胀」—— 说明**描边是后置生成的一圈膨胀**。把缩放放在它之前，描边就是在**新的剪影边界上重新生成**的，仍然 1px 单线。

**若放在描边之后**，缩放会把那 1px 描边插值成灰边 / 2px 断点 ⇒ **断言 4 必红**。这一点请在实现时作为硬约束。

### 6.3 两个画布都要归一到各自的目标

- cu 帧（44×56）：§5.6.2 的表就是为它写的，直接用。
- map 帧（32×44）：**§5.6.2 没写地图画布的目标**。但盲测表用 map 帧 ⇒ 必须在 map 画布上**建立同构的阶梯**（建议按同一比例，或按 §5.6.2 的 px 目标值等比换算）。**这一条需要 art-director 确认**，见 §7。

### 6.4 顺序：先修体量，**再**做 B3（T5）

T5 = 100%（56px，允许向上溢出 4px）—— 它是整条阶梯的**锚点**。若先做 B3，T5 会照当前的（错的）基准画，阶梯定稿后必须重画。**所以 B3 暂缓。**

---

## 7. 待他人裁定

| # | 事项 | 需要谁 |
|---|---|---|
| 1 | **§5.6.2 的容差**：规格只给目标值（≈78 / ≈88），没给带宽。我自设 ±8pt 用于软断言 —— 是否采纳？ | art-director |
| 2 | **map 画布（32×44）的 tier 体量目标**：§5.6.2 只定义在 44×56 上。盲测用 map 帧，必须补一组目标值 | art-director |
| 3 | **p4 的高宽比**（3/4 违规）是否与体量一并修，还是分开一单 | art-director |
| 4 | **§5.6.5 的 20 格盲测**：本次审计是**机械前置**，不能替代人工盲测。T5 补齐后仍需 3 人 × 各 1 次的去色 32px 4×5 网格标注 | 人（用户 / 外部） |

---

## 8. 与 R-1 的关系（为什么这不是「又发现一个 bug」）

`asset-spec.md` §4.2 的 **R-1** 写的正是这件事：

> ⚠️ **R-1 形状家族只在 T5 成立** → ×4 退化为「四套换色 + 额外帽子」，4 倍成本换 0 增量可读性
> **缓解**：B1 之后立刻做 20 格盲测；命中率 < 70% → 回退共用换皮，**止损在第 6.5 人日**

**R-1 的缓解措施（B1 之后的盲测）没有按期执行，一路做到了 B2 四族齐。** 本次审计补上了这一步的**机械部分**，结果指向 R-1 描述的失效：

- 形状通道（横轴）**成立** —— 四族的形状家族确实分化了，不是「换色 + 换帽子」。
- 体量通道（纵轴）**不成立** —— 纵轴塌了，20 格矩阵在纵向上退化成 4 格。

⇒ 现状是「**形状对了、体量没做**」，而不是「四套换色」。**这是可以修的**，且修法已由 §5.6.5 第 3 条给定（改体量）。**但若不做，×4 的增量可读性确实接近 0** —— 因为玩家认不出 tier，剩下能认的只有族。

---

*本审计的所有数字均来自 `npm run audit:b0` 的实测输出，复算命令见 §1。*
