# 资产规格与生成管线 · V2（定稿）

> 状态：**建议稿，待用户拍板**（开放问题见 §8）
> 作者：林绘澄（美术定稿） · 任务 A1
> 前置文档：`design/art-bible/cartoon-style.md`（色板 / 造型语言 / 搞笑规则 / **§5.6 逐 tier 剪影矩阵**）
> **权威兵种表**：`design/gdd/races.md`（4 族 × 5 兵种，映射 `dwell1…dwell5`）
> 本档目的：**让下一个人不用再问我任何事就能开画 / 开生成**

---

## 0. V2 修订说明（2026-09-18）

| 变更 | 内容 |
|---|---|
| **Q-5 已拍板** | 用户选择 **×4 每族独立兵种美术**（`races.md` / 设计线立场），**推翻** V1 的"共用换皮"建议 |
| **受影响的核心结论** | V1 的"95% 程序化 / ≤11 张 AI"这个**比例**不再成立；但 **"程序化为主"这个结论反而更强** —— 见 §4.2 R5 |
| **重算内容** | §2 资产计数与两张图集容量（新结论：**2048² 装得下，不需要第二张图集、不需要改网格、不需要 re-pack**）；§2.5 逐资产工作量；§9 生产顺序与最小验证批 |
| **新增** | `cartoon-style.md §5.6` 逐 tier 剪影矩阵（×4 的可读性保证，4×5 = 20 格） |
| **两项撤回** | 撤回 V1 的"建议图集缩到 1024²"（×4 后余量不够，见 §8 Q5）与"反对 28 张建筑图标"（×4 已拍板，只值 2 人日，见 §8 Q1） |
| **不变** | §1 尺寸网格、§3 命名规范、**§5 七步管线（原样保留）** |
| **V2.1 增补（D-17 方案②）** | 新增**喜剧布景层**的资产条目（规格正文 = `cartoon-style.md §6.1`）：§1.2 网格 + §2.3 清单 + §2.4 图集重算 + §2.5 工作量 + §3.2 命名 + §4.4 划分 + §8 Q8。**净增 38 程序化帧 / 0 张 AI**；总量 **191 → 229 帧**，Lean **27.5 → 31.2 人日**；冒险图集 **17.0% → 28.3%**（仍不需第二张图集） |

> **一句话结论**：×4 独立设计在**程序化**路线下可行且划算；在 AI 路线下几乎必然退化（§4.2 R5）。图集与帧数都**不是**瓶颈，**真正的成本杠杆是动画帧数**（§8 Q6，差 36 人日）。

---

## 1. 精灵尺寸网格

### 1.1 硬约束（来自代码，不可协商）

| 约束 | 值 | 来源 |
|---|---|---|
| 地图格边长 | **TILE = 32 px** | `src/render/atlas.ts:11` |
| 地图坐标 ⇄ 像素 | 1:1 正交 | `src/render/ortho.ts` |
| 物件锚点默认 | `ax = (32 - w)/2`，`ay = 32 - h`（贴地） | `atlas.ts:701-702` |
| 城堡锚点 | `ax = -4, ay = -32` | `atlas.ts:19-20` |
| 相机 zoom | 默认 **2**，区间 0.75–3 | `camera.ts:30-34` |
| 战斗单位画布 | **44 × 56**，锚点 `ax=-22, ay=-48` | `combatAtlas.ts:113` |
| 战斗 hex 地板 | **40 × 46**，锚点 `ax=-20, ay=-23` | `combatAtlas.ts:98` |
| 战斗单位渲染倍率 | `scale = 2` | `BattleRenderer.ts:54` |
| **冒险图集画布** | **2048 × 2048**（⚠️ `DESIGN.md` 写 1024 是**错的**） | `atlas.ts:751` |
| **战斗图集画布** | **1024 × 512** | `combatAtlas.ts:92` |

### 1.2 网格表

| 类别 | 画布 (w×h) | 锚点 (ax, ay) | 与现状关系 |
|---|---|---|---|
| 地形砖 | 32 × 32 | 0, 0 | 不变 |
| 岸线砖 | 32 × 32 | 0, 0 | 不变 |
| 小装饰 `deco_*` | 16 × 16 | 居中 / 贴地 | 不变（数量 6 → 12） |
| **地面布景 `sd_g_*`** | **32×32 / 64×32 / 64×64** | 贴地 `(0, 0)` | 新增（烘进布景层地面画布，**永不遮挡单位**） |
| **立体布景 `sd_v_*`** | **32×32 – 32×64 / 96×32** | 贴地 `(0, 32-h)` | 新增（参与 y 排序，**可遮挡单位**） |
| **活动物 `sd_a_*`** | **32×32 – 32×64** | 贴地 `(0, 32-h)` | 新增（每帧，按档位限流） |
| **图标**（资源 / 标记 / 徽记 / 建筑 / 地标 / 被动） | **24 × 24** | 居中 | 🔄 从 16×16 与 18×18 **统一到 24×24** |
| 地图小物件（树 / 石 / 矿 / 箱 / 泉 / 神器） | **32 × 48** | `(0, 32-h)` 贴地 | 🔄 高度预算从 40/46 统一到 **48** |
| **地图兵种** | **32 × 44** | 贴地 | 🔄 从 30/44 统一到 **44** |
| 英雄（地图） | **32 × 48** | 贴地 | 🔄 从 44 → 48（给大头） |
| 城镇主体（2×2） | **40 × 52** | 贴地 | 不变（美术换 4 族风格） |
| 城堡（2×2） | **72 × 96** | `ax=-4, ay=-32` | **不变**（锚点绝不能动） |
| 城镇面板建筑图标 | **40 × 40** | 居中 | 新增（`races.md` §8.2 要 28 个） |
| 战斗 hex 地板 | 40 × 46 | `ax=-20, ay=-23` | 不变 |
| **战斗兵种** | **44 × 56** | `ax=-22, ay=-48` | **不变**（见 `cartoon-style.md` §8.5） |
| 战斗队伍环 | 40 × 22 | `ax=-20, ay=-8` | 不变 |
| VFX 帧 | **48 × 48** | 居中 | 新增 |
| 阵营徽记（小） | 24 × 24 | 居中 | 新增 |
| 阵营立绘（大 / 选族页卡片 / 徽记主图） | 256 × 384 | — | 新增，**AI 生成，不进图集** |

> **为什么图标统一到 24×24**：现状 16×16 / 18×18 加上卡通化描边后会爆框（描边吃掉 1px 后可用内容只剩 14×14 / 16×16）。24×24 才装得下"描边 + 图形"。
>
> **为什么地图物件高度统一到 48**：现在 `tree=40 / mine=42 / vault=44 / mtn=46` 四档不齐，每画一个新资产都要重算 `ay`。统一后所有物件共用一条 `ay = 32 - 48 = -16`。
>
> **为什么新增布景三层**（`cartoon-style.md §6.1`）：地面布景烘进**独立画布**（与地形层同构、可整体开关）；立体布景与活动物**并入现有 y 排序**（`MapRenderer.ts:305-330`）以取得正确遮挡。三者全部程序化、全部 1px `ink0` 描边（高 ≤64px）。

### 1.3 与 zoom / dpr 的关系（决定"描边看起来多粗"、"动画要几帧"）

```
最终屏幕像素 = 画布像素 × zoom(2) × dpr(2~3) = 画布像素 × 4~6
```

→ **1 画布像素 = 手机上 4~6 物理像素**。两个直接后果：
1. 描边**不要加粗**（44×56 的 1px 描边在手机上已是 4–6 物理像素的粗黑边，改成 2px 会变成一团墨）。
2. **动画不需要逐帧画**：整数位移（±1px = 手机上 ±4~6px）与 `squash & stretch` 已经足够被眼睛读到。→ 这是 ×4 唯一的成本杠杆，见 §8 Q6。

---

## 2. 资产计数（×4 重算）

### 2.1 现有资产（119 帧 · 全部程序化 · 保留）

| 组 | 帧数 | 明细 |
|---|---|---|
| 地形砖 | 26 | 6 类 × 3 变体 + 水 2 变体 × 4 帧 |
| 岸线 | 8 | `sh_{n,s,e,w,ne,nw,se,sw}` |
| 地图物件 | 20 | 树 2 / 石 2 / 山 2 / 箱 / 泉 / 神器 / 资源堆 3 / 矿 7 / 宝库 |
| 城镇 + 城堡 | 40 | 5 owner × 4 tier × 2（**帧数不变，美术换成 4 族风格**） |
| 英雄 | 5 | `hero_{p1..p4,neutral}` |
| 怪物 | 3 | 狼 / 猪 / 食人魔 |
| 标记 + 图标 + 装饰 | 17 | `mk_*` 4 + `ic_*` 7 + `deco_*` 6 |
| **合计（冒险图集）** | **119** | 另有战斗图集 19 帧 |

### 2.2 兵种清单：4 族 × 5 tier × 3 帧（**权威表 = `races.md` §6.1**）

每个兵种产 **3 帧**（`cartoon-style.md` §4.1 方案 A；取舍理由见 §8 Q6）：

| 帧 | 帧名 | 画布 | 说明 |
|---|---|---|---|
| 地图帧 | `u_<faction>_<unit>_map` | 32×44 | 冒险地图上一"队"的代表图 |
| 战斗基础帧 | `cu_<faction>_<unit>` | 44×56 | 待机 / 移动 / 受击 / 死亡全部由它 + 程序化变换演出 |
| 战斗攻击帧 | `cu_<faction>_<unit>_atk` | 44×56 | 预烘焙的 0.85× 高度 squash 帧（避免运行时非整数缩放糊边） |

| 阵营 | T | 兵种（`races.md`） | `dwell` 槽 | 地图帧 | 战斗帧 | 攻击帧 | 帧数 | 方式 |
|---|---|---|---|---|---|---|---|---|
| **晨曦** | 1 | 提灯侍从 | dwell1 | 1 | 1 | 1 | **3** | 程序化 |
| 晨曦 | 2 | 号角弩手 | dwell2 | 1 | 1 | 1 | **3** | 程序化 |
| 晨曦 | 3 | 铁誓枪兵 | dwell3 | 1 | 1 | 1 | **3** | 程序化 |
| 晨曦 | 4 | 圣殿骑士 | dwell4 | 1 | 1 | 1 | **3** | 程序化 |
| 晨曦 | 5 | 加班天使 | dwell5 | 1 | 1 | 1 | **3** | 程序化 |
| **赤焰** | 1 | 捡破烂小鬼 | dwell1 | 1 | 1 | 1 | **3** | 程序化 |
| 赤焰 | 2 | 投斧蛮子 | dwell2 | 1 | 1 | 1 | **3** | 程序化 |
| 赤焰 | 3 | 暴走狼骑 | dwell3 | 1 | 1 | 1 | **3** | 程序化 |
| 赤焰 | 4 | 火油狂徒 | dwell4 | 1 | 1 | 1 | **3** | 程序化 |
| 赤焰 | 5 | 熔岩巨魔 | dwell5 | 1 | 1 | 1 | **3** | 程序化 |
| **翠林** | 1 | 浇水矮人 | dwell1 | 1 | 1 | 1 | **3** | 程序化 |
| 翠林 | 2 | 荆棘射手 | dwell2 | 1 | 1 | 1 | **3** | 程序化 |
| 翠林 | 3 | 藤蔓卫士 | dwell3 | 1 | 1 | 1 | **3** | 程序化 |
| 翠林 | 4 | 树人大叔 | dwell4 | 1 | 1 | 1 | **3** | 程序化 |
| 翠林 | 5 | 独角兽园丁 | dwell5 | 1 | 1 | 1 | **3** | 程序化 |
| **紫晶** | 1 | 石雕小怪 | dwell1 | 1 | 1 | 1 | **3** | 程序化 |
| 紫晶 | 2 | 喷火学徒 | dwell2 | 1 | 1 | 1 | **3** | 程序化 |
| 紫晶 | 3 | 蹦跳魔偶 | dwell3 | 1 | 1 | 1 | **3** | 程序化 |
| 紫晶 | 4 | 亡灵图书管理员 | dwell4 | 1 | 1 | 1 | **3** | 程序化 |
| 紫晶 | 5 | 失控大魔像 | dwell5 | 1 | 1 | 1 | **3** | 程序化 |
| **合计** | | **20 个兵种** | | **20** | **20** | **20** | **60 帧** | **0 张 AI** |

> ⚠️ **兵种全部走程序化，一张 AI 都不用。** 理由见 §4.2 R5：×4 的 4×5 正交矩阵（形状家族 × tier 体量）只有程序化能保证成立。
> ⚠️ **`dwell1…dwell5` 槽位映射照抄 `races.md`，不提议移动槽位**（建筑系统零改动是这份设计的前提）。

### 2.3 非兵种资产清单

| 组 | 帧数 | 规格 | 方式 |
|---|---|---|---|
| **城镇 + 城堡（4 族风格）** | 40 | `town_<faction>_<tier>` 40×52 / `castle_<faction>_<tier>` 72×96 | 程序化（换 4 套母题，帧数不变） |
| **城镇面板建筑图标**（`races.md` §8.2） | **28** | 4 族 × (5 兵营 + 2 专属) = 28 × 40×40 | 程序化 |
| 共用建筑图标（经济 / 城防 / 行会） | 15 | 酒馆 / 市场 / 议事堂 / 工坊 / 墙 3 / 行会 3 … | 程序化（**共用一套，不按族重画**，依 `races.md` §8.2 尾注） |
| **UI 图标** | **12** | 4 个种族被动图标 + 8 个专属建筑图标，24×24 | 程序化 |
| **地图地标图标**（`maps.md` 脚本事件） | **6** | 「迷路的信使」「同乐会山贼」等，24×24 | 程序化 |
| 阵营徽记（小） | 5 | `crest_<faction>` 24×24 | 程序化 |
| 怪物 | **7** | 现有 3 + 新 4，32×44 / 44×56 | 程序化 |
| 小装饰 | **12** | 16×16（6 → 12） | 程序化 |
| **喜剧布景道具**（`cartoon-style.md §6.1`） | **38** | 地面涂鸦 `sd_g_*` 12 + 立体道具 `sd_v_*` 14 + 活动物 `sd_a_*` 12（32×32 – 96×32） | 程序化（**0 张 AI**） |
| VFX | **12** | `fx_*` 48×48（火球 / 闪电 / 治疗 / 爆炸 / 碎墙 / 中毒 …） | 程序化 |
| 英雄 | 5 | `hero_<owner>` 32×48 | 程序化 |
| **AI 生成（见 §6）** | **11** | 4 阵营立绘(256×384) + 5 英雄城堡(→72×96) + 2 地图地标 | **AI** |

### 2.4 总量与图集容量判定

**冒险图集 2048² —— ✅ 装得下，不需要第二张图集 / 不需要缩小网格 / 不需要 re-pack。**

| 项 | 宽度占用 | 行高 |
|---|---|---|
| 现有 119 帧（实测） | 填满 2048 | 212 px（2 个 96px 行 + 1 个 16px 行） |
| + 20 地图兵种（32→34 槽） | 680 | 44 |
| + 28 建筑图标（40→42 槽） | 1176 | 40 |
| + 12 UI 图标 + 6 地标 + 5 徽记（24→26 槽） | 598 | 24 |
| + 4 新怪物（32→34 槽） | 136 | 44 |
| + 6 新装饰（16→18 槽） | 108 | 16 |
| + 12 VFX（48→50 槽） | 600 | 48 |
| **新增宽度合计** | **3298 px** | → **2 行**（每行上限 2048） |
| **新增高度** | | **≈ 136 px** |
| **新总高** | | **≈ 348 px / 2048 = 17.0%** |

> **判定**：2048² 的 **17%**。余量 ≈1700 px 高，足够再装 3 倍内容。
> 注：shelf 打包是宽度贪婪的，上表按**分配面积**（含行尾空隙与 2px 间隙）算，已是保守值。

**V2.1 追加：喜剧布景层（38 帧 · `cartoon-style.md §6.1`）占用重算**

| 项 | 宽度占用 | 行高 |
|---|---|---|
| + A 组地面涂鸦（6 型 ×2 变体 = 12 帧） | 536 px | 64（+2） |
| + B 组立体道具（7 型 ×2 变体 = 14 帧） | 668 px | 64 / 34（两行） |
| + C 组活动物（4 型 ×3 帧 = 12 帧） | 408 px | 64（+2） |
| **布景新增宽度合计** | **1612 px** | → **< 1 行**（每行上限 2048） |
| **布景新增高度** | | **≈ 232 px** |
| **V2.1 新总高** | | **≈ 348 + 232 = 580 px / 2048 = 28.3%**（原 17.0%） |

> **判定（V2.1）**：冒险图集仍只用 **28.3%**，余量 ≈1468 px 高。**仍不需要第二张图集、不需要缩小网格、不需要 re-pack**（沿用原 shelf 顺序继续排即可）。
> 注：布景帧全程序化，**图集占用是唯一成本，不产生任何外部文件体积**。

**战斗图集 1024×512 —— ✅ 装得下（现有条带 830×56，即 8.9%）。**

| 项 | 计算 |
|---|---|
| 现有 19 帧实测占用 | ≈ **830 px 宽 × 56 高** |
| + 20 基础帧 + 20 攻击帧 = 40 × 46 槽 | = **1840 px 宽** |
| 行数 | 首行还能放 4 帧 → 剩 36 帧 / 每行 22 帧 → **共 3 行** × 56 高（含 2px 间隙） |
| 新总高 | **≈ 172 px / 512 = 33.6%** |
| 余量 | 第 3 行还剩 380 px → 还能再放 **8 帧** |

**结论汇总**

| 问题 | 答案 |
|---|---|
| 20 个兵种 ×3 帧要不要第二张图集？ | ❌ **不要**。冒险图集 **28.3%**（含布景层）、战斗图集 33.6% |
| 要不要缩小每单位网格？ | ❌ **不要**。按 §1.2 网格原样即可 |
| 要不要 re-pack？ | ❌ **不要**。按现有 shelf 顺序排下去即可 |
| 有别的容量风险吗？ | ⚠️ 有一条：**若按 `races.md` §8.3 字面做 5 动作/单位（200–300 帧）**，战斗图集需求将超 512 高，**必须扩到 1024×1024 或改网格**。这正是 §8 Q6 要守住的 |

### 2.5 逐资产工作量（P1.4 估算依据）

> 口径：**人日 = 8 小时，我方专业判断，非实测**。程序化单帧含：形状 / 道具 / 表情 / 描边 / 调色 / 试渲染。

| 工作包 | 帧数 | 单帧 | 小计 |
|---|---|---|---|
| 兵种骨架 + 4 组"形状家族"参数化（一次性） | — | — | **2.5 人日** |
| **20 兵种 × 3 帧（= 60 帧）** | 60 | 1.5 h | **11.5 人日** |
| 城镇 / 城堡 4 族风格（改造现有 `town()` / `castle()`） | 32 | 1.2 h | **5 人日** |
| 28 建筑图标 + 15 共用建筑图标 | 43 | 0.6 h | **3 人日** |
| UI 图标 12 + 地标 6 + 徽记 5 + 怪物 4 + 装饰 6 + VFX 12 | 45 | 0.4 h | **2.5 人日** |
| AI 11 张（出图重试 + 跑管线 + 手工修 + 管线工具 1 天） | 11 | 1.2 h | **3 人日** |
| **喜剧布景道具**（`cartoon-style.md §6.1`：地面 12 + 立体 14 + 活动物 12） | 38 | 0.8 h | **3.7 人日** |
| **合计（Lean，推荐）** | **≈ 229 帧 + 11 张** | | **≈ 31.2 人日** |
| ⚠️ 若按 5 动作 / 单位字面执行 | + ≈190 帧 | 1.5 h | **+36 人日 → ≈ 66.7 人日** |

**→ 动画帧数是唯一能一次性砍掉约 53% 成本（31.2 → 66.7 人日）的杠杆。**
> **V2.1 注**：布景层另需工程 **≈2.5 人日**（`SetDressingLayer` + 放置 + y 排序接入 + 档位接线 + 烘焙），美术 + 工程合计 **≈6.2 人日**，详见 `cartoon-style.md §6.1.8`。

---

## 3. 命名规范

### 3.1 规则

```
<前缀>_<族>_<对象>[_<变体>]
- 全小写 + 下划线（沿用现状，不要引入驼峰）
- 族 = p1 | p2 | p3 | p4 | neutral
- 变体 = 0..n 或方向 = n|s|e|w|ne|nw|se|sw
- 兵种短名由 races.ts 的 UnitType.id 决定（§3.2 表里给的是建议值）
```

### 3.2 前缀登记表

| 前缀 | 含义 | 示例 | 画布 |
|---|---|---|---|
| `g_` | 地形砖 | `g_grass_0` | 32×32 |
| `sh_` | 岸线砖 | `sh_ne` | 32×32 |
| `deco_` | 地表小装饰 | `deco_7` | 16×16 |
| `tree` / `rock` / `mtn` | 植被 / 石 / 山 | `tree1`, `mtn0` | 32×48 |
| `res_` | 地图上的资源堆 | `res_gold` | 32×48 |
| `mine_` | 矿场 | `mine_crystal` | 32×48 |
| `chest` / `fountain` / `artifact` / `vault` | 单体物件 | `chest` | 32×48 |
| `town_` | 城镇（**每族独立**） | `town_p2_3` | 40×52 |
| `castle_` | 城堡（**每族独立**） | `castle_p4_2` | 72×96 |
| `outpost_` | 1×1 前哨 | `outpost_p1_1` | 32×40 |
| **`u_`** | **地图上的兵种** | **`u_p3_thornarcher_map`** | **32×44** |
| **`cu_`** | **战斗中的兵种**（沿用现状前缀） | **`cu_p3_thornarcher`** | **44×56** |
| **`cu_…_atk`** | **战斗攻击帧** | **`cu_p3_thornarcher_atk`** | **44×56** |
| `hero_` | 英雄 | `hero_p1` | 32×48 |
| `mon_` | 中立怪物 | `mon_wolf` | 32×44 |
| `cf_` / `chi_` / `cbase_` | 战斗地板 / hex 高亮 / 队伍环 | `chi_atk` | 40×46 / 40×22 |
| **`bld_`** | **城镇面板建筑图标** | **`bld_p2_dwell3`**, `bld_shared_market` | **40×40** |
| **`abi_`** | 种族被动图标 | `abi_p4` | 24×24 |
| **`lm_`** | 地图地标图标 | `lm_lostcourier` | 24×24 |
| `crest_` | 阵营徽记（小） | `crest_p1` | 24×24 |
| **`crestL_`** | 阵营立绘（大，**不进图集**） | `crestL_p2` | 256×384 |
| `mk_` / `ic_` | 地图标记 / UI 资源图标 | `mk_gold`, `ic_crystal` | 24×24 |
| **`fx_`** | **VFX** | **`fx_fireball_02`** | **48×48** |
| **`sd_g_`** | **喜剧布景 · 地面涂鸦** | `sd_g_hopscotch_0` | **32×32 – 64×64** |
| **`sd_v_`** | **喜剧布景 · 立体道具** | `sd_v_signpost_1` | **32×32 – 32×64 / 96×32** |
| **`sd_a_`** | **喜剧布景 · 活动物** | `sd_a_chicken` | **32×32 – 32×64** |

> 建筑图标命名：`bld_shared_*` = 15 个共用建筑（依 `races.md` §8.2 尾注，共用一套不按族重画）；`bld_<faction>_*` = 4 族 × 7 个（5 兵营 + 2 专属）。
> **布景命名例外**（`cartoon-style.md §6.1`）：`sd_*` 三类**无族段**（布景是中立道具，不按族重画）→ 形如 `sd_g_<对象>_<变体>` / `sd_v_<对象>_<变体>` / `sd_a_<对象>`。三个前缀互不为前缀，解析无歧义。

### 3.3 文件落地（仅 AI 资产才落盘）

```
assets/sprites/<前缀>/<名称>.png
  例：assets/sprites/crestL/crestL_p1.png
      assets/sprites/castle/castle_p2_3.png
```

**格式硬约束**：
- **PNG-8（索引色），≤ 32 色**，带完整 alpha（**0 或 255，禁止半透明**——半透明在硬边像素风里会变灰边）
- 无交错（no interlace）、无 `sRGB` / `gAMA` / ICC chunk（避免浏览器色彩管理导致偏色）
- 单文件 ≤ 8 KB；`crestL_*` 立绘放宽到 **≤ 24 KB**
- 文件名即精灵名（`crestL_p1.png` → 帧名 `crestL_p1`）

> ⚠️ **本节（§3.3）的适用范围（2026-09-18 订正）**：以上约束**只约束「AI 资产落盘的 PNG 文件」** —— 这是本节标题「文件落地（**仅 AI 资产才落盘**）」的字面含义。
> **它不约束程序化精灵**（`atlas.ts` 里用 `PixBuf` 直接画进图集的部分，包括全部 20 个兵种、建筑、图标、**以及 §2.3 新增的 38 帧喜剧布景**）—— 程序化精灵根本不产生 PNG 文件，本节对它们无意义。
> 不要拿「alpha 只能 0/255」去要求程序化精灵。反过来说，程序化精灵也应**主动**避免半透明（理由见 `cartoon-style.md` §1.4 的订正：半透明在 `zoom 2 × dpr 3` 下会变 4~6px 的灰紫脏边）—— 结论一致，但依据不同，不要混用。

---

## 4. 程序化 vs AI 生成：划分结论（×4 重算）

### 4.1 结论

> **卡通化不需要推翻"零外部素材"。×4 之后这一点更强，不是更弱。**
> 程序化覆盖 **≈95% 的帧数**（全部 20 个兵种 ×3 帧 + 全部建筑 / 城镇 / 图标 / VFX）。
> AI 生成只保留 **11 张「英雄级」静态资产**（§6）。
> **兵种：0 张 AI。**

### 4.2 逐条理由

| # | 理由 | 论证 |
|---|---|---|
| **R1** | **方向是反的** | AI 的价值在"补有机细节"（布褶 / 毛发 / 材质纹理）。卡通的定义是**减少信息量 + 放大特征量**。我们要丢掉的正是 AI 最擅长生产的东西 |
| **R2** | **搞笑靠动，不靠静** | 卡通笑点 = `squash & stretch` + idle 浮动 + 受击位移。AI 产出**静态位图**，做不了程序化动画 |
| **R3** | **尺寸天花板** | 目标画布 32–72 px，AI 出图 1024²，**信息损失 16–32 倍**。它最擅长的细节在这一步被销毁 |
| **R4** | **色板纪律** | AI 天生不守色板（P1.4 草稿约 40 种浑浊中间调，缩到 44px 读成一坨褐色）。要用 AI 就必须加"钳色"后处理，而钳色之后剩下的信息量，程序化本来就能给 |
| **R5** ⭐ | **×4 让"一致性"从重要变成决定性** | 20 个独立单位必须满足 `cartoon-style.md §5.6` 的 **4×5 正交约束**：4 组形状家族 × 5 档 tier 体量。程序化的一致性来自**结构**（同一套骨架 + 参数化形状家族），一定成立；用 AI 生成 20 张再互相凑（"这 4 张要像一个族、这 5 张要像同一 tier、20 张两两可辨"）是**生成模型最不可控的任务**，几乎必然退化成"四套换色 + 额外帽子"。**→ ×4 是把"程序化"从推荐变成唯一可行解的那条决策** |
| **R6** | **架构代价** | 引入 AI = 打破"零外部素材"（见 §4.3）。收益是 11 张门面，成本是 100% 的架构约定 |

### 4.3 如果打破"零外部素材"，代价清单（诚实版）

| 代价 | 具体 | 缓解 |
|---|---|---|
| **① 体积** | 11 张（含 4 张 256×384 立绘）≈ 80–140 KB | 严格锁死在 ≤11 张 |
| **② 离线 / PWA** | `public/sw.js` 缓存清单要加，否则离线打开缺图 | 见 ③ |
| **③ 请求数** | 每个 PNG 一次请求（首屏 11 次） | **推荐 base64 内联**：保持**同步构建**（`Atlas.build()` 是同步的）+ **零请求** + **离线天然可用**。代价 +33% 体积。✅ 值 |
| **④ 管线工具** | 新增 `tools/import-sprite.mjs`（§5 的 7 步）= 1 人日 | 一次性投入 |
| **⑤ 维护路径变长** | 程序化改 1 行代码即可调；AI 资产要"重新生成 → 重跑管线" | 锁死在 ≤11 张，可接受 |
| **⑥ 一致性风险** | AI 资产可能与程序化资产"不像一家人" | §5.4 的**描边重建 + 钳色**是强制步骤，是本风险的唯一解 |

### 4.4 分项划分表

| 资产类别 | 帧数 | 方式 | 理由 |
|---|---|---|---|
| 地形 / 岸线 / 装饰 | 46 | **程序化（现状不动 / 装饰微调）** | 已达标；重写成本 >> 收益 |
| 地图物件（树石矿箱泉） | 20 | **程序化** | 形状简单、数量大（20+），AI 一致性成本高 |
| **兵种（20 × 3 帧）** | **60** | **程序化** | ⭐ R5：4×5 正交矩阵只有程序化能保证成立 |
| 城镇 / 城堡（4 族） | 40 | **程序化** | 需要**结构一致、仅换母题**的 32 张，只有程序化能做到 |
| 建筑图标（4 族 ×7 + 共用 15） | 43 | **程序化** | 尺寸小（40px），AI 在此档无优势 |
| 图标 / 标记 / 徽记 / 被动 | 23 | **程序化** | 24px，同上 |
| 怪物 / 英雄 | 12 | **程序化** | 要参与战斗缩放动画 |
| VFX | 12 | **程序化** | 必须逐帧可控 + 是"动作" |
| **喜剧布景道具**（`cartoon-style.md §6.1`） | **38** | **程序化（0 张 AI）** | 笑点靠「动」（鸡 / 鹅 / 烟）+ 尺寸仅 32–96px + 必须成组同源一致 → 三条同时指向程序化（同 R2 / R3 / R5） |
| **阵营立绘兼徽记主图** | **4 张** | **AI** | 256×384 够大、只 4 张、是**门面**（选族页 / 加载图）；AI 的构图能力在这里是真实增益 |
| **每族"英雄建筑"城堡** | **5 张** | **AI** | 72×96 是全项目最大精灵，也是"城市风格差异化"最强的表达点 |
| **2 张 example map 的地标** | **2 张** | **AI** | 一次性、不可复用、需要想象力（巨像 / 沉船 / 古树） |

---

## 5. 生成 → 落地管线（7 步，含参数）

> **本节 V1 → V2 未修改**（`races.md` 未改变管线需求，团队已确认原样保留）。

### 5.1 全流程图

```
[0] 出图 1024×1024（洋红底 + 指定区块）
      ↓
[1] 抠底（ΔRGB < 40 → 透明）      ← 必须在降采样【之前】
      ↓
[2] 块众数降采样 8:1 → 128×128 真像素
      ↓
[3] 调色板钳制（映射到 cartoon-style §3 色板）
      ↓
[4] 裁剪 → 整数比缩放 → 画布落位 → 基线对齐
      ↓
[5] 描边重建（丢掉 AI 的边，重跑 outline）
      ↓
[6] 手工修（只修 3 处，见 §5.5）
      ↓
[7] 存 PNG-8 ≤32 色 → assets/sprites/<前缀>/<名>.png → base64 内联 → 进图集
```

### 5.2 已知陷阱与解法（**本节是文档的核心价值**）

> **陷阱：AI 的"伪像素"直接降采样会糊成一团。**

**病因**（三条同时发生）：
1. AI 画的"像素块"是**软边**的，块与块之间有 1–3px 的抗锯齿过渡带；
2. `bilinear` / `area` 平均会把过渡带**平均成新的中间色** → 输出的每个像素都变成"糊掉的平均色"，相邻块互相污染 → 全图退化为**低对比度灰浆**；
3. AI 的块尺寸**不是精确 8**，会在 7–9px 之间漂移 → 用 `nearest` 会随机采样到过渡带像素（得到错色），表现为 sprite 上出现**随机杂点**。

**解法（三步组合，缺一不可）**：

| 步骤 | 做法 | 为什么这样能解决 |
|---|---|---|
| **① 先抠底，再降采样** | 在 **1024 原图**上按色距把洋红键成透明，**不要等降采样之后再抠** | 若先降采样，洋红会和角色边缘的过渡带平均成**粉边**，且粉边无法再被抠掉（色距已经不匹配） |
| **② 用「块众数」降采样，不用 nearest / bilinear** | 按检测到的块尺寸 k（默认 **8**）切方格；每格取**出现次数最多的颜色**（mode，平票时取中位亮度）作为输出像素 | mode 天然完成两件事：㈠ 吃掉块内过渡带（少数派过渡色被多数派真实色击败）；㈡ **自动色彩量化**（AI 的伪渐变被压成平面色块，正是我们要的"平涂"）。nearest 会采到过渡带，bilinear 会造出新色——mode 都避开了 |
| **③ 降采样后再钳色** | 把 128×128 的每个颜色映射到 `cartoon-style §3` 色板里**最近的色**（先在 HSL 空间分桶到 5 个明度台阶，再取同台阶内色相最近者；**不要用朴素 RGB 欧氏距离**，它会把深红映射到深绿） | 这是"一家人"的保险。AI 永远会带脏色，不钳色就不像本项目 |

### 5.3 出图阶段的参数建议（把问题挡在生成侧）

| 参数 | 建议值 | 理由 |
|---|---|---|
| 分辨率 | **1024 × 1024** | 现状草稿就是这个尺寸 |
| 底色 | **纯洋红 `#FF00FF`** | 与任何自然色都远离，色距阈值好定 |
| **区块大小** | 明确要求 **"each visible pixel block = 8 × 8 output pixels"** | 让 §5.2 的 k 可预期。**这是最有效的一条 prompt 约束** |
| 抗锯齿 | 明确要求 **"hard edges, no anti-aliasing, no gradients, no dithering"** | 从源头减少过渡带 |
| 色数 | 明确要求 **"limited palette, at most 8–12 flat colors"** | AI 会倾向画 40 色，限死它 |
| 构图 | 单主体居中、**四周留 ≥64px 空白**、**无投影 / 无地面 / 无文字水印** | 草稿右下角有"AI生成 WORKBUDDY"水印，**必须在 prompt 里禁止** |
| 参考色 | prompt 里给 **3 个 hex**（该族 `accent` / `wall` / `clash`） | 让 AI 提前靠拢色板，减少钳色的暴力程度 |

**Prompt 模板**（可直接粘贴）：

```
pixel art sprite, <主体描述>, <视角: 3/4 top-down view>,
flat cel-shaded colors with bold 1px warm-dark outline,
cartoon proportions: oversized head (1:3 head-to-body), expressive eyes,
limited palette of at most 10 flat colors: <#hex1>, <#hex2>, <#hex3>,
each visible pixel block = 8x8 output pixels, hard edges,
NO anti-aliasing, NO gradients, NO dithering, NO texture noise,
solid magenta background #FF00FF, subject centered,
at least 64px empty margin on all sides,
no shadow, no ground, no text, no watermark, no logo
```

### 5.4 步骤 [4]【5】的具体规则

| 步骤 | 规则 |
|---|---|
| **[4] 裁剪** | 按 alpha 非零的 bbox 裁掉四周空白 |
| **[4] 缩放** | **只用整数比**（1/2、1/3、1/4），且目标 ≤ 画布尺寸。**禁止非整数比**（破坏像素栅格，行列宽窄不均） |
| **[4] 落位** | 放入目标画布（如 72×96），**水平居中，底部基线严格对齐画布底边**。所有精灵共用同一条基线（见 `atlas.ts:702` 的 `ay = TILE - h`） |
| **[5] 描边重建** | ⚠️ **丢掉 AI 自带的描边**（粗细不均、发灰、有抗锯齿残留）。用 `PixBuf.outline(ink0, 1.0)` 重跑；≥65px 的图跑 2 次或加 `width=2`。**这是让 AI 资产与程序化资产"像一家人"的唯一决定性步骤** |

### 5.5 步骤 [6]：**哪些必须手工修**

只修这三类（**每个精灵 5–15 分钟**）：

| # | 修什么 | 判据 |
|---|---|---|
| 1 | **眼睛 / 表情** | 见 `cartoon-style.md` §4 H3。AI 的眼睛基本不可用（1px 或糊成灰点），**必须重画成 ≥2px + 嘴线** |
| 2 | **剪影毛边** | 抠底 / 降采样常在轮廓上留 1–2px 毛刺；吃掉它，保证轮廓是一条干净的 1px 线 |
| 3 | **遮挡与穿模** | 弓弦穿手、长矛穿身体、旗杆穿屋顶 —— AI 不理解的物理关系 |

**不要手工修**：色板（靠 §5.2③ 自动钳）、描边（靠 §5.4 自动重跑）、比例（要改比例就重新生成）。

### 5.6 落地方式（二选一，推荐 B）

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A. `fetch` + PNG 文件** | 存 `dist/assets/sprites/*.png`，启动时 `await` 加载 → `drawImage` 入图集 | 源码干净、体积小 | ⚠️ `Atlas.build()` 是**同步**的（`atlas.ts:750`），改异步会牵动 `main.ts` 启动流程；`sw.js` 缓存清单要加；首屏 11 次请求 |
| **B. base64 内联（推荐）** | `tools/import-sprite.mjs` 输出 `src/render/generated/sprites.ts`，`atlas.ts` 启动时 `new Image()` + `drawImage` 同步可用 | ✅ 保持**同步构建**、**零网络请求**、**PWA 离线天然可用**、TS 类型安全 | 体积 +33%（11 张约 110–190 KB） |

### 5.7 管线在 ×4 下的适用范围（P1.4 估算用）

管线**只服务 11 张 AI 资产**（§6）。兵种不走管线，走 §2.5 的程序化工作包。
两条路径共用的只有：**色板（`cartoon-style.md §3`）** 与 **描边重建规则（§5.4）** —— 这正是"AI 资产与程序化资产同族"的技术保证。

---

## 6. AI 生成资产清单（11 张）

| 编号 | 资产 | 输出画布 | 数量 | 出图要点 | 是否进图集 |
|---|---|---|---|---|---|
| **AI-1** | 阵营立绘兼徽记主图 `crestL_<faction>` | **256 × 384** | **4** | 该族招牌兵种 + 母题场景（尖顶钟楼 / 兽刺棚寨 / 树冠花园 / 水晶实验室）；族色板 3 色；**不要**文字 | ❌ 不进（选族页 DOM 卡片直接 `<img>`） |
| **AI-2** | 每族"英雄建筑"城堡 `castle_<faction>_hero` | → **72 × 96** | **5** | 2×2 城堡，**平视略俯**（与现状 `castle()` 视角一致）；必须读得出 `cartoon-style.md §5` 的族母题；出图留好左右 4px / 上 32px 余量（配合 `ax=-4, ay=-32`） | ✅ 进（冒险图集） |
| **AI-3** | example map 地标 `lm_landmark_*` | → **96 × 128** | **2** | 叙事地标（巨像 / 沉船 / 古树），占图 3×3–4×4 格 | ✅ 进 |

**为什么是这 3 类而不是别的**：
1. 它们是**尺寸最大的 3 类**（≥72px），AI 的细节损失最小；
2. 它们是**数量最少的 3 类**（合计 11），一致性风险可控；
3. 它们**不参与程序化动画**（不需要 squash & stretch）；
4. ⭐ **×4 下的降风险技巧**：AI-2 的 5 张里，城堡**结构**先在程序化侧搭好，AI **只覆盖"屋顶母题 + 装饰"** —— 既拿到 AI 的想象力，又保住图集里的结构一致性。

---

## 7. 工程交接（摘要，详见 `cartoon-style.md` §8）

| 模块 | 动作 |
|---|---|
| `render/pixel.ts` | 新增 `rrect`（圆角矩形）、`outline(width)`（粗描边）、`cap`（胶囊）、`rimlight`、`face(expr)`、`groundContact`、`scaled(sx,sy)`（整数最近邻） |
| `render/atlas.ts` | ① 订正文档（2048² 不是 1024²）；② **不要缩到 1024²**（×4 后用到 17%，1024² 下需 ≈5 个 96px 行，余量紧张 —— 见 §8 Q5）；③ 新增 base64 位图入图集通道；④ 新增 `u_*` / `bld_*` / `abi_*` / `lm_*` / `crest_*` / `fx_*` 槽位 |
| `render/combatAtlas.ts` | 单位循环从硬编码 8 个 id 改为**按 `races.ts` 的族 × 兵种表遍历**（20 × 2 帧）；`Shelf(1024,512)` **不需扩容**（用到 33.6%）。⚠️ 若 §8 Q6 走字面 5 动作，此图集必须扩到 **1024×1024** |
| `render/lightLayer.ts` | 夜晚 floor 0.58 → **0.70**；夜晚 tint → **`rgb(178,182,205)`**；主体层可选浅乘 |
| `render/camera.ts` | `minZoom` 0.75 → **1.0**；`maxZoom` 3 → **2**；静止时 zoom 吸附整数 |
| `core/data/*` | ⚠️ 需要 `races.ts`（汇总 20 兵种）+ `FactionDef.units`。**结构由 D1 决定**，我方规格是"每族 5 个单位 × 3 帧"。等 D1 交付后对齐，不要各自假设 |
| `tools/` | 新增 `tools/import-sprite.mjs`（§5 的 7 步，Node 版无 DOM） |

---

## 8. 开放问题（需用户拍板）

| # | 问题 | 选项 | 我的建议 |
|---|---|---|---|
| ~~Q-5~~ | ~~每族兵种独立设计 vs 共用换皮~~ | ✅ **已拍板：×4 独立设计** | 已接受。可执行保证 = `cartoon-style.md §5.6`；成本 = §2.5 的 **≈31.2 人日**（含布景层） |
| **Q1** | 城镇面板建筑差异化做到哪层？ | (a) 只做地图城堡 / 城镇；(b) `races.md` 要的 **28 张每族建筑图标**；(c) 通用图标 + 族色 `clash` 调色（15 张） | **(b) 现在可以做了**。×4 已拍板，28 张只占 3 人日、图集 1176px 宽。**撤回 V1 的反对意见** |
| **Q3** | 是否接受引入 AI（11 张）？ | (a) 0 张（100% 程序化）；(b) **11 张** | **(b)**。若选 (a)：4 阵营立绘改程序化会明显掉档次（选族页是门面），总工期 −3 人日 |
| **Q4** | 图标从 16/18 统一到 24×24？ | 是 / 否 | **是**（否则加粗描边爆框） |
| **Q5** | 图集 2048² → 1024²？ | 现在做 / 押后 / 不做 | **不做**。×4 后冒险图集用到 17%，1024² 下需 ≈5 个 96px 行，余量紧张。**撤回 V1 的"建议缩到 1024²"** —— 12MB 显存换不来这个风险 |
| **Q6** ⭐ | **动画帧数**：`races.md` §8.3 要 5 动作 / 单位（字面 = 200–300 帧），还是 Lean（每单位 2 帧烘焙 + 程序化演出）？ | (a) 字面 5 动作；(b) **3 帧/单位，其余程序化** | **(b)**。差 **36 人日**（**31.2 vs 66.7**）；且 (a) 下战斗图集必须扩容。依据：1 画布像素 = 手机上 4–6 物理像素，整数位移 + squash&stretch + alpha 淡出已经完全够读。**这是本次唯一真正需要用户拍板的成本决策** |
| **Q7** | `deco_*` 从 6 扩到 12 种？ | 扩 / 不扩 | **扩**（成本极低） |
| **Q8** | **喜剧布景层**是否做？荒诞上限？中端是否开「动」？ | 规格全文见 `cartoon-style.md §6.1`；三个待拍板点见 `cartoon-style.md §9` 的 **Q7 / Q8 / Q9** | **做**（≈6.2 人日、冒险图集用到 28.3%、低端归零）。荒诞上限**取严格「每屏 1 处高度荒诞」**；中端**建议**补 1–2 只活动物——中端是默认档，且成本 <1% |

---

## 9. 生产顺序与最小验证批

### 9.1 最小验证批（**B0，必须第一个做**）

> **问题**：先做哪一批，能用最小成本同时证明"管线可用 + 风格成立 + ×4 可读"？

**答案：6 个程序化帧 + 1 张 AI = 7 个资产，1.5 人日。**

| 内容 | 数量 | 覆盖的规格 |
|---|---|---|
| 晨曦 T1「提灯侍从」：`u_p1_lampbearer_map` + `cu_p1_lampbearer` + `cu_p1_lampbearer_atk` | 3 帧 | 32×44 / 44×56 两种画布；1px 描边 |
| 赤焰 T1「捡破烂小鬼」：同三帧 | 3 帧 | 同上；**两族并排**（验证横向可辨） |
| AI：`crestL_p1`（256×384 立绘兼徽记） | 1 张 | **七步管线全流程**；降采样 + 钳色后与程序化资产同色域；2px 描边规则 |

**为什么这 7 个是"最小"而不是更小**：它同时覆盖
① 两种画布规格（32×44 / 44×56）
② 两种生产方式（程序化 / AI）
③ 两个族（验证 `§5.6` 的横向可辨通道）
④ 两种描边规则（1px / 2px）
⑤ 管线的全部 7 步
⑥ 同一 tier 的横向对比（验证纵向 tier 通道）

少任何一个，都会漏掉一整类风险。

**B0 验收断言（7 条，全过才进 B1）**

| # | 断言 | 通过标准 |
|---|---|---|
| 1 | 抠底无粉边 | 100% 边缘像素 alpha ∈ {0, 255}，无洋红残留 |
| 2 | 块众数降采样无糊 | 128×128 输出的相邻色差 ≥ 2 个色板台阶（不是连续渐变） |
| 3 | 钳色后同色域 | AI 资产的颜色 100% 属于 `cartoon-style.md §3` 色板 |
| 4 | 描边重跑干净 | 轮廓是 1px 单线，无 2px 断点、无灰边 |
| 5 | 基线对齐 | 两个族的 map 帧并排，脚底基线偏差 = 0px |
| 6 | **去色 32px 可辨**（核心） | 晨曦 T1 与赤焰 T1 去色缩到 32px，3 个盲测者各 3 次**全部答对** |
| 7 | 手机上表情可读 | 44×56 帧在 zoom 2 × dpr 3 下截图，眼睛与嘴线可辨 |

### 9.2 全量生产顺序（按"最早暴露风险"排序）

| 批 | 内容 | 帧数 | 估算 | 为什么这个顺序 |
|---|---|---|---|---|
| **B0** | 验证批（§9.1） | 6 + 1 AI | **1.5 人日** | 先用 7 个资产证明管线与风格；失败只赔 1.5 天 |
| **B1** | **全部族的 T1 + T2**（8 单位） | 24 | **5 人日** | 先做"每族的第一眼"与**远程姿态**（T2 的投射物是剪影最独特的一档）；最早暴露"形状家族是否成立" |
| **B2** | **全部族的 T3 + T4**（8 单位） | 24 | **5 人日** | T3 的"板"与 T4 的"高耸结构 / 坐骑"是体量差最大的一档；紫晶 T4（唯一远程精英）必须早验 |
| **B3** | **全部族的 T5**（4 单位） | 12 | **3 人日** | 冠军最贵、笑点最集中，放最后以便吸收前三批的经验 |
| **B4** | 建筑图标 28 + 共用 15 + UI 12 + 地标 6 + 徽记 5 + 怪物 4 + 装饰 6 + VFX 12 | 88 | **5.5 人日** | 与 B1–B3 无依赖，可并行给第二个人 |
| **B5** | 城镇 / 城堡 4 族风格 + AI 余下 10 张 | 32 + 10 AI | **7 人日** | 需要 B4 的图标作参照；AI 城堡要先有 B0 验证过的管线 |
| **B6** | 动画扩充（**按 B0–B3 实测决定补哪些动作**） | 预留 | **0–5 人日** | 不预先承诺。B0 的第 7 条断言会告诉我们"程序化演出"够不够 |
| **B7** | **喜剧布景层**（`cartoon-style.md §6.1`：地面涂鸦 12 + 立体道具 14 + 活动物 12） | **38 + 0 AI** | **3.7 人日**（工程另 +2.5） | 与 B1–B5 **零依赖**（不改任何现有资产）、**可与其他批并行**；建议与 B4 同批交付 |
| **合计** | **≈229 帧 + 11 张 AI** | | **≈31.2–35.7 人日** | |

### 9.3 风险清单（诚实版）

| 风险 | 影响 | 触发条件 | 缓解 |
|---|---|---|---|
| ⚠️ **R-1 形状家族只在 T5 成立** | ×4 退化为"四套换色 + 额外帽子"，4 倍成本换 0 增量可读性 | B1 的断言 6 不过 | **B1 之后立刻做 20 格盲测**（`cartoon-style.md §5.6.5`）；命中率 < 70% → 回退共用换皮，**止损在第 6.5 人日** |
| ⚠️ **R-2 动画帧数失控** | 31.2 人日 → 66.7 人日；战斗图集须扩容 | Q6 选 (a) | 见 §8 Q6，**建议 (b)** |
| ⚠️ **R-3 20 个独立设计的一致性漂移** | 同一族 5 个单位越画越不像 | 单人连续作业 >3 个单位 | B1–B3 每批结束后跑一次族内 5 格一致性检查（头部 / 手 / 道具比例是否同源） |
| ⚠️ **R-4 AI 城堡与程序化城镇不像一家人** | 地图上主城与分城风格断裂 | B5 的 AI-2 未与程序化母题对齐 | 用 §6 第 4 条的降风险技巧：**AI 只覆盖屋顶母题 + 装饰，结构由程序化给** |
| ⚠️ **R-5 `races.ts` 结构未定** | 我方帧名与 D1 的 `UnitType.id` 不一致 → 返工 | D1 交付前先开工 | 帧名的**族 / tier 部分已锁定**（`u_<faction>_<tier>_*` 可用 tier 号兜底），中文名映射等 D1 |
| ℹ️ **R-6 图集容量** | — | — | **实测结论：不是风险**（冒险 **28.3%**（含布景层）/ 战斗 33.6%）。除非 Q6 选 (a) |
| ⚠️ **R-7 布景层破坏 §1.1 / §1.3** | 加粗描边把画面糊成一团墨；多处失调使搞笑塌成乱码 | 布景被画得比主体更卡通 | 用 `cartoon-style.md §6.1.7` 的 C1 / C3 / C5 三条守则做**批次级自检**（描边宽度、每屏荒诞处数、每道具失调处数） |

---

## 10. 一页速览（给主理人）

| 问题 | 答案 |
|---|---|
| ×4 之后还要不要 AI 生成？ | **要，但仍是 11 张，且兵种 0 张 AI。** ×4 让程序化从"推荐"变成"唯一可行解"（§4.2 R5）。**新增的 38 帧喜剧布景也是 0 张 AI** |
| 20 个兵种 ×3 帧，2048² 装得下吗？ | ✅ **装得下**。冒险图集用到 **28.3%**（含新增的喜剧布景层 38 帧），战斗图集用到 **33.6%**。不需要第二张图集、不需要改网格、不需要 re-pack |
| 要花多久？ | **≈31.2 人日**（Lean，含布景层 3.7）/ **≈66.7 人日**（若按 `races.md` §8.3 字面做 5 动作/单位） |
| **喜剧布景层值不值得做？**（`cartoon-style.md §6.1`） | **值**：美术 **3.7 人日** + 工程 **2.5 人日** = **≈6.2 人日**；运行时 **<3% 帧预算、低端归零**；不改任何地形砖。是目前**唯一**能让「地图也搞笑」的改法 |
| 先做什么？ | **B0 验证批：6 程序化帧 + 1 张 AI = 7 个资产，1.5 人日**，7 条断言全过才铺开（§9.1） |
| 最大风险是什么？ | **R-1：形状家族只在 T5 成立** → ×4 白花钱。判定点在 **B1 之后（第 6.5 人日）的 20 格盲测**，命中率 < 70% 就回退 |
| 唯一真需用户拍板的成本决策 | **Q6 动画帧数**（差 36 人日） |
