node_modules/
dist/
# 临时构建目录（隔离验证用）：别让它们在版本库里、也别让它们被误服务
# （"磁盘上第二个能被误服务的构建"已害过我们一次 —— 见 roadmap 2026-09-21）
dist-*/
.DS_Store
*.log
.workbuddy/
# aidiagnose 探针的生成数据（可随时重跑）
homm-web/tools/_aidiagnose.json
# 签名密钥：永远不要提交（Android keystore / iOS 描述文件）
*.jks
*.keystore
*.p12
*.p8
*.mobileprovision
homm-web/android/local.properties
